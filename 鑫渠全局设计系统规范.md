# 鑫渠全局设计系统规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.7.0  
> **适用范围**：鑫渠 CRM 全部前端页面与组件  
> **文档性质**：视觉、字体、颜色、图标、主题、响应式设计指导规范

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：字体、字号、颜色、图标、主题、响应式、组件基础规范 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、设计目标

1. **视觉一致性**：全系统使用统一的字体、颜色、间距、圆角、阴影
2. **可访问性**：支持主题切换、字号放大，保证视觉障碍用户基本可用
3. **工程化落地**：所有样式基于 CSS Variables 和 rem，支持动态主题和缩放
4. **矢量图标**：所有图标使用 SVG，放缩不失真
5. **响应式适配**：在桌面、平板、移动端都能良好展示

---

## 二、字体规范

### 2.1 字体栈

```css
:root {
  --font-family-base: 'Inter', 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif;
  --font-family-mono: 'SF Mono', 'Fira Code', 'Consolas', monospace;
}
```

**说明**：
- 英文、数字优先使用 Inter，保证等宽数字显示
- 中文优先使用系统字体 PingFang SC（macOS）/ Microsoft YaHei（Windows）
- Noto Sans SC 作为跨平台兜底
- 等宽字体用于金额、编号、代码等场景

### 2.2 根字体大小

```css
html {
  font-size: 14px;
}

/* 响应式根字体 */
@media (min-width: 1920px) {
  html { font-size: 15px; }
}

@media (min-width: 2560px) {
  html { font-size: 16px; }
}
```

### 2.3 字号体系

全部使用 rem，以 14px 为基准：

| 名称 | rem | px（基准） | 用途 |
|---|---|---|---|
| text-2xs | 0.714rem | 10px | 极少使用，避免 |
| text-xs | 0.786rem | 11px | 标签、时间戳、辅助说明 |
| text-sm | 0.857rem | 12px | 表头、小字说明 |
| text-base | 0.929rem | 13px | 正文辅助、次要内容 |
| text-body | 1rem | 14px | 默认正文 |
| text-md | 1.071rem | 15px | 卡片标题、强调文字 |
| text-lg | 1.214rem | 17px | 页面二级标题、区块标题 |
| text-xl | 1.429rem | 20px | 页面主标题 |
| text-2xl | 1.714rem | 24px | KPI 数值、大标题 |
| text-3xl | 2rem | 28px | 数据大屏大标题 |
| text-4xl | 2.286rem | 32px | 超大标题、欢迎语 |

### 2.4 字重规范

| 字重 | 数值 | 用途 |
|---|---|---|
| regular | 400 | 正文 |
| medium | 500 | 按钮、标签 |
| semibold | 600 | 标题、强调 |
| bold | 700 | KPI 数值、大标题 |

### 2.5 行高规范

| 场景 | 行高 |
|---|---|
| 单行文本 | 1.4 |
| 多行正文 | 1.6 |
| 标题 | 1.3 |
| 紧凑信息 | 1.25 |

---

## 三、颜色规范

### 3.1 颜色使用 CSS Variables

所有颜色必须引用 CSS 变量，禁止写死色值。

```css
:root {
  /* 品牌色 */
  --primary: #3370FF;
  --primary-hover: #2E63E8;
  --primary-light: #E9EFFF;
  --primary-dark: #1E4ECC;

  /* 功能色 */
  --success: #34C724;
  --success-bg: #E9F7E8;
  --success-hover: #2DB21A;
  
  --warning: #FF8800;
  --warning-bg: #FFF3E5;
  --warning-hover: #E67A00;
  
  --danger: #F54A45;
  --danger-bg: #FDECEA;
  --danger-hover: #DC3F3A;
  
  --info: #3370FF;
  --info-bg: #E9EFFF;

  /* 中性色 */
  --bg: #F5F6F7;
  --card: #FFFFFF;
  --panel: #FAFBFC;
  --ink: #1A1D21;
  --sub: #6B7280;
  --placeholder: #9CA3AF;
  --line: #E5E7EB;
  --line-light: #F3F4F6;
  --gray-bg: #EEEFF2;
  --disabled: #C4C4C4;
  --disabled-bg: #F3F4F6;

  /* 阴影 */
  --shadow-card: 0 1px 2px rgba(31,35,41,.04), 0 4px 16px rgba(31,35,41,.06);
  --shadow-panel: 0 1px 2px rgba(31,35,41,.03), 0 4px 16px rgba(31,35,41,.05);
  --shadow-hover: 0 2px 4px rgba(31,35,41,.05), 0 8px 24px rgba(31,35,41,.07);
  --shadow-modal: 0 20px 60px rgba(0,0,0,.2);
}
```

### 3.2 深色主题

```css
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #1A1D21;
    --card: #23272F;
    --panel: #2A2E37;
    --ink: #F5F6F7;
    --sub: #9CA3AF;
    --placeholder: #6B7280;
    --line: #374151;
    --line-light: #2A2E37;
    --gray-bg: #2A2E37;
    --disabled: #4B5563;
    --disabled-bg: #2A2E37;
    
    --primary: #4C88FF;
    --primary-hover: #5A94FF;
    --primary-light: #1E3A66;
    
    --success-bg: #1A3D17;
    --warning-bg: #4D2E00;
    --danger-bg: #4D1A18;
    --info-bg: #1E3A66;
    
    --shadow-card: 0 1px 2px rgba(0,0,0,.2), 0 4px 16px rgba(0,0,0,.3);
    --shadow-hover: 0 2px 4px rgba(0,0,0,.3), 0 8px 24px rgba(0,0,0,.4);
  }
}
```

### 3.3 主题切换

提供手动切换开关，同时监听系统主题：

```javascript
// 监听系统主题
const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
mediaQuery.addEventListener('change', (e) => {
  document.documentElement.setAttribute('data-theme', e.matches ? 'dark' : 'light');
});
```

```css
html[data-theme="dark"] {
  /* 深色主题变量 */
}
```

### 3.4 颜色语义

| 颜色 | 用途 |
|---|---|
| --primary | 主按钮、链接、选中状态 |
| --success | 完成、健康、正向指标 |
| --warning | 待处理、关注、进行中 |
| --danger | 风险、错误、删除、冲突 |
| --info | 提示、进行中 |
| --ink | 主文字 |
| --sub | 次要文字 |
| --placeholder | 占位文字 |
| --line | 边框、分割线 |
| --bg | 页面背景 |
| --card | 卡片背景 |

---

## 四、图标规范

### 4.1 全部使用 SVG 矢量图标

禁止使用 emoji、PNG、JPG 作为图标。

### 4.2 图标实现方式

```html
<svg class="icon icon-nav" viewBox="0 0 24 24" aria-hidden="true">
  <path fill="currentColor" d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"/>
</svg>
```

```css
.icon {
  width: 1em;
  height: 1em;
  fill: currentColor;
  flex-shrink: 0;
  display: inline-block;
  vertical-align: middle;
}

.icon-xs { font-size: 12px; }
.icon-sm { font-size: 14px; }
.icon-md { font-size: 16px; }
.icon-lg { font-size: 20px; }
.icon-xl { font-size: 24px; }
.icon-2xl { font-size: 32px; }
```

### 4.3 图标尺寸规范

| 场景 | 尺寸类 | 实际尺寸 |
|---|---|---|
| 侧边栏导航 | icon-lg | 20px |
| 应用中心 | icon-2xl | 32px |
| 按钮内图标 | icon-sm | 14px |
| 表格操作图标 | icon-md | 16px |
| 状态标签图标 | icon-xs | 12px |
| 弹窗标题图标 | icon-xl | 24px |

### 4.4 图标颜色

- 默认继承父元素颜色（`currentColor`）
- 悬停时跟随文字颜色变化
- 选中状态使用 `--primary`
- 禁用状态使用 `--disabled`

### 4.5 常用图标清单

| 图标 | 用途 |
|---|---|
| home | 工作台 |
| message | 消息 |
| calendar | 日历 |
| contacts | 通讯录 |
| task | 任务 |
| apps | 应用中心 |
| star | 收藏 |
| customer | 客户 360° |
| lead | 线索管理 |
| opportunity | 意向管理 |
| approval | 审批中心 |
| equipment | 设备管理 |
| reagent | 试剂运营 |
| ticket | 售后工单 |
| kanban | 工单看板 |
| brand | 品牌库管理 |
| dealer | 经销商协同 |
| compliance | 合规风控 |
| performance | 目标绩效 |
| dashboard | 经营驾驶舱 |
| settings | 后台设置 |
| search | 搜索 |
| filter | 筛选 |
| plus | 新建 |
| edit | 编辑 |
| delete | 删除 |
| more | 更多 |
| arrow-right | 下一步 |
| arrow-left | 返回 |
| check | 完成 |
| close | 关闭 |
| bell | 通知 |
| download | 下载 |
| upload | 上传 |

---

## 五、间距与圆角

### 5.1 间距体系

| 名称 | 数值 | 用途 |
|---|---|---|
| space-1 | 4px | 图标与文字间距 |
| space-2 | 8px | 按钮内图标间距、小元素间距 |
| space-3 | 12px | 卡片内小间距 |
| space-4 | 16px | 卡片内边距、模块间距 |
| space-5 | 20px | 页面内边距 |
| space-6 | 24px | 弹窗内边距 |
| space-8 | 32px | 大模块间距 |
| space-10 | 40px | 页面区块间距 |

### 5.2 圆角体系

| 名称 | 数值 | 用途 |
|---|---|---|
| radius-sm | 4px | 小标签、输入框 |
| radius-md | 8px | 按钮、小卡片 |
| radius-lg | 12px | 卡片、面板 |
| radius-xl | 16px | 弹窗、大卡片 |
| radius-full | 999px | 标签、头像、进度条 |

---

## 六、响应式规范

### 6.1 断点

```css
/* 移动端 */
@media (max-width: 767px) { }

/* 小屏 */
@media (min-width: 768px) and (max-width: 1023px) { }

/* 中屏 */
@media (min-width: 1024px) and (max-width: 1439px) { }

/* 大屏 */
@media (min-width: 1440px) { }

/* 超大屏 */
@media (min-width: 1920px) { }
```

### 6.2 各断点适配策略

| 断点 | 策略 |
|---|---|
| 大屏 ≥1440px | 4 列卡片，完整侧边栏，所有筛选条件平铺 |
| 中屏 1024-1440px | 3 列卡片，侧边栏可折叠，筛选条件部分折叠 |
| 小屏 768-1024px | 2 列卡片，简化筛选，抽屉式导航 |
| 移动端 <768px | 单列卡片，底部固定操作栏，底部 Tab 导航 |

### 6.3 页面最小宽度

- 桌面端最小宽度 1280px
- 低于最小宽度时显示横向滚动，不破坏布局
- 移动端按移动优先设计

---

## 七、字号放大适配

### 7.1 适配原则

- 全部使用 rem 单位
- 容器宽度使用百分比、flex、max-width
- 按钮使用 `white-space: nowrap` 和 `flex-shrink: 0`
- 文本溢出使用 ellipsis 或 marquee
- 图标尺寸使用 em/rem

### 7.2 测试场景

| 缩放比例 | 检查项 |
|---|---|
| 100% | 基准布局正常 |
| 125% | 按钮不换行、文本不遮挡、侧边栏不溢出 |
| 150% | 卡片不重叠、表格可操作、弹窗完整显示 |
| 200% | 主要功能可操作，移动端布局生效 |

### 7.3 防换行/防遮挡规范

```css
/* 按钮不换行 */
.btn {
  white-space: nowrap;
  min-width: fit-content;
  flex-shrink: 0;
}

/* 文本溢出省略 */
.text-ellipsis {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  min-width: 0;
}

/* 容器防止被压缩 */
.flex-container {
  display: flex;
  min-width: 0;
}

/* 卡片最小宽度 */
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 16px;
}
```

---

## 八、组件基础规范

### 8.1 按钮

```css
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 7px 16px;
  border-radius: 8px;
  font-size: 0.929rem;
  font-weight: 500;
  line-height: 1.4;
  white-space: nowrap;
  flex-shrink: 0;
  cursor: pointer;
  transition: all 0.2s ease;
}

.btn-primary {
  background: var(--primary);
  color: white;
}

.btn-primary:hover {
  background: var(--primary-hover);
}

.btn-ghost {
  background: var(--gray-bg);
  color: var(--ink);
}
```

### 8.2 卡片

```css
.card {
  background: var(--card);
  border-radius: 12px;
  box-shadow: var(--shadow-card);
  padding: 16px;
}

.card-hover {
  transition: all 0.2s ease;
}

.card-hover:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-hover);
}
```

### 8.3 输入框

```css
.input {
  width: 100%;
  padding: 8px 12px;
  border: 1px solid var(--line);
  border-radius: 8px;
  font-size: 0.929rem;
  line-height: 1.4;
  background: var(--card);
  color: var(--ink);
}

.input:focus {
  outline: none;
  border-color: var(--primary);
}
```

### 8.4 标签

```css
.badge {
  display: inline-flex;
  align-items: center;
  padding: 2px 10px;
  border-radius: 999px;
  font-size: 0.786rem;
  font-weight: 500;
  line-height: 1.4;
  white-space: nowrap;
}
```

---

## 九、主题切换实现

### 9.1 切换开关

```html
<button class="theme-toggle" onclick="toggleTheme()">
  <svg class="icon icon-md"><!-- sun/moon icon --></svg>
</button>
```

### 9.2 JavaScript

```javascript
function toggleTheme() {
  const html = document.documentElement;
  const current = html.getAttribute('data-theme') || 'light';
  const next = current === 'light' ? 'dark' : 'light';
  html.setAttribute('data-theme', next);
  localStorage.setItem('theme', next);
}

// 初始化
const savedTheme = localStorage.getItem('theme');
const systemDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
document.documentElement.setAttribute('data-theme', savedTheme || (systemDark ? 'dark' : 'light'));
```

---

## 十、禁止事项

- 禁止在代码中写死颜色值（必须使用 CSS 变量）
- 禁止使用 emoji 或图片作为图标
- 禁止使用 px 定义字体大小（除根字体外）
- 禁止按钮文字换行
- 禁止同一组件在不同页面样式不一致
- 禁止忽略深色主题适配
- 禁止在字号放大时破坏布局

---

*文档结束*
