# 鑫渠响应式设计规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **适用范围**：鑫渠 CRM 全部前端页面与组件  
> **文档性质**：AI 开发时的响应式实现标准

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：定义断点、布局规则、组件适配、字号缩放、主题适配、手势规范 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、断点定义

鑫渠 CRM 统一使用以下 4 个断点：

| 断点名 | 宽度范围 | 设备类型 | 布局策略 |
|---|---|---|---|
| `xs` | < 768px | 手机 | 单列，底部固定操作，抽屉全屏 |
| `sm` | 768px - 1023px | 平板 | 双列或单列，侧边栏可折叠 |
| `md` | 1024px - 1439px | PC 标准 | 多列，左侧固定导航 |
| `lg` | >= 1440px | PC 宽屏 | 多列，更宽内容区 |

Tailwind 类名对应：
- `sm:` = 640px 起
- `md:` = 768px 起
- `lg:` = 1024px 起
- `xl:` = 1280px 起
- `2xl:` = 1536px 起

本规范在 Tailwind 基础上做业务封装，要求使用 `xs` / `sm` / `md` / `lg` 四个语义断点。

---

## 二、页面布局适配规则

### 2.1 列表型页面

#### PC 端（md/lg）

```
┌─────────────────────────────────────────────┐
│ 标题                              [主操作]   │
├─────────────────────────────────────────────┤
│ 统计卡片（3-5 个横向排列）                   │
├─────────────────────────────────────────────┤
│ Tab + 视图切换 + 快捷操作                    │
├─────────────────────────────────────────────┤
│ 搜索 + 筛选（横向平铺）                      │
├─────────────────────────────────────────────┤
│ 卡片网格 4/3 列 或 表格                      │
├─────────────────────────────────────────────┤
│ 分页                                         │
└─────────────────────────────────────────────┘
```

#### 移动端（xs）

```
┌─────────────────────────────────┐
│ ← 标题                [+]       │
├─────────────────────────────────┤
│ [搜索框 =========] [筛选]       │
├─────────────────────────────────┤
│ Tab 横向滚动                    │
├─────────────────────────────────┤
│ 统计卡片横向滚动                │
├─────────────────────────────────┤
│ 卡片单列                        │
│ 卡片单列                        │
│ 卡片单列                        │
├─────────────────────────────────┤
│ [加载更多]                      │
└─────────────────────────────────┘
```

**规则**：
- 统计卡片在移动端横向滚动，每个卡片最小宽度 120px。
- Tab 在移动端横向滚动，不超过屏幕宽度时居中。
- 筛选在移动端收起为底部抽屉。
- 卡片网格在移动端强制 1 列。
- 分页在移动端简化为「加载更多」，或显示为「1/10 页」。

---

### 2.2 详情型页面

#### PC 端

- 列表 + 详情双态结构：左侧列表，右侧抽屉或详情页。
- 抽屉宽度：默认 720px，复杂表单 960px，简单信息 480px。

#### 移动端

- 列表全屏，点击进入新页面详情。
- 详情页顶部固定返回按钮 + 标题。
- 底部固定主操作按钮（如「保存」「提交」）。
- 长表单分组折叠，默认展开第一组。

---

### 2.3 表单页面

#### PC 端

- 表单字段 2-3 列布局。
- 标签右对齐或顶部对齐，统一使用顶部对齐。
- 底部固定操作栏：取消、保存、提交。

#### 移动端

- 表单字段单列。
- 标签顶部对齐。
- 必填项用红色星号标识。
- 底部固定操作按钮，不遮挡内容。

---

## 三、组件适配规则

### 3.1 表格与卡片切换

- PC 端：默认列表视图，支持表格横向滚动。
- 移动端：默认卡片视图，表格需支持横向滚动或卡片化。

### 3.2 按钮

| 场景 | PC 端 | 移动端 |
|---|---|---|
| 主操作 | 正常尺寸 | 底部全宽固定 |
| 行内操作 | 文字链接/小按钮 | 长按弹出操作菜单 |
| 批量操作 | 顶部工具条 | 底部弹出操作面板 |

**禁止**：移动端按钮换行、两个主按钮并列。

### 3.3 抽屉/弹窗

| 场景 | PC 端 | 移动端 |
|---|---|---|
| 详情抽屉 | 右侧 720px | 全屏 |
| 表单抽屉 | 右侧 560px | 全屏 |
| 确认弹窗 | 居中 400px | 底部弹出 |
| 筛选抽屉 | 左侧/右侧 320px | 底部弹出 |

### 3.4 导航

| 场景 | PC 端 | 移动端 |
|---|---|---|
| 全局导航 | 左侧侧边栏 | 底部 Tab + 顶部汉堡菜单 |
| 页面内 Tab | 顶部横向 | 顶部横向滚动 |
| 返回 | 面包屑/返回按钮 | 左上角返回箭头 |

---

## 四、字号与缩放适配

### 4.1 基础字号

| 层级 | 字号 | 行高 | 用途 |
|---|---|---|---|
| 页面标题 | 17px | 24px | 页面标题 |
| 模块标题 | 15px | 22px | 卡片标题、分组标题 |
| 正文 | 14px | 22px | 列表、表单、描述 |
| 辅助文字 | 13px | 20px | 提示、次要信息 |
| 微小文字 | 12px | 18px | 标签、时间 |

### 4.2 用户放大字号

系统必须支持用户将基础字号放大到 125%、150%、175%。

**适配规则**：
- 使用 rem/em 或 CSS 变量，禁止写死 px 作为关键尺寸。
- 字号放大时，按钮、输入框、卡片高度同步放大。
- 文字放大后不能遮挡、不能换行破坏布局。
- 统计卡片在字号放大时允许横向滚动，但不允许换行。

---

## 五、主题适配

### 5.1 浅色/深色主题

所有组件必须使用 CSS 变量，禁止硬编码颜色。

```css
:root {
  --bg-body: #f8fafc;
  --bg-card: #ffffff;
  --text-main: #0f172a;
  --text-sub: #475569;
  --border: #e2e8f0;
  --primary: #2563eb;
}

.dark {
  --bg-body: #0f172a;
  --bg-card: #1e293b;
  --text-main: #f1f5f9;
  --text-sub: #94a3b8;
  --border: #334155;
  --primary: #60a5fa;
}
```

### 5.2 主题色切换

支持主色切换（如蓝、绿、紫），所有使用主题色的地方必须引用 `--primary` 变量。

---

## 六、手势规范

| 手势 | 触发场景 | 操作 |
|---|---|---|
| 下拉刷新 | 移动端列表 | 刷新当前列表 |
| 上拉加载 | 移动端列表 | 加载下一页 |
| 左滑卡片 | 移动端卡片 | 显示快捷操作 |
| 长按 | 移动端列表项 | 弹出操作菜单 |
| 点击 | 通用 | 进入详情/触发操作 |
| 双指缩放 | 日历/地图/图表 | 放大/缩小 |

---

## 七、适配检查清单

每个页面开发完成后，必须检查以下项目：

| 检查项 | 通过标准 |
|---|---|
| 移动端不横向滚动（除表格/卡片必要场景） | 无意外横向溢出 |
| 按钮不换行 | 一行内完整显示 |
| 输入框不被遮挡 | 键盘弹出时输入框可见 |
| 字号放大 150% 可用 | 布局不破坏，文字可读 |
| 深色主题可用 | 颜色对比度符合 WCAG AA |
| 移动端底部按钮固定 | 主操作随时可点 |
| 抽屉/弹窗全屏正确 | 无错位、无遮挡 |

---

## 八、常见反模式

| 反模式 | 正确做法 |
|---|---|
| PC 端表格直接搬到移动端 | 移动端使用卡片视图 |
| 移动端按钮换行显示 | 底部固定单按钮或收起为更多菜单 |
| 写死 px 尺寸 | 使用 rem/变量/Tailwind 响应式类 |
| 忽略键盘弹出 | 表单页预留键盘空间 |
| 深色模式只反色背景 | 所有颜色都使用 CSS 变量 |

---

## 九、相关文档索引

- 《AI可执行设计文档编写规范.md》
- 《鑫渠组件库清单与命名规范.md》
- 《鑫渠全局设计系统规范.md》
- 《鑫渠系统技术实现白皮书.html》

---

*本规范是鑫渠 CRM 双端一致体验的基础，所有 AI 开发任务必须遵循。*
