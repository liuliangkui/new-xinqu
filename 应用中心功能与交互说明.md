# 应用中心功能与交互说明

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **对应规范**：《AI可执行设计文档编写规范.md》v1.0、《鑫渠组件库清单与命名规范.md》v1.0、《鑫渠响应式设计规范.md》v1.0、《鑫渠字段规范模板.md》v1.0、《鑫渠验收断言编写规范.md》v1.0、《鑫渠应用文档验收标准.md》v1.1  
> **适用范围**：鑫渠 CRM 全部前端入口（PC 管理端、企业微信、小程序）  
> **适用角色**：总经理、销售总监、区域总监、销售代表、售后总监、经销商管理员、合规专员、系统管理员  

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：应用中心模块定位、业务域分组、应用卡片、工作台添加/移除、权限渲染、接口契约与验收断言 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 机器指令块

开发本模块前，AI 必须先读取并遵守以下全局约束：

- **技术栈**：Vue 3 + TypeScript + Pinia + Vue Router + Ant Design Vue 4.x + Tailwind CSS
- **组件库**：必须使用《鑫渠组件库清单与命名规范.md》中注册的组件名，禁止自行发明组件名
- **图标**：全部使用 SVG 图标或 Iconfont，禁止引用 PNG/JPG 图标
- **样式变量**：颜色、字号、间距必须使用《鑫渠全局设计系统规范.md》中的 CSS 变量
- **响应式断点**：
  - PC 宽屏：>=1440px
  - PC 标准：1024px-1439px
  - 平板：768px-1023px
  - 移动端：<768px
- **权限模型**：基于 RBAC + 数据范围，角色编码见《华鑫角色职责与用户故事.md》
- **数据规范**：字段命名必须与本文档「核心字段规范」完全一致
- **接口规范**：接口路径、字段命名必须与本文档「接口契约」完全一致
- **验收标准**：开发完成后必须对照本文档「验收断言清单」逐项验证

---

## 一、模块定位与目标

### 1.1 模块定位

应用中心是鑫渠 CRM 的**平台级应用分发与权限入口**，按业务域分组展示系统中全部业务应用。用户可在此查看自身有权限的应用、将常用应用添加到工作台快捷入口，或从工作台移除已添加的应用。

### 1.2 一句话定位

应用中心是「业务应用的统一目录与权限入口，连接系统能力与用户工作台」。

### 1.3 解决的核心问题

- 业务应用数量多、入口分散，用户难以快速找到目标功能。
- 不同角色可见的业务应用不同，缺少统一的权限感知入口。
- 高频应用需要反复通过菜单查找，缺少个性化快捷入口。
- 新增应用后，管理员需要手动配置导航与角色可见范围，维护成本高。
- 移动端业务应用入口与 PC 端不一致，导致双端体验割裂。

### 1.4 带来的业务价值

| 价值点 | 说明 |
|---|---|
| 入口统一 | 所有业务应用集中在应用中心，按业务域分组，降低查找成本。 |
| 权限感知 | 无权限应用自动置灰并禁用点击，避免用户误操作与权限困惑。 |
| 个性定制 | 用户可将常用应用添加到工作台，形成千人千面的工作门户。 |
| 配置驱动 | 应用的分组、排序、图标、权限码由后台配置，减少前端硬编码。 |
| 双端一致 | PC 端与移动端共用同一套应用元数据与权限模型，体验一致。 |

### 1.5 所属业务域

平台入口 / 系统管理域。

---

## 二、业务场景与角色

### 2.1 使用角色

| 角色编码 | 角色名称 | 关注重点 | 典型操作 |
|---|---|---|---|
| CEO | 总经理 | 全公司应用可访问、经营驾驶舱与数据决策类应用 | 查看全部应用、添加驾驶舱到工作台 |
| SALES_DIRECTOR | 销售总监 | 业务管理类应用、审批与渠道协同 | 添加意向管理、审批中心到工作台 |
| REGION_MANAGER | 区域总监 | 本区域业务应用、团队管理与数据看板 | 添加客户 360°、目标绩效到工作台 |
| SALES_REP | 销售代表 | 个人业务应用、客户与线索跟进 | 添加线索管理、客户 360° 到工作台 |
| SERVICE_DIRECTOR | 售后总监 | 售后工单与设备相关应用 | 添加售后工单、工单看板到工作台 |
| DEALER_MANAGER | 经销商管理员 | 渠道协同与部分业务应用 | 添加经销商协同到工作台 |
| COMPLIANCE_OFFICER | 合规专员 | 合规审计与审批类应用 | 添加合规风控、审批中心到工作台 |
| SYSTEM_ADMIN | 系统管理员 | 系统管理与应用配置 | 添加后台设置、流程设计器到工作台 |

### 2.2 典型业务场景

| 场景编号 | 角色 | 触发条件 | 操作流程 | 预期结果 |
|---|---|---|---|---|
| AC-01 | 销售代表 | 新入职后首次使用系统 | 登录 -> 进入应用中心 -> 浏览业务管理类应用 -> 点击「添加到工作台」 | 工作台「我的应用」区出现该应用快捷入口 |
| AC-02 | 销售总监 | 需要快速进入审批中心 | 在工作台点击「我的应用」区入口 -> 若未添加则跳转应用中心 -> 添加审批中心 | 审批中心出现在工作台并可一键进入 |
| AC-03 | 系统管理员 | 新增业务应用或调整分组 | 进入后台设置 -> 应用配置 -> 维护应用元数据 -> 发布 -> 用户刷新应用中心 | 应用中心按最新分组与权限展示应用 |
| AC-04 | 区域总监 | 查看团队数据看板 | 进入应用中心 -> 在数据决策域找到经营驾驶舱 -> 添加到工作台 | 驾驶舱成为个人高频入口 |
| AC-05 | 售后总监 | 日常处理售后工单 | 工作台已添加工单看板 -> 点击进入 -> 处理工单 | 无需每次都从应用中心查找 |
| AC-06 | 合规专员 | 定期审计风险数据 | 工作台已添加合规风控 -> 点击进入 -> 查看预警 | 高频入口固定，提升效率 |

---

## 三、核心功能清单

| 序号 | 功能名称 | 优先级 | 功能说明 | 原型对应 |
|---|---|---|---|---|
| 1 | 应用域分组展示 | 必要 | 按业务管理、渠道协同、合规审计、数据决策、系统管理五大域分组展示应用卡片。 | 应用中心主页面 |
| 2 | 应用卡片渲染 | 必要 | 每个应用显示 SVG 图标、应用名称、一句话描述、添加/已添加/无权限状态。 | 应用中心网格区 |
| 3 | 添加到工作台 | 必要 | 用户点击「添加到工作台」后，该应用进入工作台「我的应用」区，并持久化到个人偏好。 | 应用卡片操作按钮 |
| 4 | 从工作台移除 | 必要 | 已添加应用显示「已添加」按钮，点击后从工作台移除，恢复为可添加状态。 | 应用卡片操作按钮 |
| 5 | 权限感知显示 | 必要 | 根据当前角色权限码，无权限应用置灰并禁用点击，按钮显示「无权限」。 | 应用卡片透明度与禁用态 |
| 6 | 应用搜索 | 重要 | 支持按应用名称、拼音首字母、业务域关键词搜索，实时过滤应用卡片。 | 应用中心顶部搜索区 |
| 7 | 应用快捷进入 | 重要 | 有权限的应用卡片点击进入对应页面，无权限应用不可点击。 | 应用卡片点击事件 |
| 8 | 推荐应用提示 | 重要 | 根据角色推荐默认应用，未添加的默认应用可一键批量添加。 | 页面顶部提示区 |
| 9 | 移动端适配 | 必要 | 移动端单列卡片、底部固定操作、手势下拉刷新。 | 小程序/企业微信应用中心页 |
| 10 | 应用元数据配置 | 重要 | 后台维护应用 ID、名称、编码、图标、描述、分组、排序、权限码、默认应用标识。 | 后台设置 - 应用配置 |

---

## 四、业务流程

### 4.1 流程一：应用添加到工作台

```
用户进入应用中心
    │
    v
系统加载当前角色可访问应用列表与已添加应用列表
    │
    v
用户点击某个有权限应用的「添加到工作台」按钮
    │
    v
前端校验：是否已添加 / 是否超出工作台最大应用数
    │
    ├── 校验失败 ──-> 提示「已添加」或「工作台应用数量已达上限」
    │
    └── 校验通过 ──-> 调用 addAppToWorkbench 接口
                            │
                            v
                    后端更新用户应用偏好
                            │
                            v
                    成功后刷新应用中心卡片状态
                            │
                            v
                    同时刷新工作台「我的应用」区
```

| 步骤 | 操作内容 | 负责角色 | 系统校验 | 异常分支 |
|---|---|---|---|---|
| 1 | 用户从侧边栏或工作台进入应用中心。 | 任意登录用户 | 校验用户登录态与角色。 | 未登录跳转登录页。 |
| 2 | 系统根据角色权限码加载应用列表与分组。 | 系统 | 校验角色配置存在。 | 角色配置缺失时显示默认空态。 |
| 3 | 系统加载用户已添加到工作台的应用 ID 列表。 | 系统 | 校验用户偏好记录存在，不存在则返回空数组。 | 偏好读取失败时允许前端使用本地缓存。 |
| 4 | 用户点击「添加到工作台」。 | 用户 | 校验应用未添加且未达上限。 | 已添加则提示；超上限则阻止并提示。 |
| 5 | 前端调用接口保存偏好。 | 前端 | 校验应用 ID 合法、用户有权限。 | 无权限时接口返回 403，前端置灰卡片。 |
| 6 | 后端更新用户应用偏好并返回最新列表。 | 后端 | 幂等校验，避免重复写入。 | 写入失败返回错误码，前端回滚按钮状态。 |
| 7 | 应用中心与工作台同步刷新。 | 前端 | 确保两处状态一致。 | 同步失败时用户手动刷新可恢复。 |

### 4.2 流程二：应用从工作台移除

```
用户进入应用中心
    │
    v
用户点击已添加应用的「已添加」按钮
    │
    v
前端二次确认（可选，若该应用为角色默认应用则提示）
    │
    ├── 取消 ──-> 保持原状态
    │
    └── 确认 ──-> 调用 removeAppFromWorkbench 接口
                        │
                        v
                后端更新用户应用偏好
                        │
                        v
                成功后刷新应用中心与工作台
```

| 步骤 | 操作内容 | 负责角色 | 系统校验 | 异常分支 |
|---|---|---|---|---|
| 1 | 用户在应用中心找到已添加应用。 | 用户 | 按钮显示「已添加」绿色状态。 | - |
| 2 | 用户点击「已添加」按钮。 | 用户 | 无前置校验。 | - |
| 3 | 若该应用为角色默认应用，弹出二次确认提示。 | 系统 | 判断 app.isDefault 是否为 true。 | 用户取消则状态不变。 |
| 4 | 前端调用移除接口。 | 前端 | 校验应用 ID 在用户偏好列表中。 | 接口参数非法返回 400。 |
| 5 | 后端移除对应应用 ID。 | 后端 | 校验用户偏好记录存在。 | 记录不存在时初始化空列表后返回。 |
| 6 | 应用中心与工作台同步刷新。 | 前端 | 状态同步一致。 | 同步失败时用户手动刷新可恢复。 |

### 4.3 应用卡片状态机

| 状态编码 | 状态名称 | 可执行操作 | 下一状态 | 触发条件 |
|---|---|---|---|---|
| 0 | 未添加 | 添加到工作台、进入应用 | 1 | 点击「添加到工作台」且保存成功 |
| 1 | 已添加 | 进入应用、从工作台移除 | 0 | 点击「已添加」按钮并确认移除 |
| 2 | 无权限 | 无 | - | 当前角色权限码不包含应用权限码 |

> 注：「无权限」状态仅前端展示，不进入用户偏好表；有权限但未添加应用默认显示「添加到工作台」。

---

## 五、页面结构与布局

### 5.1 入口

- PC 端：侧边栏一级导航「应用中心」点击后进入 `/app-center`。
- 移动端：底部 Tab 或工作台顶部的「添加应用」链接进入 `/app-center`。
- 快捷入口：工作台「我的应用」区右上角的「添加应用」文字链接。

### 5.2 页面清单

| 页面名称 | 路由/路径 | 页面类型 | 说明 |
|---|---|---|---|
| 应用中心首页 | /app-center | 列表/目录页 | 按业务域分组展示全部应用卡片。 |
| 应用搜索结果 | /app-center?keyword=xxx | 列表/目录页 | 搜索后高亮匹配应用，隐藏不匹配分组。 |
| 应用详情抽屉（可选） | /app-center/detail/:appId 或抽屉 | 详情/抽屉 | 展示应用详细说明、依赖应用、权限说明、最近更新。 |

### 5.3 页面布局（PC 端）

```
┌─────────────────────────────────────────────────────────────────┐
│ 应用中心                                          [? 使用帮助]   │
├─────────────────────────────────────────────────────────────────┤
│ 提示卡片：根据您的角色推荐常用应用，点击添加到工作台以便快速访问。  │
├─────────────────────────────────────────────────────────────────┤
│ [搜索应用...]  [业务管理 下拉]  [全部状态 下拉]  [重置]         │
├─────────────────────────────────────────────────────────────────┤
│ 业务管理                                                        │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                 │
│ │ 图标        │ │ 图标        │ │ 图标        │                 │
│ │ 线索管理    │ │ 意向管理    │ │ 审批中心    │                 │
│ │ 线索分配... │ │ 意向报备... │ │ 通用审批... │                 │
│ │ [添加到...] │ │ [已添加]    │ │ [无权限]    │                 │
│ └─────────────┘ └─────────────┘ └─────────────┘                 │
├─────────────────────────────────────────────────────────────────┤
│ 渠道协同                                                        │
│ ┌─────────────┐                                                 │
│ │ 经销商协同  │                                                 │
│ └─────────────┘                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 合规审计 / 数据决策 / 系统管理 ...                                │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 五层架构说明

| 层级 | 板块 | 用户心理 | 对应组件 |
|---|---|---|---|
| 第一层 | 页面标题 + 帮助入口 | 「我在哪，能做什么？」 | XqPageLayout |
| 第二层 | 推荐提示卡片 | 「系统给我推荐了什么？」 | XqKpiCard / 提示卡片 |
| 第三层 | 搜索 + 业务域筛选 + 重置 | 「我要找哪个应用？」 | XqSearchBar、XqFilterBar |
| 第四层 | 按业务域分组的应用卡片网格 | 「这个应用是做什么的，能不能用？」 | XqCardGrid、XqStatusBadge |
| 第五层 | 无分页，滚动加载更多分组 | 「还有没有其他应用？」 | 原生滚动 |

### 5.5 应用卡片结构

```
┌─────────────────────────────────────┐
│ [SVG 图标]              [操作按钮]  │  (顶部：图标 + 状态按钮)
├─────────────────────────────────────┤
│ 应用名称（14px，font-weight 600）   │  (主体：应用名)
│ 一句话描述（12px，text-sub）        │  (辅助：描述)
└─────────────────────────────────────┘
```

### 5.6 组件映射

| UI 区域 | 组件名 | Props / 说明 |
|---|---|---|
| 页面外层容器 | XqPageLayout | title="应用中心"，showBack=false，showStats=false，showFilter=true |
| 顶部搜索 | XqSearchBar | placeholder="搜索应用名称或拼音首字母"，pinyinSearch=true，allowClear=true，width="280px" |
| 业务域筛选 | XqFilterBar | filters=[{field:'category',options:['业务管理','渠道协同','合规审计','数据决策','系统管理']}] |
| 状态筛选 | XqFilterBar | filters=[{field:'addedStatus',options:['全部','已添加','未添加']}] |
| 应用卡片网格 | XqCardGrid | columns=4（PC 宽屏）/ 3（PC 标准）/ 2（平板）/ 1（移动端） |
| 应用状态标签 | XqStatusBadge | statusMap={0:{text:'添加到工作台',color:'#2563eb'},1:{text:'已添加',color:'#22c55e'},2:{text:'无权限',color:'#94a3b8'}} |
| 空态 | XqEmptyState | type='search' / 'permission'，提示无搜索结果或无权限 |
| 轻提示 | XqToast | 添加/移除成功后提示 |
| 确认弹窗 | XqConfirmDialog | 移除默认应用时二次确认 |

---

## 六、核心字段规范

### 6.1 应用对象字段（app）

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| appId | 应用 ID | String | 32 | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| appCode | 应用编码 | String | 50 | 是 | 无 | 手工录入 | - | 全员可见 | 不显示 |
| appName | 应用名称 | String | 50 | 是 | 无 | 手工录入 | - | 全员可见 | 卡片标题 |
| appDesc | 应用描述 | String | 50 | 是 | 无 | 手工录入 | - | 全员可见 | 卡片辅助文字 |
| appIcon | 应用图标 | String | 50 | 是 | 无 | 字典/手工录入 | - | 全员可见 | SVG 图标组件名 |
| pageRoute | 页面路由 | String | 100 | 是 | 无 | 手工录入 | - | 全员可见 | 点击进入页面 |
| category | 业务域分组 | Enum | - | 是 | 无 | 字典 | 1:业务管理,2:渠道协同,3:合规审计,4:数据决策,5:系统管理 | 全员可见 | 分组标题 |
| sortOrder | 排序号 | Integer | 0-9999 | 是 | 0 | 手工录入 | - | 只读 | 控制组内排序 |
| permissionCode | 权限码 | String | 100 | 是 | 无 | 手工录入 | - | 只读 | 控制可见性 |
| isDefault | 是否默认应用 | Boolean | - | 是 | false | 手工录入 | - | 只读 | 角色默认应用标识 |
| status | 应用状态 | Enum | - | 是 | 1 | 系统生成 | 0:草稿,1:已发布,2:已停用 | 只读 | 状态标签 |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情显示 |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情显示 |
| createBy | 创建人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 不可见 | 不显示 |
| tenantId | 租户 ID | Integer | - | 是 | 当前租户 | 系统生成 | - | 不可见 | 不显示 |

### 6.2 用户应用偏好对象字段（userAppPreference）

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| preferenceId | 偏好记录 ID | Long | - | 是 | 无 | 系统生成 | 只读 | 不显示 |
| userId | 用户 ID | Integer | - | 是 | 当前用户 | 系统生成 | 本人可见 | 不显示 |
| roleCode | 角色编码 | String | 50 | 是 | 当前角色 | 系统生成 | 本人可见 | 不显示 |
| appIds | 已添加应用 ID 列表 | Array<String> | - | 是 | [] | 用户操作 | 本人可见 | 工作台渲染 |
| maxAppCount | 工作台最大应用数 | Integer | 1-50 | 是 | 12 | 系统参数 | 只读 | 不显示 |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | 只读 | 不显示 |

### 6.3 枚举字段详细定义

#### 6.3.1 category 业务域分组

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 1 | 业务管理 | #2563eb | 销售、客户、设备、试剂、售后等一线业务应用 |
| 2 | 渠道协同 | #f59e0b | 经销商、渠道授权、区域保护等协同应用 |
| 3 | 合规审计 | #7c3aed | 合规风控、敏感词审计、飞检抽查等 |
| 4 | 数据决策 | #10b981 | 经营驾驶舱、目标绩效、排行榜等 |
| 5 | 系统管理 | #64748b | 后台设置、流程设计器、应用配置等 |

#### 6.3.2 status 应用状态

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 草稿 | #94a3b8 | 未发布，仅管理员可见 |
| 1 | 已发布 | #22c55e | 正常可用 |
| 2 | 已停用 | #ef4444 | 已下线，所有用户不可见 |

#### 6.3.3 卡片按钮状态

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 添加到工作台 | #2563eb | 有权限且未添加 |
| 1 | 已添加 | #22c55e | 已在工作台 |
| 2 | 无权限 | #94a3b8 | 当前角色无权限 |

---

## 七、交互设计要点

### 7.1 搜索与筛选

- **搜索框**：位于页面第三层左侧，使用 XqSearchBar 组件，placeholder 为「搜索应用名称或拼音首字母」，pinyinSearch=true，allowClear=true。
- **实时搜索**：输入后 debounce 300ms 触发过滤，隐藏不包含匹配应用的分组。
- **业务域筛选**：下拉选择业务域，仅显示选中域下的应用；默认「全部」。
- **状态筛选**：下拉选择「全部 / 已添加 / 未添加」，过滤用户偏好状态。
- **重置按钮**：点击后清空搜索词与筛选条件，恢复默认分组展示。
- **搜索结果空态**：无匹配时显示 XqEmptyState type='search'，提示「未找到匹配应用，请更换关键词」。

### 7.2 操作反馈

- **添加成功**：按钮由「添加到工作台」变为「已添加」，右下角 XqToast 提示「已添加到工作台」。
- **移除成功**：按钮由「已添加」恢复为「添加到工作台」，XqToast 提示「已从工作台移除」。
- **无权限点击**：无权限卡片点击无效，悬停时鼠标为禁用样式，不触发任何路由跳转。
- **网络失败**：添加/移除接口失败后，按钮状态回滚并提示「操作失败，请稍后重试」，提供「重试」按钮。
- **达到上限**：点击添加时若 appIds.length >= maxAppCount，提示「工作台最多添加 {maxAppCount} 个应用，请先移除不常用应用」。

### 7.3 空态设计

| 场景 | 空态类型 | 文案 | 操作 |
|---|---|---|---|
| 搜索无结果 | XqEmptyState type='search' | 未找到匹配应用 | 更换关键词或重置筛选 |
| 当前角色无任何应用权限 | XqEmptyState type='permission' | 暂无可用应用，请联系管理员 | 联系管理员（仅展示） |
| 应用列表加载失败 | XqEmptyState type='error' | 加载失败，请刷新重试 | 刷新按钮 |

### 7.4 快捷操作

- 应用卡片顶部右侧显示操作按钮，状态随用户偏好实时变化。
- 已添加应用卡片左上角显示小星标（SVG 图标），提示用户该应用已加入工作台。
- 点击应用卡片主体（非按钮区域）进入对应页面；无权限卡片整体禁用点击。

### 7.5 异常处理

- **权限配置变更**：用户正在浏览应用中心时，若管理员修改了角色权限，下次进入页面时生效；当前页面不强制刷新。
- **应用停用**：已添加的应用被停用后，工作台保留入口但点击进入时提示「该应用已停用」；应用中心卡片状态变为「已停用」。
- **并发操作**：两个浏览器标签同时修改偏好时，以最后一次保存为准，后端不做复杂合并，前端以接口返回结果刷新。
- **离线状态**：移动端离线时，添加/移除操作本地缓存，网络恢复后批量同步。

---

## 八、响应式双端设计

### 8.1 PC 端布局

- **断点**：
  - PC 宽屏（>=1440px）：应用卡片网格 4 列，搜索框宽度 280px。
  - PC 标准（1024px-1439px）：应用卡片网格 3 列，搜索框宽度 240px。
  - 平板（768px-1023px）：应用卡片网格 2 列，筛选条件折叠到「更多」。
- **导航**：左侧固定侧边栏，应用中心为一级入口。
- **分组标题**：业务域分组标题左对齐，字体 13px，text-sub 颜色，下方卡片网格对齐。
- **卡片尺寸**：固定宽高比，高度约 120px，图标 28px，操作按钮右上角。
- **手势**：鼠标悬停卡片显示阴影上浮效果，点击按钮触发操作。

### 8.2 移动端布局

- **断点**：<768px。
- **布局结构**：单列卡片列表，顶部固定搜索栏吸顶。
- **导航**：底部 Tab 或工作台页面内的「添加应用」入口进入；顶部显示返回按钮。
- **筛选**：业务域与状态筛选收起为底部抽屉，点击筛选图标弹出。
- **卡片**：单列显示，图标 32px，名称与描述纵向排列，操作按钮右侧。
- **手势**：
  - 下拉刷新：下拉应用列表重新加载应用元数据与偏好。
  - 上拉加载：业务域分组较多时滚动加载。
  - 长按卡片：弹出操作菜单（添加/移除/查看详情）。
- **底部按钮**：移动端无全局底部按钮，操作集中在每张卡片的右上角。
- **字段折叠**：描述文字最多显示一行，超长省略。

### 8.3 响应式适配检查项

| 检查项 | 通过标准 |
|---|---|
| 移动端不横向滚动 | 除必要列表外无意外横向溢出 |
| 按钮不换行 | 每张卡片操作按钮单独一行，不与其他按钮并列 |
| 字号放大 150% 可用 | 应用名称与描述不遮挡，卡片高度自适应 |
| 深色主题可用 | 卡片背景、文字、按钮颜色使用 CSS 变量 |
| 移动端底部 Tab 不遮挡 | 卡片列表底部预留 Tab 高度 |

---

## 九、接口契约（前端视角）

### 9.1 获取应用中心列表

- **接口名**：getAppCenterList
- **请求方式**：GET
- **路径**：/api/v1/app-center/list
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| roleCode | String | 是 | 当前角色编码 |
| keyword | String | 否 | 搜索关键词，支持应用名称与拼音首字母 |
| category | String | 否 | 业务域编码：business / channel / compliance / data / system，为空则全部 |
| addedStatus | Integer | 否 | 0:未添加，1:已添加，为空则全部 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<AppCategoryGroup> | 按业务域分组的应用列表 |
| myAppIds | Array<String> | 当前用户已添加到工作台的应用 ID 列表 |
| maxAppCount | Integer | 工作台最大可添加应用数 |

**AppCategoryGroup 结构**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| categoryCode | String | 业务域编码 |
| categoryName | String | 业务域名称 |
| sortOrder | Integer | 分组排序 |
| apps | Array<AppItem> | 该分组下的应用列表 |

**AppItem 结构**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| appId | String | 应用 ID |
| appCode | String | 应用编码 |
| appName | String | 应用名称 |
| appDesc | String | 应用描述 |
| appIcon | String | 图标组件名或 SVG 标识 |
| pageRoute | String | 页面路由 |
| category | String | 业务域编码 |
| sortOrder | Integer | 组内排序 |
| permissionCode | String | 权限码 |
| isDefault | Boolean | 是否为角色默认应用 |
| hasPermission | Boolean | 当前角色是否有权限 |
| isAdded | Boolean | 是否已添加到工作台 |
| status | Integer | 应用状态：0草稿/1已发布/2已停用 |

### 9.2 添加到工作台

- **接口名**：addAppToWorkbench
- **请求方式**：POST
- **路径**：/api/v1/app-center/my-apps/add
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| appId | String | 是 | 要添加的应用 ID |
| roleCode | String | 是 | 当前角色编码 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |
| myAppIds | Array<String> | 更新后的已添加应用 ID 列表 |
| message | String | 提示信息 |

### 9.3 从工作台移除

- **接口名**：removeAppFromWorkbench
- **请求方式**：POST
- **路径**：/api/v1/app-center/my-apps/remove
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| appId | String | 是 | 要移除的应用 ID |
| roleCode | String | 是 | 当前角色编码 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |
| myAppIds | Array<String> | 更新后的已添加应用 ID 列表 |
| message | String | 提示信息 |

### 9.4 获取我的应用列表（供工作台调用）

- **接口名**：getMyApps
- **请求方式**：GET
- **路径**：/api/v1/app-center/my-apps
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| roleCode | String | 是 | 当前角色编码 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<AppItem> | 已添加到工作台且当前角色有权限的应用详情列表 |
| total | Integer | 数量 |
| maxAppCount | Integer | 最大可添加数 |

### 9.5 批量添加默认应用到工作台

- **接口名**：batchAddDefaultApps
- **请求方式**：POST
- **路径**：/api/v1/app-center/my-apps/batch-add
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| appIds | Array<String> | 是 | 要批量添加的应用 ID 列表 |
| roleCode | String | 是 | 当前角色编码 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |
| myAppIds | Array<String> | 更新后的已添加应用 ID 列表 |
| addedCount | Integer | 实际添加数量 |

---

## 十、验收断言清单

### 功能：应用中心首页加载

#### 断言 1：按业务域分组展示应用

- **验证对象**：应用中心首页应用列表
- **前置条件**：用户已登录，角色为销售总监，已进入 /app-center
- **操作步骤**：
  1. 等待页面加载完成。
  2. 检查页面中的应用分组标题。
- **预期结果**：
  1. 页面按「业务管理、渠道协同、合规审计、数据决策、系统管理」顺序显示分组。
  2. 每个分组下显示该角色有权限的应用卡片。
  3. 无权限应用卡片显示为置灰状态，按钮显示「无权限」。
- **验收方式**：自动化测试 / 截图对比

#### 断言 2：已添加应用显示「已添加」状态

- **验证对象**：应用卡片状态按钮
- **前置条件**：用户已将「意向管理」添加到工作台
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「意向管理」应用卡片。
- **预期结果**：
  1. 该卡片右上角按钮文案为「已添加」。
  2. 按钮颜色为绿色（#22c55e）。
  3. 已添加应用在工作台「我的应用」区同步显示。
- **验收方式**：自动化测试

#### 断言 3：无权限应用不可点击

- **验证对象**：无权限应用卡片
- **前置条件**：使用销售代表账号登录，该角色无「流程设计器」权限
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「流程设计器」卡片（位于系统管理分组）。
  3. 点击卡片主体与按钮。
- **预期结果**：
  1. 卡片透明度为 60%。
  2. 按钮文案为「无权限」且 disabled。
  3. 点击不触发任何路由跳转或接口调用。
- **验收方式**：自动化测试

### 功能：添加到工作台

#### 断言 4：正常添加应用到工作台

- **验证对象**：应用添加功能
- **前置条件**：用户已登录，应用「审批中心」未添加到工作台，当前应用数未达上限
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「审批中心」卡片。
  3. 点击「添加到工作台」按钮。
- **预期结果**：
  1. 按钮文案变为「已添加」。
  2. 右下角提示「已添加到工作台」。
  3. 调用 addAppToWorkbench 接口且返回 success=true。
  4. 工作台「我的应用」区出现「审批中心」入口。
- **验收方式**：自动化测试

#### 断言 5：工作台应用数达到上限时阻止添加

- **验证对象**：添加应用上限校验
- **前置条件**：用户已添加 12 个应用到工作台（假设 maxAppCount=12）
- **操作步骤**：
  1. 进入应用中心。
  2. 点击任意未添加应用的「添加到工作台」按钮。
- **预期结果**：
  1. 前端不调用添加接口。
  2. 提示「工作台最多添加 12 个应用，请先移除不常用应用」。
  3. 按钮状态保持不变。
- **验收方式**：自动化测试

#### 断言 6：无权限应用无法添加

- **验证对象**：添加权限校验
- **前置条件**：使用销售代表账号登录，无「后台设置」权限
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「后台设置」卡片。
  3. 尝试点击「添加到工作台」。
- **预期结果**：
  1. 按钮为 disabled 状态，不可点击。
  2. 不调用 addAppToWorkbench 接口。
- **验收方式**：自动化测试

### 功能：从工作台移除

#### 断言 7：正常移除应用

- **验证对象**：应用移除功能
- **前置条件**：用户已将「工单看板」添加到工作台
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「工单看板」卡片。
  3. 点击「已添加」按钮。
- **预期结果**：
  1. 按钮文案恢复为「添加到工作台」。
  2. 右下角提示「已从工作台移除」。
  3. 调用 removeAppFromWorkbench 接口且返回 success=true。
  4. 工作台「我的应用」区不再显示「工单看板」。
- **验收方式**：自动化测试

#### 断言 8：移除默认应用二次确认

- **验证对象**：默认应用移除确认
- **前置条件**：「客户 360°」为当前角色默认应用且已添加
- **操作步骤**：
  1. 进入应用中心。
  2. 找到「客户 360°」卡片。
  3. 点击「已添加」按钮。
- **预期结果**：
  1. 弹出 XqConfirmDialog 确认弹窗。
  2. 提示文案包含「该应用为默认应用，移除后仍可在应用中心找到」。
  3. 点击「确认」后移除；点击「取消」保持原状态。
- **验收方式**：自动化测试

#### 断言 9：移除后重新添加恢复正常

- **验证对象**：应用移除后再次添加
- **前置条件**：用户已将「目标绩效」添加到工作台
- **操作步骤**：
  1. 进入应用中心。
  2. 点击「目标绩效」卡片的「已添加」按钮，确认移除。
  3. 按钮恢复为「添加到工作台」后再次点击添加。
- **预期结果**：
  1. 移除后工作台不再显示「目标绩效」。
  2. 再次添加后按钮恢复为「已添加」。
  3. 两次操作均调用对应接口且返回 success=true。
  4. 最终工作台显示「目标绩效」入口。
- **验收方式**：自动化测试

### 功能：应用搜索与筛选

#### 断言 10：按名称搜索应用

- **验证对象**：应用搜索框
- **前置条件**：应用中心存在「线索管理」「意向管理」「客户 360°」等应用
- **操作步骤**：
  1. 在搜索框输入「意向」。
  2. 等待 300ms debounce。
- **预期结果**：
  1. 页面只显示名称包含「意向」的应用卡片。
  2. 不包含「意向」的分组被隐藏。
  3. 总结果数量正确显示。
- **验收方式**：自动化测试

#### 断言 11：按拼音首字母搜索应用

- **验证对象**：搜索框拼音搜索
- **前置条件**：应用中心存在「客户 360°」应用
- **操作步骤**：
  1. 在搜索框输入「kh」。
  2. 按回车或等待 debounce。
- **预期结果**：
  1. 列表显示「客户 360°」应用卡片。
  2. 其他不匹配应用被隐藏。
- **验收方式**：自动化测试

#### 断言 12：按业务域筛选

- **验证对象**：业务域筛选器
- **前置条件**：应用中心包含多个业务域应用
- **操作步骤**：
  1. 点击业务域筛选下拉。
  2. 选择「数据决策」。
- **预期结果**：
  1. 页面只显示「数据决策」分组下的应用。
  2. 其他分组被隐藏。
  3. 搜索关键词与筛选条件联动，同时生效。
- **验收方式**：自动化测试

### 功能：响应式与移动端

#### 断言 13：移动端单列布局

- **验证对象**：移动端应用中心页面
- **前置条件**：使用 iPhone 14 尺寸模拟器，用户已登录
- **操作步骤**：
  1. 进入 /app-center。
  2. 检查应用卡片布局。
- **预期结果**：
  1. 应用卡片单列显示。
  2. 搜索框宽度 100% 且吸顶。
  3. 筛选入口在搜索框右侧，点击弹出底部筛选面板。
- **验收方式**：截图对比 / 自动化测试

#### 断言 14：移动端下拉刷新

- **验证对象**：移动端应用列表刷新
- **前置条件**：使用 iPhone 14 尺寸模拟器，已在应用中心页面
- **操作步骤**：
  1. 向下拉动列表。
  2. 释放触发下拉刷新。
- **预期结果**：
  1. 显示加载动画。
  2. 重新调用 getAppCenterList 接口。
  3. 刷新后列表与最新数据一致。
- **验收方式**：自动化测试

#### 断言 15：移动端长按显示操作菜单

- **验证对象**：移动端应用卡片手势
- **前置条件**：使用 iPhone 14 尺寸模拟器，应用中心已加载
- **操作步骤**：
  1. 长按任意有权限应用卡片。
- **预期结果**：
  1. 底部弹出操作菜单。
  2. 菜单项包含「进入应用」「添加到工作台/移除」等当前状态允许的操作。
- **验收方式**：自动化测试 / 人工验证

---

## 十一、权限与数据范围

### 11.1 角色权限矩阵

| 功能 | 总经理 | 销售总监 | 区域总监 | 销售代表 | 售后总监 | 经销商管理员 | 合规专员 | 系统管理员 |
|---|---|---|---|---|---|---|---|---|
| 查看应用中心 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 添加应用到工作台 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 移除应用到工作台 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 查看无权限应用 | 可见置灰 | 可见置灰 | 可见置灰 | 可见置灰 | 可见置灰 | 可见置灰 | 可见置灰 | 可见置灰 |
| 进入有权限应用 | 是 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 管理应用元数据 | 否 | 否 | 否 | 否 | 否 | 否 | 否 | 是 |
| 配置角色默认应用 | 否 | 否 | 否 | 否 | 否 | 否 | 否 | 是 |

### 11.2 数据可见性

| 数据类型 | 总经理 | 销售总监 | 区域总监 | 销售代表 | 售后总监 | 经销商管理员 | 合规专员 | 系统管理员 |
|---|---|---|---|---|---|---|---|---|
| 全部应用元数据 | 可见（已发布） | 可见（已发布） | 可见（已发布） | 可见（已发布） | 可见（已发布） | 可见（已发布） | 可见（已发布） | 全部可见 |
| 本人应用偏好 | 本人 | 本人 | 本人 | 本人 | 本人 | 本人 | 本人 | 本人 |
| 他人应用偏好 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 可见 |
| 应用权限配置 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 不可见 | 可见 |

### 11.3 字段级权限

| 字段 | 普通用户 | 系统管理员 | 说明 |
|---|---|---|---|
| appId / appName / appDesc / appIcon / pageRoute / category | 可见 | 可见 | 应用基础信息 |
| permissionCode / tenantId / createBy / status=草稿 | 不可见 | 可见 | 权限与配置字段 |
| isDefault | 可见 | 可编辑 | 标识默认应用 |
| sortOrder | 不可见 | 可编辑 | 排序号 |

---

## 十二、关联与扩展

### 12.1 与其它模块的关联

| 关联模块 | 关联方式 | 说明 |
|---|---|---|
| **工作台** | 数据联动 | 应用中心添加/移除的应用直接反映在工作台「我的应用」区。 |
| **后台设置 - 应用配置** | 元数据供给 | 应用的名称、图标、描述、分组、排序、权限码由应用配置模块维护。 |
| **后台设置 - 角色权限** | 权限判定 | 应用可见性由角色权限码与数据范围共同决定。 |
| **组织用户管理** | 用户偏好存储 | 用户应用偏好按 userId + roleCode 存储。 |
| **消息中心** | 事件通知 | 应用停用时向已添加用户推送系统通知。 |
| **收藏夹** | 入口关联 | 用户可在工作台将高频应用收藏，收藏夹与我的应用可互通。 |
| **侧边栏导航** | 入口承载 | 应用中心作为一级入口固定在侧边栏，业务应用不在侧边栏平铺。 |

### 12.2 未考虑到的问题补充

1. **应用图标扩展机制**
   新增应用时，SVG 图标组件需预先注册在图标库中。需要明确应用配置中 appIcon 是引用组件名还是 SVG 字符串，以及如何在运行时动态加载未知图标。

2. **角色切换后的应用偏好隔离**
   同一用户可能拥有多个角色（如销售代表 + 区域管理员），切换角色后工作台应用偏好是否按角色隔离存储，还是合并显示。

3. **租户级默认应用与个人偏好的优先级**
   管理员配置的角色默认应用与用户手动移除的偏好冲突时，应以哪个为准；是否需要「恢复默认」功能。

4. **应用停用的前端缓存**
   应用被停用后，已添加到工作台的用户仍可能看到缓存入口。需要明确缓存刷新策略与停用提示文案。

5. **移动端小程序分包加载**
   业务应用较多时，小程序端应用中心进入后按需加载分包页面，避免首包体积过大。

6. **应用搜索的拼音库维护**
   应用名称变更后拼音首字母搜索索引需要同步更新，需明确索引构建时机与异常处理。

7. **工作台最大应用数的可配置性**
   maxAppCount 作为系统参数，是否支持按角色或租户单独覆盖，需在产品层面确认。

8. **应用中心与应用市场的边界**
   当前应用中心仅展示本租户已启用应用；未来若扩展为应用市场（可安装第三方应用），需要新增安装/卸载流程。

9. **无网络环境下的移动端操作**
   移动端离线添加/移除应用后，网络恢复时如何与后端同步，是否需要冲突检测。

10. **应用详情抽屉的必要性**
    当前原型未展示应用详情抽屉，若后续增加该功能，需要补充应用说明文档、依赖应用、权限说明等字段与页面。

---

## 附录：术语表

| 术语 | 说明 |
|---|---|
| 应用 | 鑫渠 CRM 中可独立启用、配置、访问的一个业务单元，如线索管理、意向管理。 |
| 业务域 | 应用的分组维度，包括业务管理、渠道协同、合规审计、数据决策、系统管理。 |
| 工作台 | 用户一站式工作门户，展示待办、日程、快捷应用入口等。 |
| 默认应用 | 角色预设必须展示在工作台的应用，用户可手动移除但通常建议保留。 |
| 应用偏好 | 用户针对特定角色保存的「我的应用」ID 列表，用于个性化工作台。 |

---

> 文档结束。后续迭代请在「版本记录」中补充变更，并同步更新「文档基础信息」中的版本号。
