# 鑫渠统一状态机规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **文档定位**：统一鑫渠 CRM 所有业务对象的生命周期状态语义、命名规则与流转约束，消除跨模块状态命名不一致问题。

---

## 一、设计原则

1. **语义统一**：同一语义的状态在不同业务模块中必须使用相同名称。
2. **最小可用**：每个业务对象只保留必要状态，禁止为细微差异发明新状态。
3. **状态与操作分离**：状态描述「当前在哪里」，操作描述「能做什么」，禁止把操作名当作状态名。
4. **显式终态**：每个状态机必须有明确的终态（完成/关闭/取消/驳回）。
5. **版本兼容**：状态枚举值一旦发布，不可删除，仅可新增或标记废弃。

---

## 二、通用状态基线

以下状态为全系统通用基线，各业务模块在此基础上扩展自己的子状态或别名映射：

| 状态码 | 中文名 | 语义 | 是否为终态 |
|---|---|---|---|
| `DRAFT` | 草稿 | 已创建但未提交 | 否 |
| `PENDING` | 待处理 | 已提交，等待进入处理流程 | 否 |
| `PROCESSING` | 处理中 | 正在处理，可能包含多个子阶段 | 否 |
| `PENDING_CONFIRM` | 待确认 | 处理完成，等待对方确认 | 否 |
| `COMPLETED` | 已完成 | 正常结束 | 是 |
| `CLOSED` | 已关闭 | 流程关闭（含正常/取消/重复等） | 是 |
| `REJECTED` | 已驳回 | 审批或处理未通过 | 是 |
| `CANCELLED` | 已取消 | 主动取消 | 是 |
| `SUSPENDED` | 已挂起 | 暂停处理，恢复后继续 | 否 |
| `EXPIRED` | 已过期 | 超时失效 | 是 |
| `INVALID` | 已失效 | 因冲突或规则被置为无效 | 是 |

---

## 三、各业务对象状态机

### 3.1 通用审批流状态机

适用对象：意向报备、学术活动、样品申请、返利调整、目标调整等需要审批的对象。

```
DRAFT --提交--> PENDING_APPROVAL
PENDING_APPROVAL --通过--> APPROVED
PENDING_APPROVAL --驳回--> REJECTED
REJECTED --重新提交--> PENDING_APPROVAL
APPROVED --执行--> COMPLETED
APPROVED --取消--> CANCELLED
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `DRAFT` | 草稿 | 填写中，未提交 |
| `PENDING_APPROVAL` | 审批中 | 已提交，等待审批 |
| `APPROVED` | 已通过 | 审批全部通过 |
| `REJECTED` | 已驳回 | 任一节点驳回 |
| `COMPLETED` | 已完成 | 审批通过后业务执行完成 |
| `CANCELLED` | 已取消 | 发起人撤销 |

---

### 3.2 工单状态机

适用对象：售后工单、实施工单、维保工单、巡检工单。

```
PENDING_DISPATCH --派单--> DISPATCHED
DISPATCHED --开始处理--> PROCESSING
PROCESSING --提交结果--> PENDING_CONFIRM
PENDING_CONFIRM --确认通过--> COMPLETED
PENDING_CONFIRM --确认不通过--> PROCESSING
PROCESSING --挂起--> SUSPENDED
SUSPENDED --恢复--> PROCESSING
任意非终态 --取消--> CANCELLED
任意非终态 --重复/误报--> CLOSED
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `PENDING_DISPATCH` | 待派单 | 已创建，未分配工程师 |
| `DISPATCHED` | 已派单 | 已分配工程师，等待响应 |
| `PROCESSING` | 处理中 | 工程师正在处理 |
| `PENDING_CONFIRM` | 待确认 | 等待客户/主管确认 |
| `COMPLETED` | 已完成 | 客户确认通过 |
| `SUSPENDED` | 已挂起 | 暂停计时 |
| `CANCELLED` | 已取消 | 客户取消 |
| `CLOSED` | 已关闭 | 重复报修、误报等原因关闭 |

> **子状态标记**：处理中可叠加子状态标签（到场/远程支持/备件等待），但主状态仍保持 `PROCESSING`。

---

### 3.3 经销商授权状态机

```
DRAFT --提交--> PENDING_APPROVAL
PENDING_APPROVAL --通过--> ACTIVE
PENDING_APPROVAL --驳回--> REJECTED
ACTIVE --过期且未续约--> EXPIRED
ACTIVE --触发清退--> REVOKED
REJECTED --修改后重新提交--> PENDING_APPROVAL
EXPIRED --续约成功--> ACTIVE
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `DRAFT` | 草稿 | 准入申请填写中 |
| `PENDING_APPROVAL` | 审批中 | 准入审批流程中 |
| `ACTIVE` | 授权中 | 已启用，可开展业务 |
| `REJECTED` | 被驳回 | 审批未通过 |
| `EXPIRED` | 已过期 | 授权到期未续约 |
| `REVOKED` | 已失效 | 主动清退或禁用 |
| `SUSPENDED` | 暂停合作 | 资质临期或处罚中 |

---

### 3.4 区域保护意向状态机

```
REPORTING --提交--> PROTECTED
REPORTING --冲突检测--> CONFLICT_PENDING
CONFLICT_PENDING --裁决支持--> PROTECTED
CONFLICT_PENDING --裁决驳回--> REJECTED
PROTECTED --到期/结束--> EXPIRED
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `REPORTING` | 报备中 | 已提交，待校验 |
| `PROTECTED` | 保护期中 | 通过区域校验，进入保护期 |
| `CONFLICT_PENDING` | 冲突待裁决 | 与其他生效意向冲突 |
| `REJECTED` | 已驳回 | 裁决不支持当前申请 |
| `EXPIRED` | 已失效 | 保护期结束 |

---

### 3.5 返利结算单状态机

```
PENDING_CALC --采集完成--> CALCULATED
CALCULATED --经销商确认--> PENDING_SETTLEMENT
CALCULATED --发起异议--> DISPUTE
DISPUTE --审批通过--> PENDING_SETTLEMENT
DISPUTE --审批驳回--> CALCULATED
PENDING_SETTLEMENT --付款完成--> SETTLED
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `PENDING_CALC` | 待计算 | 周期数据采集中 |
| `CALCULATED` | 已计算 | 返利金额已计算 |
| `DISPUTE` | 异议中 | 经销商对金额有异议 |
| `PENDING_SETTLEMENT` | 待结算 | 等待财务付款 |
| `SETTLED` | 已结算 | 付款完成 |

---

### 3.6 样品/临评状态机

```
PENDING_OUT --出库--> OUT
OUT --签收--> RECEIVED
OUT --超期未签收--> OVERDUE
RECEIVED --回库--> RETURNED
RECEIVED --核销--> WRITTEN_OFF
RECEIVED --超期未回--> OVERDUE
OVERDUE --补充说明/核销--> WRITTEN_OFF
OVERDUE --实际回库--> RETURNED
```

| 状态码 | 中文名 | 说明 |
|---|---|---|
| `PENDING_OUT` | 待出库 | 申请已批准，等待出库 |
| `OUT` | 已出库 | 已发货，在途 |
| `RECEIVED` | 已签收 | 客户已签收 |
| `RETURNED` | 已回库 | 样品退回仓库 |
| `WRITTEN_OFF` | 已核销 | 临评完成或确认耗用 |
| `OVERDUE` | 超期未回 | 超过预计回库日 |

---

### 3.7 拜访记录状态机

```
DRAFT --保存--> DRAFT
DRAFT --提交且校验通过--> PENDING_APPROVAL
DRAFT --提交需整改--> PENDING_RECTIFICATION
PENDING_RECTIFICATION --整改后重新提交--> PENDING_APPROVAL
PENDING_APPROVAL --通过--> APPROVED
PENDING_APPROVAL --驳回--> REJECTED
REJECTED --整改后重新提交--> PENDING_APPROVAL
```

---

## 四、状态命名规范

### 4.1 命名规则

1. 状态码使用英文大写下划线命名：`PENDING_APPROVAL`、`PROCESSING`。
2. 中文名使用「已/待/中」等状态词，不使用动作词：
   - ✅ 待审批、处理中、已完成
   - ❌ 提交、审批、关闭（这些是动作）
3. 禁止同一语义多个命名：
   - 「待处理」「待审批」「处理中」不可混用。
   - 审批类对象统一使用 `PENDING_APPROVAL`，通用处理使用 `PENDING`。

### 4.2 状态扩展规则

业务模块可在通用状态基线基础上扩展子状态，但需满足：
1. 主状态必须映射到通用基线。
2. 子状态以标签或附加字段形式存在，不能替代主状态。
3. 新增状态需在本文档中登记，并说明与其他状态的区别。

---

## 五、状态流转校验规则

1. **前置条件校验**：状态变更前必须校验当前状态是否允许该操作。
2. **权限校验**：只有授权角色才能触发特定状态变更。
3. **并发控制**：同一对象同一时刻只允许一个生效操作，使用乐观锁（version 字段）。
4. **事件触发**：状态变更成功后必须发布对应的领域事件。
5. **日志留痕**：所有状态变更必须记录操作日志，包含：变更前状态、变更后状态、操作人、操作时间、原因。

---

## 六、状态与界面映射

| 状态分组 | 颜色 | 适用状态 |
|---|---|---|
| 进行中 | 蓝色 | DRAFT、PENDING、PROCESSING、PENDING_CONFIRM |
| 成功/完成 | 绿色 | COMPLETED、APPROVED、SETTLED、RETURNED |
| 警告/待处理 | 橙色 | PENDING_APPROVAL、CONFLICT_PENDING、DISPUTE、PENDING_SETTLEMENT |
| 危险/异常 | 红色 | REJECTED、OVERDUE、EXPIRED、REVOKED |
| 中性终态 | 灰色 | CLOSED、CANCELLED、INVALID、WRITTEN_OFF |
| 暂停 | 紫色 | SUSPENDED |

---

## 七、文档同步要求

1. 所有业务模块文档中的状态枚举必须引用本规范。
2. 新增业务对象状态机前，须先在本规范中确认或新增基线状态。
3. 状态字段在数据库中必须存储状态码（英文），中文名仅用于前端展示。
