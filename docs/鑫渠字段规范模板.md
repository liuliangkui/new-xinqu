# 鑫渠字段规范模板

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **适用范围**：鑫渠 CRM 全部业务对象字段定义  
> **文档性质**：AI 开发时的字段命名、类型、约束、权限字典

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：定义通用字段、字段属性标准、常见业务对象字段示例 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、字段属性标准

每个字段定义必须包含以下 10 个属性：

| 属性 | 是否必填 | 说明 |
|---|---|---|
| 字段编码 | 是 | 英文或拼音驼峰命名，全局唯一 |
| 中文名称 | 是 | 页面显示名称 |
| 数据类型 | 是 | String / Number / Integer / Boolean / Date / DateTime / Enum / Array / Object / Money |
| 长度/精度 | 是 | 字符串长度、数字精度、日期格式 |
| 是否必填 | 是 | 是 / 否 / 条件必填 |
| 默认值 | 是 | 无默认值写「无」 |
| 数据来源 | 是 | 手工录入 / 系统生成 / 字典 / 计算 / 关联 |
| 枚举值 | 条件必填 | Enum 类型必须给出 |
| 读写权限 | 是 | 角色可见/可编辑规则 |
| 渲染方式 | 是 | 列表/详情/表单中如何展示 |

---

## 二、命名规则

1. **英文优先**：能用简短英文表达的字段直接用英文，如 `customerName`。
2. **拼音备用**：业务强相关中文概念可用拼音缩写，如 `yiyuanLevel`（医院等级）。
3. **统一后缀**：
   - 状态字段以 `Status` 结尾
   - 类型字段以 `Type` 结尾
   - 时间字段以 `Time` 结尾
   - 日期字段以 `Date` 结尾
   - 金额字段以 `Amount` 结尾
   - 数量字段以 `Count` / `Qty` 结尾
   - 是否字段以 `Flag` / `Is` 开头
4. **禁止拼音全拼过长的字段名**，如 `yixiangbianhaoleixing` 应改为 `intentionType`。

---

## 三、通用字段（跨业务对象复用）

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| id | 主键 ID | Integer/Long | - | 是 | 无 | 系统生成 | 只读 | 不显示 |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | 只读 | 列表/详情显示 |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | 只读 | 详情显示 |
| createBy | 创建人 ID | Integer | - | 是 | 当前用户 | 系统生成 | 只读 | 详情显示 |
| createByName | 创建人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | 只读 | 列表/详情显示 |
| updateBy | 更新人 ID | Integer | - | 是 | 当前用户 | 系统生成 | 只读 | 详情显示 |
| tenantId | 租户 ID | Integer | - | 是 | 当前租户 | 系统生成 | 不可见 | 不显示 |
| orgId | 组织 ID | Integer | - | 是 | 当前组织 | 系统生成 | 不可见 | 不显示 |
| deleted | 删除标记 | Boolean | - | 是 | false | 系统生成 | 不可见 | 不显示 |
| remark | 备注 | String | 500 | 否 | 无 | 手工录入 | 全员可见 | 表单多行文本 |

---

## 四、业务对象字段示例

### 4.1 客户（HCO）对象

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| customerId | 客户 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| customerName | 客户名称 | String | 100 | 是 | 无 | 手工录入 | - | 全员可见，负责人可编辑 | 列表/详情/表单 |
| customerLevel | 客户等级 | Enum | - | 是 | 无 | 字典 | 1:三甲, 2:三乙, 3:二甲, 4:二乙, 5:一级 | 全员可见 | 标签 |
| regionCode | 区域编码 | String | 20 | 是 | 无 | 字典 | - | 全员可见 | 列表/详情 |
| regionName | 区域名称 | String | 50 | 是 | 无 | 字典 | - | 全员可见 | 列表/详情 |
| healthScore | 健康度评分 | Integer | 0-100 | 否 | 0 | 计算 | - | 全员可见 | 圆环/数字 |
| ownerId | 负责人 ID | Integer | - | 是 | 当前用户 | 关联用户 | - | 全员可见，经理可编辑 | 列表/详情 |
| ownerName | 负责人姓名 | String | 50 | 是 | 当前用户 | 关联用户 | - | 全员可见 | 列表/详情 |

### 4.2 意向对象

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| intentionId | 意向 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| intentionCode | 意向编号 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见 | 列表/详情 |
| intentionName | 项目名称 | String | 100 | 是 | 无 | 手工录入 | - | 全员可见 | 列表/详情/表单 |
| customerId | 客户 ID | Long | - | 是 | 无 | 关联客户 | - | 全员可见 | 表单下拉 |
| businessType | 业务类型 | Enum | - | 是 | 无 | 字典 | 1:招标, 2:非招标, 3:TP, 4:捐赠 | 全员可见 | 标签 |
| amount | 预计金额 | Money | 18,2 | 否 | 0 | 手工录入 | - | 全员可见 | 列表/详情 |
| status | 意向状态 | Enum | - | 是 | 0 | 系统生成 | 0:草稿, 1:审批中, 2:生效中, 4:被驳回, 5:已结束 | 全员可见 | 状态标签 |
| protectDeadline | 保护期截止 | Date | yyyy-MM-dd | 否 | 无 | 计算 | - | 全员可见 | 倒计时 |
| reporterId | 报单人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 只读 | 列表/详情 |

### 4.3 线索对象

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| leadId | 线索 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| leadCode | 线索编号 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见 | 列表/详情 |
| leadSummary | 线索摘要 | String | 200 | 是 | 无 | 手工录入 | - | 全员可见 | 列表/详情/表单 |
| customerName | 客户名称 | String | 100 | 是 | 无 | 手工录入 | - | 全员可见 | 列表/详情/表单 |
| department | 科室 | String | 50 | 否 | 无 | 手工录入 | - | 全员可见 | 列表/详情 |
| sourceType | 线索来源 | Enum | - | 是 | 无 | 字典 | 1:学术会, 2:招标, 3:经销商推荐, 4:市场活动, 5:拜访, 6:其他 | 全员可见 | 标签 |
| status | 线索状态 | Enum | - | 是 | 0 | 系统生成 | 0:待分配, 1:跟进中, 2:已转意向, 3:已废弃 | 全员可见 | 状态标签 |
| assigneeId | 归属人 ID | Integer | - | 否 | 无 | 分配 | - | 全员可见 | 列表/详情 |
| nextFollowTime | 下次跟进时间 | DateTime | yyyy-MM-dd HH:mm | 否 | 无 | 手工录入 | - | 归属人可见 | 列表/详情 |

---

## 五、枚举字段标准

每个枚举字段必须包含：
- 编码（数值或字符串）
- 显示文案
- 颜色（用于状态标签）
- 排序

示例：

```markdown
| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 草稿 | #94a3b8 | 未提交 |
| 1 | 审批中 | #f59e0b | 等待审批 |
| 2 | 生效中 | #22c55e | 已通过 |
| 4 | 被驳回 | #ef4444 | 审批不通过 |
| 5 | 已结束 | #64748b | 流程闭环 |
```

---

## 六、字段权限标准

字段权限使用以下标签描述：

| 标签 | 含义 |
|---|---|
| 全员可见 | 所有有页面权限的用户可见 |
| 角色可见 | 仅指定角色可见，需列出角色 |
| 本人可见 | 仅数据创建人可见 |
| 上级可见 | 本人及其上级可见 |
| 只读 | 可见但不可编辑 |
| 可编辑 | 可见且可编辑 |
| 脱敏显示 | 部分隐藏，如手机号 138****1234 |
| 不可见 | 前端不展示 |

---

## 七、渲染方式标准

| 渲染方式 | 适用字段 | 组件 |
|---|---|---|
| 文本 | 名称、编号、备注 | XqText |
| 标签 | 状态、类型、等级 | XqStatusBadge |
| 金额 | 金额、预算 | XqMoney |
| 日期 | 创建时间、截止日期 | XqDate |
| 倒计时 | 保护期、跟进倒计时 | XqCountdown |
| 评分 | 健康度、满意度 | XqScore |
| 头像+名称 | 负责人、创建人 | XqUser |
| 下拉选择 | 枚举、关联对象 | XqSelect |
| 级联选择 | 省市区、组织架构 | XqCascader |
| 多行文本 | 备注、描述 | XqTextarea |
| 文件上传 | 合同、资质 | XqUpload |
| 开关 | 是否字段 | XqSwitch |

---

## 八、相关文档索引

- 《AI可执行设计文档编写规范.md》
- 《鑫渠组件库清单与命名规范.md》
- 《鑫渠响应式设计规范.md》
- 《鑫渠系统技术实现白皮书.html》

---

*本模板是鑫渠 CRM 数据模型的基础字典，所有业务对象字段必须按此标准定义。*
