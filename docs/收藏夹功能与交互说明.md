# 收藏夹功能与交互说明

> **文档版本**：v1.0
> **更新日期**：2026-07-29
> **对应原型**：《鑫渠高保真原型.html》v1.8.0
> **对应规范**：《AI可执行设计文档编写规范.md》v1.0、《鑫渠组件库清单与命名规范.md》v1.0、《鑫渠响应式设计规范.md》v1.0、《鑫渠字段规范模板.md》v1.0、《鑫渠验收断言编写规范.md》v1.0、《鑫渠应用文档验收标准.md》v1.1
> **适用角色**：销售代表、销售经理、区域总监、渠道管理员、厂家经理、系统管理员

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：收藏夹模块定位、功能清单、业务流程、页面结构、字段规范、接口契约、验收断言 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 机器指令块

开发本模块前，AI 必须先读取并遵守以下全局约束：

- **技术栈**：Vue 3 + TypeScript + Pinia + Vue Router + Ant Design Vue 4.x + Tailwind CSS
- **组件库**：必须使用《鑫渠组件库清单与命名规范.md》中注册的组件名，禁止自行发明组件名
- **图标**：全部使用 SVG 图标或 Iconfont，禁止引用 PNG/JPG 图标
- **样式变量**：颜色、字号、间距必须使用《鑫渠全局设计系统规范.md》中的 CSS 变量
- **响应式断点**：
  - PC 宽屏：>=1440px
  - PC 标准：1024px-1439px
  - 平板：768px-1023px
  - 移动端：<768px
- **权限模型**：基于 RBAC + 数据范围，角色编码见《华鑫角色职责与用户故事.md》
- **数据规范**：字段命名必须与本文档「核心字段规范」完全一致
- **接口规范**：接口路径、字段命名必须与本文档「接口契约」完全一致
- **验收标准**：开发完成后必须对照本文档「验收断言清单」逐项验证

---

## 一、模块定位与目标

### 1.1 一句话定位

收藏夹是鑫渠 CRM 平台级**用户自定义常用功能与业务对象快捷入口**，让每位用户把高频访问的客户、意向、文档、项目、页面等聚合到一处，实现「一键触达、效率优先」的个人工作导航。

### 1.2 所属业务域

**平台入口域 / 个人工作区**。收藏夹与消息中心、日历、工作台、通讯录、任务、应用中心共同构成全局侧边栏一级入口。

### 1.3 解决的业务痛点

| 痛点 | 说明 |
|---|---|
| 入口分散 | 高频客户/项目/文档分散在各业务模块，反复切换消耗时间 |
| 查找成本高 | 数据量增大后，通过列表搜索定位目标对象的成本上升 |
| 个人习惯差异 | 不同角色、不同岗位的关注点不同，无法由系统统一固定 |
| 跨端体验不一致 | PC 与移动端缺少统一的个人快捷入口，移动端尤为明显 |
| 临时关注遗漏 | 近期重点跟进的对象容易被新数据淹没，缺少置顶/收藏机制 |

### 1.4 带来的量化价值

| 价值 | 量化目标 |
|---|---|
| 降低平均访问路径 | 高频对象访问步数从 3-4 步降至 1 步 |
| 提升日活跃对象回访率 | 收藏对象 7 日内回访率目标 >= 60% |
| 减少列表搜索次数 | 收藏用户日均搜索次数下降 20% 以上 |
| 增强移动端效率 | 移动端通过收藏夹进入目标的时长 <= 5 秒 |
| 支撑个性化工作区 | 为后续工作台「我的收藏」小组件提供数据基础 |

---

## 二、业务场景与角色

### 2.1 使用角色

| 角色 | 主要职责 | 高频操作 |
|---|---|---|
| **销售代表** | 维护个人负责的重点客户、意向、项目 | 收藏重点客户、查看收藏列表、取消已成交项目 |
| **销售经理** | 关注团队核心客户与待审批意向 | 收藏待审批意向、重点医院、团队文档 |
| **区域总监** | 监控大区关键客户与经营指标 | 收藏经营驾驶舱、目标绩效、区域重点客户 |
| **渠道管理员** | 管理经销商相关对象与协同任务 | 收藏经销商档案、渠道任务、合规文档 |
| **厂家经理** | 关注本厂家相关设备/试剂/项目 | 收藏设备机型、试剂项目、售后工单 |
| **系统管理员** | 管理平台配置与全局设置 | 收藏后台设置、应用配置、流程设计器 |

### 2.2 典型业务场景

#### 场景 1：销售代表收藏重点客户

| 要素 | 内容 |
|---|---|
| **场景名称** | 销售代表收藏重点客户 |
| **触发条件** | 销售代表在客户 360° 列表或详情页发现需要持续跟进的重点医院 |
| **操作角色** | 销售代表 |
| **预期结果** | 该客户被加入「我的收藏」，在收藏夹列表中按类型展示，点击可直达客户 360° 详情页 |

#### 场景 2：区域总监通过收藏夹进入经营驾驶舱

| 要素 | 内容 |
|---|---|
| **场景名称** | 区域总监通过收藏夹进入经营驾驶舱 |
| **触发条件** | 区域总监每日早会前需要快速查看区域业绩指标 |
| **操作角色** | 区域总监 |
| **预期结果** | 在收藏夹中点击已收藏的「经营驾驶舱」，直接跳转到对应页面并保留上次筛选条件 |

#### 场景 3：销售经理取消已结束项目的收藏

| 要素 | 内容 |
|---|---|
| **场景名称** | 销售经理取消已结束项目的收藏 |
| **触发条件** | 项目已成交或关闭，不再需要持续关注 |
| **操作角色** | 销售经理 |
| **预期结果** | 从收藏夹移除该项目，列表即时刷新，被移除项目不再占用快捷入口 |

#### 场景 4：跨端同步查看收藏

| 要素 | 内容 |
|---|---|
| **场景名称** | 跨端同步查看收藏 |
| **触发条件** | 用户在 PC 端添加的收藏，需要在手机端同样可见 |
| **操作角色** | 全部角色 |
| **预期结果** | 收藏数据按用户 ID 与租户 ID 持久化，PC 端与移动端收藏列表完全一致 |

---

## 三、核心功能清单

| 功能编号 | 功能名称 | 优先级 | 功能说明 |
|---|---|---|---|
| F01 | 收藏列表查看 | 必要 | 以分类 Tab + 卡片/列表形式展示当前用户全部收藏项 |
| F02 | 添加收藏 | 必要 | 在业务对象页面点击收藏图标，将对象加入收藏夹 |
| F03 | 取消收藏 | 必要 | 在收藏夹列表或业务对象页面移除收藏项 |
| F04 | 按类型筛选 | 必要 | 支持按客户、意向、项目、文档、页面等类型过滤收藏 |
| F05 | 关键词搜索 | 必要 | 支持按名称、拼音首字母搜索收藏项 |
| F06 | 排序调整 | 重要 | 支持拖拽或手动调整收藏项展示顺序 |
| F07 | 快捷跳转 | 必要 | 点击收藏项直接跳转到对应业务页面或详情页 |
| F08 | 空态引导 | 重要 | 无收藏时展示空态并引导用户去业务模块添加 |
| F09 | 最近访问联动 | 可选 | 工作台「最近访问」小组件可读取收藏相关数据 |
| F10 | 收藏数量统计 | 可选 | 顶部统计各类型的收藏数量 |

---

## 四、角色与权限

### 4.1 角色权限矩阵

| 功能 | 销售代表 | 销售经理 | 区域总监 | 渠道管理员 | 厂家经理 | 系统管理员 |
|---|---|---|---|---|---|---|
| 查看本人收藏列表 | 是 | 是 | 是 | 是 | 是 | 是 |
| 添加收藏 | 是 | 是 | 是 | 是 | 是 | 是 |
| 取消本人收藏 | 是 | 是 | 是 | 是 | 是 | 是 |
| 调整排序 | 是 | 是 | 是 | 是 | 是 | 是 |
| 搜索收藏 | 是 | 是 | 是 | 是 | 是 | 是 |
| 查看他人收藏 | 否 | 否 | 否 | 否 | 否 | 是（审计场景） |
| 管理他人收藏 | 否 | 否 | 否 | 否 | 否 | 否 |

### 4.2 数据可见性

| 范围 | 说明 |
|---|---|
| 个人隔离 | 每个用户只能看到和操作自己的收藏数据 |
| 租户隔离 | 收藏数据按 tenantId 隔离，不同租户互不可见 |
| 对象权限联动 | 收藏项跳转目标时，仍需校验当前用户对该对象的访问权限；对象权限被回收后，收藏项可见但点击提示「无权限查看」 |
| 对象删除联动 | 被收藏的业务对象被删除后，收藏项自动置灰并提示「对象已删除」，保留记录直至用户手动移除 |

---

## 五、核心业务流程

### 5.1 流程 1：添加收藏

#### 流程图

```
[用户在业务对象页] --> [点击收藏图标] --> [校验是否已收藏]
       |                                        |
       | 已收藏                                  | 未收藏
       v                                        v
  [提示"已收藏"]                            [校验对象权限]
       |                                        |
       | 无权限                                  | 有权限
       v                                        v
  [提示"无权限收藏"]                        [调用 addFavorite 接口]
                                                 |
                                                 v
                                          [收藏成功 Toast 提示]
                                                 |
                                                 v
                                          [收藏图标变为实心]
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 用户 | 进入客户 360° / 意向 / 项目 / 文档等业务页面 | 校验页面访问权限 | 展示对象详情或列表 |
| 2 | 用户 | 点击对象卡片/详情页标题旁的收藏图标 | 校验用户是否已登录 | 图标切换为实心/高亮 |
| 3 | 系统 | 调用添加收藏接口 | 校验目标对象是否存在、用户是否有权限查看 | 返回 favoriteId 或错误码 |
| 4 | 系统 | 更新本地 Pinia 状态与 UI | 校验接口返回成功 | 显示「收藏成功」轻提示 |
| 5 | 用户 | 进入收藏夹页面 | 校验用户身份 | 新收藏项出现在列表顶部 |

#### 异常分支表

| 异常场景 | 触发条件 | 处理方式 | 页面反馈 |
|---|---|---|---|
| 重复收藏 | 用户再次点击已收藏对象的收藏图标 | 前端拦截，不调用接口 | Toast 提示「已收藏」 |
| 对象无权限 | 用户尝试收藏无查看权限的对象 | 后端校验失败 | Toast 提示「暂无权限收藏该对象」 |
| 对象已删除 | 收藏时对象已被物理删除 | 后端返回对象不存在 | Toast 提示「对象不存在，无法收藏」 |
| 网络超时 | 接口请求超时 | 前端重试 1 次后失败 | Toast 提示「网络异常，请稍后重试」，图标回滚 |
| 收藏数量超限 | 超出单用户最大收藏数量（默认 200） | 后端校验失败 | Toast 提示「收藏数量已达上限，请取消后再添加」 |

### 5.2 流程 2：取消收藏

#### 流程图

```
[用户进入收藏夹] --> [选择目标收藏项] --> [点击取消收藏]
       |                                          |
       v                                          v
[确认弹窗] <------------------------- [二次确认开关判断]
       |
       v
[调用 removeFavorite 接口]
       |
       v
[列表移除该项] --> [Toast 提示"取消收藏成功"]
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 用户 | 进入收藏夹列表页 | 校验用户身份 | 展示收藏列表 |
| 2 | 用户 |  hover/长按收藏项，点击取消收藏按钮或图标 | 校验该收藏项归属当前用户 | 弹出确认弹窗（可在设置中关闭二次确认） |
| 3 | 用户 | 确认取消 | 校验 favoriteId 有效性 | 调用取消收藏接口 |
| 4 | 系统 | 更新数据库与本地状态 | 校验删除成功 | 列表移除该项，显示「取消收藏成功」 |
| 5 | 系统 | 同步更新源页面的收藏图标状态 | 校验对象关联关系 | 源页面收藏图标恢复为空心 |

#### 异常分支表

| 异常场景 | 触发条件 | 处理方式 | 页面反馈 |
|---|---|---|---|
| 取消失败 | 接口返回错误 | 前端保留原项，不执行动画 | Toast 提示「操作失败，请重试」 |
| 收藏项不存在 | favoriteId 已被删除 | 后端返回 404 | 列表刷新后该条消失 |
| 非本人收藏 | 用户篡改 favoriteId | 后端按 userId 校验 | 返回无权限，前端不做任何变化 |

### 5.3 流程 3：查看收藏并跳转

#### 流程图

```
[用户进入收藏夹] --> [选择类型 Tab 或输入关键词] --> [点击目标收藏项]
       |                                                    |
       v                                                    v
[列表筛选/搜索]                              [校验目标对象权限]
       |                                                    |
       | 有权限                                              | 无权限
       v                                                    v
[路由跳转至目标页面]                              [提示"无权限查看"]
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 用户 | 进入收藏夹页面 | 校验用户身份 | 展示全部收藏 |
| 2 | 用户 | 切换类型 Tab 或输入搜索词 | 校验输入字符长度 | 列表按条件过滤 |
| 3 | 用户 | 点击目标收藏项 | 校验目标对象是否仍存在、用户是否有查看权限 | 进入目标页面或提示无权限 |
| 4 | 系统 | 记录最近访问时间 | 校验写入成功 | 用于工作台「最近访问」排序 |

---

## 六、页面清单与路由

| 页面名称 | 路由/路径 | 页面类型 | 对应功能 |
|---|---|---|---|
| 收藏夹列表页 | /favorites | 列表页 | F01、F04、F05、F06、F07、F08、F10 |
| 收藏确认弹窗 | /favorites (弹窗组件) | 弹窗 | F02 的二次确认（可选） |
| 取消收藏确认弹窗 | /favorites (弹窗组件) | 弹窗 | F03 的二次确认 |
| 业务对象页收藏入口 | 各业务对象页面内嵌 | 行内按钮 | F02、F03 |

### 6.1 页面入口

- **PC 端全局侧边栏**：一级导航「收藏」，图标使用 SVG 星形图标，文案「收藏」。
- **应用中心**：收藏夹不作为应用中心内的业务应用展示，仅通过侧边栏一级入口进入。
- **移动端**：底部 Tab 栏或侧边栏折叠菜单中的「收藏」入口，与 PC 端保持同一图标与文案。
- **工作台联动**：工作台可配置「我的收藏」小组件，点击「查看更多」跳转至 /favorites。

---

## 七、页面结构与布局

### 7.1 收藏夹列表页

#### 7.1.1 页面信息分层

```
┌─────────────────────────────────────────────┐
│ 我的收藏                          [管理]     │  说明：页面标题 + 操作
├─────────────────────────────────────────────┤
│ 统计卡片：全部 / 客户 / 意向 / 项目 / 文档   │  说明：类型统计（可选）
├─────────────────────────────────────────────┤
│ Tab：全部 / 客户 / 意向 / 项目 / 文档 / 页面 │  说明：分类导航
├─────────────────────────────────────────────┤
│ 搜索框 + 排序按钮                            │  说明：搜索与排序
├─────────────────────────────────────────────┤
│ 收藏项列表（卡片/列表视图）                  │  说明：明细内容
├─────────────────────────────────────────────┤
│ 分页 / 加载更多                              │  说明：分页
└─────────────────────────────────────────────┘
```

#### 7.1.2 核心组件

| 组件 | 组件名 | 用途 | Props 关键配置 |
|---|---|---|---|
| 页面布局容器 | XqPageLayout | 统一五层结构容器 | title="我的收藏"，showStats=true，showFilter=true |
| 统计卡片 | XqKpiCard | 展示各类型的收藏数量 | clickable=true，点击后切换对应 Tab |
| 分类 Tab | XqNavTabs | 按类型筛选收藏 | type='line'，showCount=true |
| 搜索栏 | XqSearchBar | 按名称/拼音搜索收藏 | placeholder='搜索客户、意向、项目、文档...'，pinyinSearch=true |
| 排序按钮 | XqButton / XqIconSort | 切换默认排序与自定义排序 | ghost 样式 |
| 收藏项列表 | XqDataTable / XqCardGrid | 展示收藏项 | 默认卡片视图，数据量 >20 可切换列表视图 |
| 收藏项卡片 | XqFavoriteCard（业务组件） | 单条收藏项展示 | data, showActions |
| 空态 | XqEmptyState | 无收藏时引导 | type='empty'，action={text:'去应用中心看看'} |
| 确认弹窗 | XqConfirmDialog | 取消收藏二次确认 | dangerous=false |
| 轻提示 | XqToast | 操作反馈 | type='success' / 'error' |

#### 7.1.3 收藏项卡片结构

```
┌─────────────────────────────────────────────────────────────┐
│ [图标]  对象名称（单行截断）                    [类型标签]   │
│         对象副标题（类型说明 / 区域 / 负责人 / 时间）        │
│                                                   [取消收藏] │
└─────────────────────────────────────────────────────────────┘
```

**必显字段**：对象图标、对象名称（targetName）、对象副标题（targetSubTitle）、类型标签（targetTypeText）。

**操作按钮**：取消收藏（空心星形图标或文字按钮）。

### 7.2 PC 端布局

- **布局结构**：单页全宽布局，无左右双栏。页面内边距 16px-20px。
- **统计卡片**：横向排列 3-5 个，宽度均分，>=1440px 时 5 个，1024px-1439px 时 4 个，768px-1023px 时 3 个。
- **Tab 导航**：统计卡片下方，横向排列，类型数量不超过 7 个。
- **搜索栏**：位于 Tab 右侧或第三层筛选区左侧，PC 端宽度 240px。
- **明细区域**：默认卡片网格视图，>=1440px 时 4 列，1024px-1439px 时 3 列，768px-1023px 时 2 列。
- **操作交互**：鼠标悬停卡片显示「取消收藏」按钮；点击卡片整行跳转。
- **分页**：底部分页组件，卡片视图每页 8 条，列表视图每页 12 条。

### 7.3 移动端布局

- **布局结构**：单列布局，顶部固定标题栏，内容区可滚动。
- **标题栏**：左侧返回/菜单按钮（如从工作台进入），标题「我的收藏」，右侧「管理」按钮进入批量/排序模式。
- **统计卡片**：横向滚动，每个卡片最小宽度 120px，不超过 5 个。
- **Tab 导航**：顶部横向滚动，当前 Tab 高亮。
- **搜索栏**：宽度 100%，吸顶显示，placeholder 与 PC 端一致。
- **明细区域**：强制单列卡片列表，每个卡片占满屏幕宽度。
- **手势**：
  - 左滑卡片：显示「取消收藏」快捷操作。
  - 长按卡片：弹出操作菜单（取消收藏、置顶、查看详情）。
  - 下拉刷新：刷新收藏列表。
  - 上拉加载：加载下一页。
- **排序模式**：点击「管理」进入拖拽排序，底部固定「完成」按钮。
- **分页**：简化为「加载更多」或「1/10 页」。

### 7.4 业务对象页收藏入口

- **位置**：客户 360° 详情页标题右侧、意向卡片右上角、项目详情页标题右侧、文档预览页标题右侧、应用中心应用卡片右上角。
- **图标**：未收藏显示空心星形 XqIconStar，已收藏显示实心星形 XqIconStarFilled，颜色使用 `--primary`。
- **交互**：点击切换收藏状态，操作成功后显示 Toast 提示。
- **权限**：仅当用户对该对象有查看权限时才显示收藏入口；无权限时不显示。

---

## 八、核心字段规范

### 8.1 收藏主表字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| favoriteId | 收藏 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| userId | 用户 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 不可见 | 不显示 |
| tenantId | 租户 ID | Integer | - | 是 | 当前租户 | 系统生成 | - | 不可见 | 不显示 |
| targetType | 收藏对象类型 | Enum | - | 是 | 无 | 系统生成/字典 | 1:客户, 2:意向, 3:项目, 4:文档, 5:页面, 6:任务, 7:联系人, 8:设备, 9:试剂 | 全员可见 | 状态标签 |
| targetId | 收藏对象 ID | String | 100 | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| targetName | 收藏对象名称 | String | 200 | 是 | 无 | 关联对象 | - | 全员可见 | 列表/详情/卡片标题 |
| targetSubTitle | 收藏对象副标题 | String | 300 | 否 | 无 | 关联对象/计算 | - | 全员可见 | 列表/详情辅助文字 |
| targetRoute | 跳转路由 | String | 500 | 是 | 无 | 关联对象/计算 | - | 只读 | 不显示，用于点击跳转 |
| iconType | 图标类型 | String | 50 | 否 | 按 targetType 默认 | 字典 | - | 全员可见 | 图标组件名或 SVG 标识 |
| iconColor | 图标颜色 | String | 20 | 否 | #2563eb | 字典 | - | 全员可见 | 图标颜色 |
| sortOrder | 排序序号 | Integer | - | 是 | 0 | 系统生成/用户调整 | - | 本人可编辑 | 用于列表排序 |
| isTop | 是否置顶 | Boolean | - | 是 | false | 用户操作 | - | 本人可编辑 | 开关/置顶标识 |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情显示 |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情显示 |
| deleted | 删除标记 | Boolean | - | 是 | false | 系统生成 | - | 不可见 | 不显示 |

### 8.2 targetType 枚举值

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 1 | 客户 | #2563eb | 客户 360° 对象 |
| 2 | 意向 | #f59e0b | 意向管理对象 |
| 3 | 项目 | #22c55e | 项目/商机对象 |
| 4 | 文档 | #64748b | 文档/资料对象 |
| 5 | 页面 | #7c3aed | 系统页面/应用入口 |
| 6 | 任务 | #0ea5e9 | 任务对象 |
| 7 | 联系人 | #ec4899 | 通讯录联系人 |
| 8 | 设备 | #14b8a6 | 设备管理对象 |
| 9 | 试剂 | #8b5cf6 | 试剂运营对象 |

### 8.3 搜索/筛选参数字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| keyword | 搜索关键词 | String | 100 | 否 | 无 | 手工录入 | 本人可见 | 搜索框输入 |
| targetType | 类型筛选 | Enum | - | 否 | 0（全部） | 字典/Tab 选择 | 本人可见 | Tab 标签 |
| sortField | 排序字段 | Enum | - | 否 | createTime | 字典 | 本人可见 | 排序下拉 |
| sortOrder | 排序方向 | Enum | - | 否 | desc | 字典：asc/desc | 本人可见 | 排序下拉 |
| pageNum | 页码 | Integer | - | 是 | 1 | 系统生成 | 本人可见 | 分页组件 |
| pageSize | 每页条数 | Integer | - | 是 | 8 | 系统生成 | 本人可见 | 分页组件 |

---

## 九、交互设计要点

### 9.1 搜索与筛选

- **搜索框**：使用 XqSearchBar 组件，placeholder 为「搜索客户、意向、项目、文档...」。
- **实时搜索**：输入后 debounce 300ms 触发搜索，同时支持拼音首字母匹配。
- **分类 Tab**：与搜索条件联动，切换 Tab 时保留搜索关键词，重置分页到第 1 页。
- **排序**：默认按 isTop 降序 + createTime 降序；用户可切换为按名称升序/降序或自定义拖拽排序。
- **清空**：搜索框支持一键清空，清空后恢复全部列表。

### 9.2 操作反馈

- **添加收藏**：图标由空心变为实心，同时显示 Toast「收藏成功」。
- **取消收藏**：列表中该项移除（动画 200ms），显示 Toast「已取消收藏」。
- **排序调整**：拖拽释放后显示 Toast「排序已保存」。
- **网络异常**：操作失败时图标状态回滚，列表不做删除动画，显示错误提示。
- **无权限跳转**：点击收藏项后若对象无权限，显示 XqToast type='error' 提示「暂无权限查看该对象」。

### 9.3 空态设计

- **无收藏**：使用 XqEmptyState type='empty'，标题「暂无收藏」，描述「收藏客户、意向、项目等，快速访问常用内容」，操作按钮「去应用中心看看」。
- **搜索无结果**：使用 XqEmptyState type='search'，标题「未找到相关收藏」，描述「尝试更换关键词或切换类型筛选」，操作按钮「清空搜索条件」。
- **对象已删除**：收藏项置灰，副标题显示「对象已删除」，点击显示 Toast「该对象已被删除」。

### 9.4 快捷操作

- **PC 端**：鼠标悬停卡片显示「取消收藏」星形按钮；点击卡片主体跳转。
- **移动端**：左滑显示「取消收藏」按钮；长按弹出操作菜单（置顶、取消收藏、查看详情）。
- **批量管理**：点击「管理」进入批量模式，显示复选框，支持批量取消收藏。

### 9.5 异常处理

| 异常场景 | 处理方式 | 页面反馈 |
|---|---|---|
| 网络错误 | 前端重试 1 次，失败后回滚状态 | Toast「网络异常，请稍后重试」 |
| 对象不存在 | 后端返回后前端将该项置灰 | 项显示「对象已删除」，保留移除操作 |
| 对象无权限 | 跳转前校验，失败则阻止跳转 | Toast「暂无权限查看该对象」 |
| 收藏数量超限 | 后端返回错误码 | Toast「收藏数量已达上限（200）」 |
| 数据同步冲突 | 多端同时操作同一条收藏 | 以服务端最后写入为准，刷新列表 |

---

## 十、响应式双端设计

### 10.1 断点规则

| 断点 | 宽度范围 | 布局策略 |
|---|---|---|
| PC 宽屏（lg） | >=1440px | 统计卡片 5 列，卡片网格 4 列 |
| PC 标准（md） | 1024px-1439px | 统计卡片 4 列，卡片网格 3 列 |
| 平板（sm） | 768px-1023px | 统计卡片 3 列，卡片网格 2 列，侧边栏可折叠 |
| 移动端（xs） | <768px | 单列布局，底部固定操作，抽屉全屏 |

### 10.2 PC 端布局细则

- **页面标题**：左侧「我的收藏」，右侧「管理」按钮（进入批量/排序模式）。
- **统计区**：使用 XqKpiCard 横向排列，点击可切换对应类型 Tab。
- **Tab 区**：全部 / 客户 / 意向 / 项目 / 文档 / 页面，不超过 7 个。
- **搜索区**：XqSearchBar 宽度 240px，右侧为排序下拉。
- **内容区**：默认卡片网格，支持卡片/列表视图切换（XqViewSwitch）。
- **分页**：底部分页组件，切换视图/Tab/搜索后重置到第 1 页。

### 10.3 移动端布局细则

- **顶部栏**：标题「我的收藏」居中，右侧「管理」按钮。
- **统计区**：横向滚动，每个卡片最小宽度 120px，不折行。
- **Tab 区**：顶部横向滚动，当前 Tab 下划线高亮。
- **搜索区**：吸顶搜索框，宽度 100%。
- **内容区**：单列卡片列表，卡片内信息垂直排列。
- **底部按钮**：管理模式下底部固定「完成」按钮；普通模式无底部按钮。
- **手势**：下拉刷新、上拉加载、左滑取消收藏、长按弹出菜单。

### 10.4 字段折叠规则

- **PC 端卡片**：显示对象图标、名称、副标题、类型标签、取消收藏按钮。
- **移动端卡片**：显示对象图标、名称（单行截断）、副标题（单行截断）、类型标签；取消收藏通过左滑或长按调出，默认不显示。

### 10.5 按钮位置规则

- **PC 端**：取消收藏按钮在卡片右上角，管理模式下复选框在卡片左上角。
- **移动端**：取消收藏按钮在左滑后的右侧，或长按菜单中；管理模式下复选框在卡片左侧。

---

## 十一、接口契约（前端视角）

### 11.1 获取收藏列表

- **接口名**：getFavoriteList
- **请求方式**：POST
- **路径**：/api/v1/favorite/list
- **场景**：收藏夹列表页加载、筛选、搜索、分页
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| keyword | String | 否 | 搜索关键词，支持名称和拼音首字母 |
| targetType | Integer | 否 | 类型筛选，0 或不传表示全部 |
| sortField | String | 否 | 排序字段：createTime / targetName / sortOrder，默认 createTime |
| sortOrder | String | 否 | 排序方向：asc / desc，默认 desc |
| pageNum | Integer | 是 | 页码，默认 1 |
| pageSize | Integer | 是 | 每页条数，默认 8 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<Object> | 收藏列表，字段见 8.1 |
| total | Integer | 总条数 |
| pageNum | Integer | 当前页码 |
| pageSize | Integer | 每页大小 |
| typeCountMap | Object | 各类型数量，如 {1: 5, 2: 3} |

### 11.2 添加收藏

- **接口名**：addFavorite
- **请求方式**：POST
- **路径**：/api/v1/favorite/add
- **场景**：在业务对象页点击收藏图标
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| targetType | Integer | 是 | 收藏对象类型，见 8.2 |
| targetId | String | 是 | 收藏对象唯一标识 |
| targetName | String | 是 | 收藏对象显示名称 |
| targetSubTitle | String | 否 | 收藏对象副标题 |
| targetRoute | String | 是 | 跳转路由或页面标识 |
| iconType | String | 否 | 图标类型 |
| iconColor | String | 否 | 图标颜色 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| favoriteId | Long | 新增收藏 ID |
| targetType | Integer | 收藏类型 |
| createTime | DateTime | 创建时间 |
| success | Boolean | 是否成功 |
| errorCode | String | 错误码，失败时返回 |
| errorMsg | String | 错误信息，失败时返回 |

### 11.3 取消收藏

- **接口名**：removeFavorite
- **请求方式**：POST
- **路径**：/api/v1/favorite/remove
- **场景**：在收藏夹列表或业务对象页取消收藏
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| favoriteId | Long | 否 | 收藏记录 ID，与 targetType+targetId 二选一 |
| targetType | Integer | 否 | 收藏对象类型 |
| targetId | String | 否 | 收藏对象 ID |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |
| errorCode | String | 错误码，失败时返回 |
| errorMsg | String | 错误信息，失败时返回 |

### 11.4 更新收藏排序

- **接口名**：updateFavoriteSort
- **请求方式**：POST
- **路径**：/api/v1/favorite/sort
- **场景**：用户拖拽调整收藏顺序
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| favoriteIds | Array<Long> | 是 | 按新顺序排列的收藏 ID 数组 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |
| errorCode | String | 错误码，失败时返回 |
| errorMsg | String | 错误信息，失败时返回 |

### 11.5 检查是否已收藏

- **接口名**：checkFavorite
- **请求方式**：GET
- **路径**：/api/v1/favorite/check
- **场景**：业务对象页加载时判断收藏按钮状态
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| targetType | Integer | 是 | 收藏对象类型 |
| targetId | String | 是 | 收藏对象 ID |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| isFavorite | Boolean | 是否已收藏 |
| favoriteId | Long | 收藏记录 ID，已收藏时返回 |

---

## 十二、验收断言清单

### 功能：收藏列表查看

#### 断言 1：收藏列表默认展示全部类型

- **验证对象**：收藏夹列表页
- **前置条件**：用户已登录，收藏夹中存在 3 条客户、2 条意向、1 条文档收藏
- **操作步骤**：
  1. 点击侧边栏「收藏」进入收藏夹页面
  2. 等待列表加载完成
- **预期结果**：
  1. 页面标题显示「我的收藏」
  2. 默认 Tab 为「全部」
  3. 列表展示 6 条收藏记录
  4. 统计卡片显示客户 3、意向 2、文档 1
- **验收方式**：自动化测试

#### 断言 2：切换类型 Tab 过滤收藏

- **验证对象**：分类 Tab 筛选
- **前置条件**：用户已进入收藏夹页面，存在多类型收藏
- **操作步骤**：
  1. 点击「客户」Tab
  2. 等待列表刷新
- **预期结果**：
  1. 列表仅显示 targetType=1 的收藏项
  2. 总条数更新为客户类型数量
  3. 其他类型收藏项不显示
- **验收方式**：自动化测试

#### 断言 3：搜索收藏支持拼音首字母

- **验证对象**：搜索框
- **前置条件**：列表中存在名称为「昆明市第一人民医院」的客户收藏
- **操作步骤**：
  1. 在搜索框输入「kmsdyrmyy"
  2. 等待 300ms debounce
- **预期结果**：
  1. 列表仅显示「昆明市第一人民医院」
  2. 总条数更新为 1
  3. 清空搜索后列表恢复
- **验收方式**：自动化测试

### 功能：添加收藏

#### 断言 4：客户详情页添加收藏成功

- **验证对象**：客户 360° 详情页收藏入口
- **前置条件**：用户已登录并进入一个有查看权限的客户详情页，该客户未被收藏
- **操作步骤**：
  1. 点击标题右侧空心星形收藏图标
  2. 等待接口返回
- **预期结果**：
  1. 图标变为实心星形
  2. 显示 Toast「收藏成功」
  3. 调用 /api/v1/favorite/add 接口，入参 targetType=1
  4. 进入收藏夹页面，新收藏项位于列表顶部
- **验收方式**：自动化测试

#### 断言 5：重复收藏前端拦截

- **验证对象**：收藏图标状态
- **前置条件**：用户已收藏当前客户
- **操作步骤**：
  1. 再次点击实心星形收藏图标
- **预期结果**：
  1. 不调用添加接口
  2. 显示 Toast「已收藏」
  3. 图标保持实心状态
- **验收方式**：自动化测试

#### 断言 6：收藏数量超限提示

- **验证对象**：添加收藏接口
- **前置条件**：当前用户已收藏 200 条记录（达到上限）
- **操作步骤**：
  1. 进入新客户详情页
  2. 点击收藏图标
- **预期结果**：
  1. 调用添加接口
  2. 接口返回数量超限错误码
  3. 显示 Toast「收藏数量已达上限（200）」
  4. 图标保持空心
- **验收方式**：接口测试 / 自动化测试

### 功能：取消收藏

#### 断言 7：收藏夹列表取消收藏成功

- **验证对象**：收藏夹列表取消收藏功能
- **前置条件**：用户已进入收藏夹页面，存在至少 1 条收藏
- **操作步骤**：
  1. 鼠标悬停某条收藏卡片
  2. 点击「取消收藏」按钮
  3. 在确认弹窗中点击「确认」
- **预期结果**：
  1. 调用 /api/v1/favorite/remove 接口
  2. 该卡片从列表移除（带动画）
  3. 显示 Toast「已取消收藏」
  4. 总条数减 1
- **验收方式**：自动化测试

#### 断言 8：取消收藏后端权限校验

- **验证对象**：取消收藏接口权限
- **前置条件**：用户 A 存在收藏记录，用户 B 登录
- **操作步骤**：
  1. 用户 B 通过篡改请求参数尝试取消用户 A 的收藏
- **预期结果**：
  1. 接口返回无权限错误
  2. 用户 A 的收藏数据未被删除
  3. 前端不做任何列表变化
- **验收方式**：接口测试

#### 断言 9：移动端左滑取消收藏

- **验证对象**：移动端收藏卡片手势
- **前置条件**：使用 iPhone 14 尺寸模拟器，进入收藏夹页面
- **操作步骤**：
  1. 在某条收藏卡片上向左滑动
  2. 点击显示的「取消收藏」按钮
- **预期结果**：
  1. 左滑后显示「取消收藏」按钮
  2. 点击后该卡片移除
  3. 显示 Toast「已取消收藏」
- **验收方式**：截图对比 / 自动化测试

### 功能：收藏跳转

#### 断言 10：点击收藏项跳转客户详情

- **验证对象**：收藏项点击跳转
- **前置条件**：收藏夹中存在一条客户类型的收藏，且用户有该客户查看权限
- **操作步骤**：
  1. 点击该客户收藏卡片
- **预期结果**：
  1. 路由跳转至客户 360° 详情页 /customer/{targetId}
  2. 详情页正确加载客户数据
- **验收方式**：自动化测试

#### 断言 11：无权限收藏项点击提示

- **验证对象**：收藏项权限联动
- **前置条件**：收藏夹中存在一条用户已失去查看权限的客户收藏
- **操作步骤**：
  1. 点击该收藏卡片
- **预期结果**：
  1. 不执行路由跳转
  2. 显示 Toast「暂无权限查看该对象」
- **验收方式**：自动化测试

#### 断言 12：已删除对象点击提示

- **验证对象**：已删除对象收藏项
- **前置条件**：收藏夹中存在一条目标对象已被删除的收藏
- **操作步骤**：
  1. 点击该收藏卡片
- **预期结果**：
  1. 不执行路由跳转
  2. 显示 Toast「该对象已被删除」
- **验收方式**：自动化测试

### 功能：响应式布局

#### 断言 13：PC 端卡片网格列数正确

- **验证对象**：PC 端收藏夹列表布局
- **前置条件**：使用 1440px 宽度浏览器，收藏数量 >= 4
- **操作步骤**：
  1. 进入收藏夹页面
- **预期结果**：
  1. 卡片网格每行展示 4 列
  2. 统计卡片横向排列 5 个
- **验收方式**：截图对比

#### 断言 14：移动端单列布局正确

- **验证对象**：移动端收藏夹列表布局
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 进入收藏夹页面
  2. 向下滚动列表
- **预期结果**：
  1. 收藏卡片单列展示
  2. 搜索框吸顶不随滚动消失
  3. 内容区无横向溢出
- **验收方式**：截图对比 / 自动化测试

---

## 十三、关联与扩展

### 13.1 与其它模块关联

| 关联模块 | 关联关系 | 说明 |
|---|---|---|
| 工作台 | 数据联动 | 工作台「我的收藏」小组件读取收藏数据，点击更多跳转收藏夹 |
| 客户 360° | 收藏来源 | 客户详情/列表页提供收藏入口，收藏夹可跳转客户详情 |
| 意向管理 | 收藏来源 | 意向详情/列表页提供收藏入口，收藏夹可跳转意向详情 |
| 任务 | 收藏来源 | 任务详情页提供收藏入口，收藏夹可跳转任务详情 |
| 通讯录 | 收藏来源 | 联系人详情页提供收藏入口 |
| 设备管理 | 收藏来源 | 设备详情页提供收藏入口 |
| 试剂运营 | 收藏来源 | 试剂项目详情页提供收藏入口 |
| 消息中心 | 潜在联动 | 收藏对象发生变更时可推送消息提醒（可选） |
| 应用中心 | 收藏来源 | 应用卡片右上角提供收藏入口，收藏后可在收藏夹快速打开应用 |

### 13.2 易遗漏点

1. **对象删除后的收藏项处理**：被收藏对象删除后，收藏项不能静默消失，应置灰并提示用户手动清理。
2. **权限回收后的处理**：用户失去对象查看权限后，收藏项仍可见但点击跳转需拦截并提示。
3. **跨端同步**：PC 端添加的收藏需在移动端实时同步，依赖接口数据而非本地缓存。
4. **收藏数量上限**：需在前后端同时校验单用户最大收藏数量，默认 200，可后台配置。
5. **排序持久化**：自定义拖拽排序必须保存到服务端，刷新后顺序保持一致。
6. **图标一致性**：各业务模块的收藏图标状态必须联动，取消收藏后源页面图标同步恢复空心。
7. **搜索拼音兼容性**：targetName 为中文、英文、数字混合时需同时支持拼音首字母与原文匹配。
8. **最近访问记录**：点击收藏项后应更新最近访问时间，供工作台最近访问小组件使用。
9. **批量管理模式**：移动端管理模式下底部固定完成按钮，避免误触取消收藏。
10. **深色模式适配**：收藏图标高亮状态在深色模式下需使用 `--primary` 变量，避免硬编码颜色。

### 13.3 待确认问题

1. 收藏夹是否支持收藏「报表/驾驶舱」等数据看板页面？建议纳入 targetType=5 页面类型。
2. 是否需要在收藏夹展示「最近添加」或「智能推荐」分组？建议 V1.1 版本再考虑。
3. 移动端「管理」模式下的拖拽排序是否需要独立的排序编辑页面？当前方案为列表内拖拽。
4. 收藏数量上限默认值是否为 200，是否按角色差异化配置？建议默认统一，后台可配。

---

*本文档为鑫渠 CRM 收藏夹模块 AI 可执行设计文档，开发完成后须对照「验收断言清单」逐项验证。*
