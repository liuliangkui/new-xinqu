# XQCOP 开发日志

> 记录当前开发状态、已完成工作、待处理事项和下一步计划。
> 换电脑、会话中断或终端崩溃后，优先阅读此文件恢复上下文。

---

## 当前状态（最近更新：2026-08-01）

### ✅ 已完成

1. **后端脚手架搭建**
   - NestJS + TypeScript 工程结构
   - Prisma + PostgreSQL + Redis 集成
   - JWT 认证模块（`/api/v1/auth/login`）
   - 用户、客户、线索基础 CRUD
   - Swagger 文档：`http://localhost:3000/api/docs`

2. **API 接口规范制定**
   - 统一响应格式 `{ code, data, message, success }`
   - 前后端请求封装 `apps/web/src/api/request.ts`
   - OpenAPI 类型生成 `npm run api:types`

3. **Monorepo 目录结构调整**
   - 前端：`apps/web/`
   - 后端：`apps/server/`
   - 文档：`docs/`
   - 原型：`prototype/`
   - 脚本：`scripts/`

4. **项目管理文件补齐**
   - 根目录 `README.md`
   - `CONTRIBUTING.md`
   - `docs/README.md` 文档索引
   - `.github/workflows/ci.yml`
   - `.nvmrc`
   - 前后端基础单元测试

### 🔄 待处理 / 阻塞

- [ ] **GitHub push 网络问题**：commit `59f76c2` 已本地提交，但 push 因网络失败，需手动重试 `git push`
- [ ] **Docker 数据库迁移**：本地 Docker Desktop 未启动，`npm run db:migrate` 尚未执行

### 📌 下一步计划（按优先级）

1. **启动 Docker，跑通数据库**
   - `docker-compose up -d postgres redis`
   - `npm run db:migrate`
   - `npm run db:seed`
   - 验证 `POST /api/v1/auth/login`（admin / admin123）

2. **前端登录对接真实接口**
   - 修改 `apps/web/src/views/login/index.vue`
   - 调用 `login()` from `@/api/auth`
   - `authStore` 存储 token 和用户信息

3. **补齐组织架构与权限基础**
   - 部门树接口
   - 角色权限接口
   - 数据权限范围（SELF / DEPT / DEPT_AND_CHILD / REGION / ALL）

4. **开发第一个业务模块：客户 360°**
   - 后端：Customer CRUD + 联系人/科室/拜访记录
   - 前端：客户列表、详情、表单

---

## 重要文件路径速查

| 用途 | 路径 |
|------|------|
| 后端入口 | `apps/server/src/main.ts` |
| 后端 Prisma Schema | `apps/server/prisma/schema.prisma` |
| 后端认证 | `apps/server/src/modules/auth/` |
| 前端入口 | `apps/web/src/main.ts` |
| 前端请求封装 | `apps/web/src/api/request.ts` |
| 前端登录页 | `apps/web/src/views/login/index.vue` |
| API 规范 | `docs/XQCOP-API接口规范.md` |
| 领域模型 | `docs/XQCOP-领域建模方案.md` |
| 技术架构 | `docs/XQCOP-技术架构方案.md` |
| 原型 | `prototype/鑫渠高保真原型.html` |

---

## 环境恢复命令

```bash
cd /Users/mac/qucheng

# 1. 安装依赖
npm install --legacy-peer-deps

# 2. 启动数据库
docker-compose up -d postgres redis

# 3. 数据库迁移 + 种子
npm run db:migrate
npm run db:seed

# 4. 启动开发服务
npm run dev:server  # 后端
npm run dev:web     # 前端
```

---

## 变更记录

| 日期 | 内容 | Commit |
|------|------|--------|
| 2026-08-01 | 补齐项目管理文件与工程规范 | `59f76c2`（待 push） |
| 2026-08-01 | 调整 monorepo 目录结构 | `55cd739` |
| 2026-08-01 | 制定接口规范并完善前后端请求封装 | `168e98f` |
| 2026-08-01 | 搭建 NestJS + Prisma + Redis 后端脚手架 | `51f5566` |
