# 鑫渠领域模型与主数据规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **文档定位**：定义鑫渠 CRM 全局领域边界、核心聚合根、主数据标准与跨模块引用规则，作为所有业务模块字段设计的统一依据。

---

## 一、核心领域与聚合根

| 领域（限界上下文） | 聚合根 | 核心职责 | 依赖领域 |
|---|---|---|---|
| 客户域 | HCO（医院/医疗机构） | 维护客户主数据、科室、决策链、地址、等级 | 用户权限、标签、附件 |
| 线索域 | 线索（Lead） | 低门槛商机登记、查重、分配、跟进、转意向 | 客户域、用户权限 |
| 意向域 | 意向单（Intention） | 高门槛项目报备、审批、保护期、冲突裁决 | 客户域、品牌域、用户权限、工作流 |
| 订单域 | 订单/合同 | 中标、签约、送货、回款 | 意向域、客户域、品牌域 |
| 设备域 | 设备档案 | 招标跟踪、装机、台账、维保、保修预警 | 客户域、品牌域、工单域 |
| 试剂域 | 试剂档案 | 设备-试剂绑定、消耗记录、库存快照、预测 | 设备域、客户域、品牌域 |
| 工单域 | 售后工单 | 报修、派单、处理、回访、满意度 | 设备域、客户域、用户权限 |
| 经销商域 | 经销商档案 | 准入、授权、区域保护、资质年审 | 用户权限、品牌域 |
| 营销域 | 市场活动/样品 | 学术活动、样品管理、拜访任务 | 客户域、经销商域、合规域 |
| 合规域 | 合规记录/飞检任务 | 拜访合规、活动审批、样品审计、飞检抽查 | 客户域、营销域、经销商域 |
| 绩效域 | 绩效目标/达成 | 目标拆解、达成跟踪、预警 | 意向域、订单域、设备域、试剂域 |
| 用户权限域 | 用户/角色/组织 | 账号、角色、数据范围、字段权限 | 无（基础设施） |
| 工作流域 | 流程定义/实例 | 审批流、业务流、自动化流 | 各业务域 |
| 标签域 | 标签定义/绑定 | 全局/应用/对象三级标签 | 各业务域 |
| 通知域 | 消息/待办 | 站内信、企业微信、短信、邮件推送 | 各业务域 |

> **设计原则**：每个聚合根只负责本聚合内部的数据一致性，跨聚合数据变更通过**领域事件**或**应用服务编排**完成，禁止直接跨库表修改。

---

## 二、主数据清单与引用规则

### 2.1 主数据定义

主数据是指在多个业务模块中被共享、需要保持一致性的核心业务对象。鑫渠主数据包括：

| 主数据 | 维护主体 | 消费方 | 是否允许业务模块本地快照 |
|---|---|---|---|
| HCO 客户 | 客户 360° / 后台设置 | 线索、意向、设备、试剂、工单、经销商协同、合规风控 | 是（需记录引用主键 + 快照字段） |
| 科室 | 客户 360° | 设备、试剂、工单、拜访 | 否（必须引用） |
| 联系人/决策链 | 客户 360° | 意向、拜访、活动、样品 | 是（联系人姓名/职务快照） |
| 品牌 | 品牌库管理 | 意向、设备、试剂、经销商授权 | 否（必须引用） |
| 产品线/型号 | 品牌库管理 | 意向、设备、试剂、工单备件 | 否（必须引用） |
| 注册证 | 品牌库管理 | 意向、设备、试剂 | 否（必须引用） |
| 经销商 | 后台设置 / 经销商协同 | 授权、区域保护、返利、库存、任务 | 否（必须引用） |
| 设备档案 | 设备管理 | 试剂、工单、客户 360° | 否（必须引用） |
| 用户/组织/角色 | 后台设置 | 全模块 | 否（必须引用） |

### 2.2 引用规则

1. **主键引用 + 冗余快照**：业务单据必须保存主数据 ID，同时可冗余保存当时的主数据名称、编码等快照字段，用于查询性能与历史追溯。
2. **快照刷新策略**：
   - 普通字段变更（如图片、简介）：不强制刷新历史单据快照。
   - 关键字段变更（如客户名称、型号名称、注册证号）：系统提示影响范围，可由管理员选择「仅更新主数据」或「同步刷新历史快照」。
3. **主数据删除**：主数据不支持物理删除，仅支持「禁用/失效」。被禁用的主数据在新单据中不可选，但历史单据仍可正常显示。
4. **数据一致性校验**：业务单据保存时，必须校验引用的主数据是否存在且处于有效状态。

---

## 三、核心领域事件

| 事件 | 发布方 | 消费方 | 业务含义 |
|---|---|---|---|
| `LeadCreated` | 线索域 | 待办、消息 | 新线索录入 |
| `LeadConverted` | 线索域 | 意向域 | 线索转为意向 |
| `IntentSubmitted` | 意向域 | 工作流、消息 | 意向提交审批 |
| `IntentApproved` | 工作流 | 意向域、设备域、消息 | 意向审批通过 |
| `IntentConflicted` | 意向域 | 经销商域、消息 | 区域冲突触发 |
| `EquipmentInstalled` | 设备域 | 试剂域、工单域、客户 360° | 设备完成装机 |
| `ReagentConsumptionTracked` | 试剂域 | 绩效域、驾驶舱 | 试剂消耗记录更新 |
| `WorkOrderCreated` | 工单域 | 设备域、客户 360°、消息 | 售后工单创建 |
| `WorkOrderClosed` | 工单域 | 客户 360°、满意度、知识库 | 工单关闭 |
| `DealerAuthorized` | 经销商域 | 意向域、库存域 | 经销商授权生效 |
| `ComplianceViolationFound` | 合规域 | 消息、驾驶舱 | 合规违规命中 |
| `TargetCreated` | 绩效域 | 驾驶舱 | 目标发布 |

> **事件设计原则**：事件命名使用过去时，只描述「发生了什么」，不包含下一步动作指令；消费方自行决定如何处理。

---

## 四、跨模块字段一致性约定

| 字段语义 | 统一字段编码 | 数据类型 | 说明 |
|---|---|---|---|
| 客户 | `customerId` | 关联对象 | 必须关联 HCO 主数据 |
| 客户名称快照 | `customerName` | 文本 | 冗余保存，长度 ≤200 |
| 科室 | `departmentId` | 关联对象 | 必须关联客户下的科室 |
| 负责人 | `ownerId` | 关联用户 | 默认上级可在权限范围内调整 |
| 创建人 | `createdBy` | 关联用户 | 系统自动填充 |
| 创建时间 | `createdAt` | 日期时间 | ISO 8601 |
| 更新时间 | `updatedAt` | 日期时间 | ISO 8601 |
| 状态 | `status` | 枚举 | 必须引用统一状态机 |
| 删除标记 | `isDeleted` | 布尔 | 逻辑删除，禁止物理删除 |
| 租户标识 | `tenantId` | 文本 | 多租户扩展预留 |

---

## 五、数据范围与可见性基础

| 数据范围 | 定义 | 适用场景 |
|---|---|---|
| 本人 | 只能查看自己创建或负责的数据 | 销售代表个人线索/意向 |
| 本部门 | 自己及所在部门的数据 | 销售经理团队视图 |
| 本部门及下属 | 自己及所有下属部门的数据 | 区域总监大区视图 |
| 本区域 | 自己负责地理区域的数据 | 按行政区划授权 |
| 全部 | 全部数据 | 高管、系统管理员 |
| 自定义 | 通过表达式配置 | 复杂渠道授权场景 |

> **计算顺序**：先按数据范围过滤，再按字段权限脱敏，最后按操作权限控制按钮显隐。

---

## 六、主数据变更影响评估

| 变更类型 | 影响范围 | 处理策略 |
|---|---|---|
| 新增主数据 | 无历史影响 | 直接生效 |
| 修改非关键字段 | 历史单据显示 | 可选同步快照 |
| 修改关键字段 | 统计口径、历史单据 | 必须提示影响范围，走变更审批 |
| 禁用主数据 | 新单据不可引用 | 历史单据保留，统计时按失效标记 |
| 合并重复主数据 | 多个业务单据 | 必须生成合并映射表，保留审计日志 |

---

## 七、与其他文档的关系

- 本规范是《鑫渠字段规范模板.md》的上位规范。
- 各业务模块文档中的字段说明不得与本规范冲突。
- 新增业务模块前，须先在本规范中确认其聚合根、主数据引用关系与领域事件。
