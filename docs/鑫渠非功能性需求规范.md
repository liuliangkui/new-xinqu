# 鑫渠非功能性需求规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **适用范围**：鑫渠 CRM 全系统（IVD 行业渠道营销数字化平台）  
> **适用角色**：架构师、研发工程师、测试工程师、运维工程师、安全合规管理员

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：性能、可用性、可靠性、安全、可扩展性、可维护性、兼容性、数据保留与归档、合规等非功能性需求 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、设计目标

本规范定义鑫渠 CRM 在功能需求之外必须满足的非功能性需求（Non-Functional Requirements，NFR），目标包括：

- 为多租户、高并发的 IVD 行业渠道营销场景提供可预期的系统表现。
- 保障租户数据安全、业务连续性与合规可追溯。
- 支撑系统随业务增长横向扩展，降低运维与升级成本。
- 统一各业务模块在性能、安全、可靠性、兼容性方面的设计底线。

---

## 二、性能需求

### 2.1 页面加载

| 场景 | 指标 | 说明 |
|---|---|---|
| PC 端首屏加载 | ≤ 3 秒（标准网络） | 从输入 URL 到首屏可交互，CDN 静态资源除外 |
| 移动端首屏加载 | ≤ 4 秒（4G 网络） | 企业微信/小程序首屏可交互 |
| 路由切换 | ≤ 500 毫秒 | 同一应用内页面切换，不含首次加载 |
| 大数据量列表首次渲染 | ≤ 1.5 秒 | 单页 50 条以内，含筛选与排序 |

### 2.2 API 响应

| 场景 | 指标 | 说明 |
|---|---|---|
| 普通查询接口 | P95 ≤ 500 毫秒 | 单条/列表查询，不含复杂报表 |
| 复杂查询/聚合接口 | P95 ≤ 2 秒 | 含多表关联、聚合计算的查询 |
| 写操作接口 | P95 ≤ 800 毫秒 | 提交表单、审批操作、状态变更 |
| 文件上传接口 | ≤ 10 秒 / 20 MB | 单文件 20 MB 以内，支持分片 |

### 2.3 搜索

| 场景 | 指标 | 说明 |
|---|---|---|
| 全局搜索首字母/拼音 | P95 ≤ 800 毫秒 | ⌘K 全局搜索，结果前 10 条 |
| 应用内模糊搜索 | P95 ≤ 600 毫秒 | 按名称、编号、客户等字段 |
| 搜索结果返回 | ≤ 1 秒 | 含权限过滤后的结果 |

### 2.4 报表生成

| 场景 | 指标 | 说明 |
|---|---|---|
| 实时报表 | ≤ 3 秒 | 单租户、时间范围 ≤ 30 天 |
| 离线/复杂报表 | ≤ 5 分钟 | 跨租户、跨年、多维度聚合 |
| 报表导出（Excel） | ≤ 60 秒 / 10 万行 | 支持异步生成与下载链接 |

### 2.5 批量导入

| 场景 | 指标 | 说明 |
|---|---|---|
| 用户/组织架构导入 | ≤ 5 分钟 / 1 万条 | 含校验、去重、写入 |
| 客户/线索批量导入 | ≤ 10 分钟 / 5 万条 | 含重复判定、区域保护校验 |
| 导入失败反馈 | 实时展示前 100 条错误 | 支持下载完整错误报告 |

---

## 三、可用性与可靠性

### 3.1 SLA 目标

| 指标 | 目标值 | 说明 |
|---|---|---|
| 系统可用性 | ≥ 99.9%（月度） | 计划内维护窗口除外 |
| 核心审批流程可用性 | ≥ 99.95%（月度） | 工作流平台运行时 |
| 计划外停机 | ≤ 4 小时 / 季度 | 单次故障 |

### 3.2 RTO / RPO

| 指标 | 目标值 | 说明 |
|---|---|---|
| RTO（恢复时间目标） | ≤ 4 小时 | 核心业务恢复可接受水平 |
| RPO（恢复点目标） | ≤ 15 分钟 | 故障时允许丢失的最大数据量 |
| 数据库备份 RPO | ≤ 1 小时 | 关键业务库增量备份 |

### 3.3 部署与发布

- 支持灰度发布、蓝绿部署或滚动发布，单批次影响租户比例 ≤ 10%。
- 配置变更支持版本化与一键回滚，回滚时间 ≤ 15 分钟。
- 发布窗口优先安排在非业务高峰时段，并提前 24 小时通知受影响租户。

### 3.4 监控与告警

- 核心接口、数据库、消息队列、缓存、定时任务统一接入监控。
- 告警分级：P0（立即处理）、P1（30 分钟内）、P2（2 小时内）、P3（次日处理）。
- 错误日志聚合，支持按租户、接口、堆栈、时间维度检索。

---

## 四、安全需求

### 4.1 身份认证与访问控制

- 所有接口必须鉴权，禁止匿名访问业务数据。
- 支持企业微信 OAuth、账号密码、短信验证码等多种登录方式。
- 密码策略：最小 8 位，含大小写字母、数字、特殊字符；90 天强制更换；历史 5 次不可复用。
- 登录失败 5 次锁定 30 分钟，登录成功/失败/异常均记录审计日志。
- 会话有效期：PC 端 8 小时无操作自动退出，移动端 7 天或手动退出。

### 4.2 数据传输与存储加密

- 全站 HTTPS，TLS ≥ 1.2。
- 敏感字段（手机号、身份证、银行卡、密钥）存储必须加密，算法优先 AES-256-GCM。
- API Key、Secret、Token 等密钥使用 KMS/HSM 管理，界面脱敏展示，禁止明文落库。
- 数据库备份加密存储。

### 4.3 数据脱敏

- 按角色配置脱敏规则，默认对非授权角色脱敏手机号、金额、决策链、竞品价格。
- 脱敏规则覆盖列表、详情、导出、打印、接口返回等全部输出渠道。
- 管理员查看原始数据需二次确认并记录审计日志。

### 4.4 审计与日志

- 关键操作（登录、配置变更、审批、导出、敏感数据访问）100% 留痕。
- 审计日志包含：操作人、时间、IP、操作类型、对象、前后值、结果。
- 审计日志防篡改，保留策略见第八节。

### 4.5 备份

- 数据库每日全量备份 + 每小时增量备份。
- 文件对象存储跨区域冗余，版本保留 30 天。
- 密钥与证书单独备份，离线存储。

---

## 五、可扩展性

### 5.1 租户隔离

- 多租户采用逻辑隔离（schema/字段隔离），关键资源按租户限流。
- 单个租户数据量增长不得影响其他租户性能。
- 支持租户级功能开关、配置独立、数据独立导出。

### 5.2 插件与扩展点

- 提供标准化扩展点：自定义字段类型、自定义审批节点、自定义报表、自定义集成适配器。
- 扩展点通过 SDK/插件包注册，支持热插拔或版本化发布。
- 第三方系统集成通过统一集成网关接入，支持限流、熔断、重试。

### 5.3 水平扩展

- 应用服务无状态化，支持多实例水平扩展。
- 数据库读写分离，支持分库分表策略预留。
- 缓存、消息队列、搜索引擎支持集群部署。

---

## 六、可维护性

### 6.1 日志

- 日志统一采用结构化输出（JSON），包含 trace_id、tenant_id、user_id、接口路径、耗时、状态码。
- 日志级别规范：DEBUG/INFO/WARN/ERROR/FATAL，生产环境默认 INFO 及以上。
- 日志保留：应用日志 30 天，错误日志 90 天，审计日志按合规要求保留。

### 6.2 配置迁移

- 应用配置、流程定义、权限矩阵支持导出为 JSON/YAML，支持跨环境导入。
- 配置导入前提供冲突检测与预览，支持覆盖/跳过/合并策略。
- 环境间配置迁移纳入 CI/CD 或变更管理流程。

### 6.3 告警与运维

- 告警规则覆盖：错误率突增、接口耗时异常、数据库连接池耗尽、磁盘/内存/CPU 阈值、定时任务失败。
- 提供运维看板：实例健康度、接口 TOP 慢查询、异常分布、租户资源占用。
- 故障处理 SOP 文档化，关键故障 30 分钟内启动应急响应。

### 6.4 文档

- 每个模块必须同步维护设计文档、接口文档、部署文档、运维手册。
- 文档版本与代码/原型版本保持对应关系。

---

## 七、兼容性

### 7.1 浏览器

| 浏览器 | 最低版本 | 说明 |
|---|---|---|
| Chrome | ≥ 90 | 推荐 |
| Edge | ≥ 90 | 推荐 |
| Firefox | ≥ 88 | 支持 |
| Safari | ≥ 14 | 支持 |

### 7.2 移动端

| 平台 | 最低版本/说明 |
|---|---|
| iOS | ≥ 14 |
| Android | ≥ 10 |
| 企业微信 | 最新两个大版本 |
| 微信小程序 | 基础库 ≥ 2.20 |

### 7.3 企业微信与小程序

- 核心审批、任务、拜访签到、消息通知必须在企业微信/小程序可用。
- 复杂配置（流程设计、应用配置、权限矩阵）禁止在移动端操作。
- 企业微信 JS-SDK 接口按官方版本兼容。

### 7.4 上游/下游系统

- 与 ERP/OA/HR/主数据/财务系统对接时，优先采用 REST/HTTPS + JSON，支持 SFTP/数据库/SOAP 等 legacy 协议适配。
- 接口变更必须向后兼容至少一个大版本，废弃接口提前 6 个月通知。
- 提供接口版本管理（如 /api/v1、/api/v2）。

---

## 八、数据保留与归档

### 8.1 操作日志保留

| 日志类型 | 在线保留 | 归档保留 | 说明 |
|---|---|---|---|
| 登录日志 | 90 天 | 3 年 | 含失败/成功/异常 |
| 操作日志 | 180 天 | 5 年 | 含字段级变更 |
| 审批日志 | 180 天 | 10 年 | 合规长期保存 |
| 审计报告 | 1 年 | 10 年 | 飞检与合规审计 |
| 接口调用日志 | 30 天 | 1 年 | 集成排障 |

### 8.2 冷存储

- 超过在线保留期的日志、历史审批单、历史业务单据转冷存储（对象存储或低成本数据库）。
- 冷存储数据支持按租户、时间、对象类型检索，检索响应 ≤ 10 秒。
- 冷数据删除需符合合规要求，禁止物理删除未过保留期的审计数据。

---

## 九、合规要求

### 9.1 IVD 行业合规

- 满足医疗器械经营企业质量管理规范（GSP）对销售行为、客户资质、产品追溯的要求。
- 学术活动、样品管理、拜访记录等场景需支持飞检抽查与合规审计。
- 关键业务变更（价格、授信、授权）必须留痕并可追溯。

### 9.2 数据隐私

- 遵循《个人信息保护法》《数据安全法》要求，最小化收集与使用个人信息。
- 用户可查询、导出、删除本人数据（在业务允许范围内）。
- 与第三方共享数据需签订数据处理协议并记录授权。

### 9.3 审计要求

- 支持按租户、时间、操作人、对象类型导出审计报告。
- 审计日志不可被普通管理员修改或删除。
- 系统管理员操作同样纳入审计，并支持上级复核。

---

*文档结束*
