# 意向管理功能与交互说明

> **文档版本**：v1.4  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **对应规范**：《AI可执行设计文档编写规范.md》v1.0、《鑫渠组件库清单与命名规范.md》v1.0、《鑫渠响应式设计规范.md》v1.0、《鑫渠字段规范模板.md》v1.0、《鑫渠验收断言编写规范.md》v1.0、《鑫渠应用文档验收标准.md》v1.1  
> **适用角色**：报单人、上级领导、厂家经理、厂家负责人、送货审批人、总经理/销售总监  

---

## 机器指令块

开发本模块前，AI 必须先读取并遵守以下全局约束：

- **技术栈**：Vue 3 + TypeScript + Pinia + Vue Router + Ant Design Vue 4.x + Tailwind CSS
- **组件库**：必须使用《鑫渠组件库清单与命名规范.md》v1.0 中注册的组件名，禁止自行发明组件名
- **图标**：全部使用 SVG 图标或 Iconfont，禁止引用 PNG/JPG 图标
- **样式变量**：颜色、字号、间距必须使用《鑫渠全局设计系统规范.md》中的 CSS 变量
- **响应式断点**：
  - PC 宽屏：>=1440px
  - PC 标准：1024px-1439px
  - 平板：768px-1023px
  - 移动端：<768px
- **权限模型**：基于 RBAC + 数据范围，角色编码见《华鑫角色职责与用户故事.md》
- **数据规范**：字段命名必须与本文档「核心字段规范」完全一致
- **接口规范**：接口路径、字段命名必须与本文档「接口契约」完全一致
- **验收标准**：开发完成后必须对照本文档「验收断言清单」逐项验证
- **禁止项**：文档与代码中禁止出现真实 emoji 和可能渲染为彩色 emoji 的 Unicode 符号

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.4 | 2026-07-29 | 按 AI 可执行标准升级：补充机器指令块、组件映射、字段编码、接口契约、验收断言、响应式双端设计；同步原型 v1.8.0 | - |
| v1.3 | 2026-07-29 | 补充意向列表页布局设计理念、板块分布、组件清单 | - |
| v1.2 | 2026-07-29 | 意向列表页增加列表/卡片视图切换与分页，适配大量意向场景 | - |
| v1.1 | 2026-07-28 | 模块重命名：报备管理统一改为意向管理 | - |
| v1.0 | 2026-07-28 | 初始发布：报备管理模块完整功能、页面字段、交互流程、业务规则 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、模块定位与目标

### 1.1 一句话定位

意向管理是鑫渠 CRM 「销售过程管控域」的核心模块，用于管理 IVD 行业从线索/意向提交到项目结束的全生命周期，通过审批、保护期、冲突裁决机制确保每个项目都有唯一授权 Owner。

### 1.2 业务痛点

| 序号 | 痛点 | 说明 |
|---|---|---|
| 1 | 撞单频发 | 多个销售/渠道同时跟进同一客户同一项目，导致内部资源内耗 |
| 2 | 归属不清 | 项目缺乏明确 Owner，关键节点无人推进或多人重复推进 |
| 3 | 流程不透明 | 意向审批、跟进、结束状态分散，管理层难以掌握管道健康度 |
| 4 | 模式差异大 | 招标、非招标、TP/投放、捐赠业务的审批路径和结束条件不同 |
| 5 | 保护期失控 | 项目授权后缺乏到期提醒和进展更新机制，导致商机沉睡 |

### 1.3 量化价值

| 序号 | 价值 | 量化指标 |
|---|---|---|
| 1 | 降低撞单率 | 同客户同项目冲突检测覆盖率 100%，冲突处理时效 <= 2 个工作日 |
| 2 | 明确项目 Owner | 每个生效意向有且仅有 1 个负责人，授权率 100% |
| 3 | 提升审批效率 | 常规初审审批时效 <= 1 个工作日，审批记录 100% 留痕 |
| 4 | 沉淀过程数据 | 意向从提交到结束全流程数据可追溯，支撑业绩统计和复盘 |
| 5 | 保护期预警 | 保护期到期前 3 天、1 天、当天分别推送提醒 |

### 1.4 所属业务域

销售过程管控域 / 业务管理类应用。

### 1.5 与线索管理的关系

```
线索（登记层，无需审批） -> 查重/分配/跟进 -> 转意向（授权层，需要审批） -> 保护期跟进 -> 项目结束
```

| 维度 | 线索管理 | 意向管理 |
|---|---|---|
| 定位 | 信息信号 | 授权商机 |
| 是否需要审批 | 否 | 是 |
| 保护期 | 无 | 有 |
| 冲突处理 | 提示补充或合并 | 冲突裁决（双人并行审批） |
| 数据可见性 | 区域/经销商可见 | 按授权与审批结果可见 |

---

## 二、角色与权限

### 2.1 角色职责

| 角色编码 | 角色名称 | 核心权限 | 系统操作 |
|---|---|---|---|
| ROLE_REPORTER | 报单人 | 发起意向报备、修改被驳回单据、更新意向跟进信息、查看本人所有意向记录 | 新建、编辑、保存草稿、重新提交、录入跟进、结束分类 |
| ROLE_LEADER | 上级领导 | 无冲突场景一级初审 | 通过、驳回 |
| ROLE_FACTORY_MANAGER | 厂家经理 | 冲突场景并行审批、招标授权流程审批 | 冲突裁决通过/驳回、授权通过/驳回 |
| ROLE_FACTORY_OWNER | 厂家负责人 | 冲突场景与厂家经理并行复核审批 | 冲突裁决通过/驳回 |
| ROLE_DELIVERY_APPROVER | 送货审批人 | 招标中标后专项送货审批 | 通过、驳回 |
| ROLE_GENERAL_MANAGER | 总经理/销售总监 | 权限范围内全部意向（按数据切片） | 查看、导出、督办 |

### 2.2 功能权限矩阵

| 功能 | 报单人 | 上级领导 | 厂家经理 | 厂家负责人 | 送货审批人 | 总经理/销售总监 |
|---|---|---|---|---|---|---|
| 新建意向 | 是 | 否 | 否 | 否 | 否 | 否 |
| 编辑草稿 | 是 | 否 | 否 | 否 | 否 | 否 |
| 重新提交 | 是 | 否 | 否 | 否 | 否 | 否 |
| 提交审批 | 是 | 否 | 否 | 否 | 否 | 否 |
| 初审通过/驳回 | 否 | 是 | 否 | 否 | 否 | 否 |
| 冲突裁决 | 否 | 否 | 是 | 是 | 否 | 否 |
| 招标授权审批 | 否 | 否 | 是 | 否 | 否 | 否 |
| 送货审批 | 否 | 否 | 否 | 否 | 是 | 否 |
| 录入跟进 | 是 | 否 | 否 | 否 | 否 | 否 |
| 结束分类 | 是 | 否 | 否 | 否 | 否 | 否 |
| 查看全部 | 否 | 否 | 否 | 否 | 否 | 是 |
| 查看本人/下属 | 是 | 是 | 是 | 是 | 是 | 是 |

### 2.3 数据可见性

| 角色 | 可见范围 |
|---|---|
| 报单人 | 本人报备的全部意向 |
| 上级领导 | 下属报单人提交的意向 |
| 厂家经理/厂家负责人 | 需要其审批或裁决的意向 |
| 送货审批人 | 已中标待送货审批的意向 |
| 总经理/销售总监 | 权限范围内全部意向（按数据切片） |

### 2.4 字段权限

详见「八、核心字段规范」中「读写权限」列。

---

## 三、核心业务流程

### 3.1 意向主状态机

| 状态编码 | 状态名称 | 可执行操作 | 下一状态 | 触发条件 |
|---|---|---|---|---|
| 0 | 草稿 | 编辑、删除、提交 | 1 | 点击「提交」按钮 |
| 1 | 审批中 | 审批通过、驳回、撤回 | 2/4 | 上级领导点击通过/驳回 |
| 2 | 生效中 | 录入跟进、结束 | 5 | 完成所有审批节点 |
| 3 | 冲突无效 | 查看 | - | 冲突审批后原冲突意向被置为无效 |
| 4 | 被驳回 | 编辑、重新提交 | 1 | 报单人修改后重新提交 |
| 5 | 已结束 | 查看、导出 | - | 流程闭环 |

### 3.2 业务子状态（阶段）

| 阶段编码 | 阶段名称 | 所属主状态 | 说明 |
|---|---|---|---|
| DRAFT | 草稿 | 0 | 未提交 |
| LEADER_REVIEW | 上级初审 | 1 | 等待上级领导审批 |
| CONFLICT_PENDING | 冲突待判定 | 1 | 存在撞单，等待厂家经理+厂家负责人并行裁决 |
| FOLLOW_UP | 意向跟进 | 2 | 已通过，进入统一主干跟进 |
| BID_PUBLISHED | 招标挂网 | 2 | 招标项目已挂网 |
| BID_AUTHORIZE | 招标授权 | 2 | 招标项目等待厂家经理授权审批 |
| BID_RESULT | 招标结果 | 2 | 等待录入中标/未中标结果 |
| DELIVERY_REVIEW | 送货审批 | 2 | 中标后等待送货审批 |
| TP_FOLLOW_UP | TP/捐赠跟进 | 2 | TP/捐赠项目等待录入装机状态 |
| END_CATEGORY | 结束分类 | 2 | 等待选择正常结束/否单/其他 |
| ENDED | 已结束 | 5 | 流程闭环 |
| REJECTED | 被驳回 | 4 | 被退回，可重新提交 |

### 3.3 状态标签颜色

| 状态 | 颜色变量 | 色值 |
|---|---|---|
| 草稿 | --badge-gray | #94a3b8 |
| 审批中 | --badge-orange | #f59e0b |
| 生效中 | --badge-green | #22c55e |
| 已结束 | --badge-dark | #64748b |
| 被驳回 | --badge-red | #ef4444 |
| 冲突待判定 | --badge-red | #ef4444 |
| 冲突无效 | --badge-gray | #94a3b8 |

### 3.4 流程一：无冲突提交流程

#### 流程目标

报单人提交意向，经上级领导初审通过后进入生效跟进阶段。

#### 参与角色

报单人、上级领导。

#### 流程图

```
报单人提交意向
    │
    v
系统校验：无同客户同项目有效意向
    │
    v
上级领导初审
    │
    ├── 驳回 --> 退回报单人 --> 报单人修改后重新提交
    │
    └── 通过 --> 状态变为「生效中」--> 进入统一主干：意向跟进
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 报单人 | 点击「新建意向」 | 校验今日新建上限 | 打开新建弹窗 |
| 2 | 报单人 | 填写客户、项目、类型、金额 | 校验必填项、格式、客户是否存在 | 表单高亮错误 |
| 3 | 系统 | 点击「下一步」时自动查重 | 校验是否存在冲突意向 | 无冲突进入步骤 4，有冲突进入冲突流程 |
| 4 | 上级领导 | 初审通过/驳回 | 校验审批权限、审批意见是否填写 | 状态变为「生效中」或「被驳回」 |
| 5 | 系统 | 审批通过后计算保护期 | 校验是否配置保护期规则 | 生成保护期截止时间 |

#### 异常分支表

| 异常场景 | 触发条件 | 处理方式 | 页面反馈 |
|---|---|---|---|
| 提交时存在冲突意向 | 同客户同项目存在未结束生效意向 | 弹窗提示冲突，可选择查看原意向或走冲突审批 | 冲突检测弹窗 |
| 审批人缺失 | 上级领导未配置或离职 | 提交时校验审批链 | XqToast 提示「请联系管理员配置上级领导」 |
| 必填项未填写 | 客户、项目、业务类型等必填项为空 | 前端表单校验拦截 | 字段边框变红，显示「此项为必填项」 |
| 重复提交 | 用户连续点击提交按钮 | 按钮提交后立即置为 loading | 按钮禁用，防止重复调用接口 |

### 3.5 流程二：冲突处理流程

#### 流程目标

当检测到撞单时，通过厂家经理和厂家负责人并行裁决，确定意向归属。

#### 参与角色

报单人、厂家经理、厂家负责人。

#### 流程图

```
报单人提交意向
    │
    v
系统校验：存在同客户同项目有效意向
    │
    v
弹窗提示冲突
    │
    ├── 查看原意向 --> 打开原意向详情
    │
    └── 继续提交 --> 状态变为「冲突待判定」
                            │
                            v
            厂家经理 + 厂家负责人并行审批
                            │
            ┌───────────────┴───────────────┐
            │                               │
        两人都通过                       任一驳回
            │                               │
            v                               v
    当前意向生效                当前意向结束
    原意向置为「冲突无效」        退回报单人
            │                               │
            v                               v
    进入意向跟进                    流程终止
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 报单人 | 提交意向 | 系统检测到冲突 | 弹出冲突检测弹窗 |
| 2 | 报单人 | 选择「继续提交，走冲突审批」 | 校验用户是否有冲突提交权限 | 状态变为「冲突待判定」 |
| 3 | 厂家经理 | 审批通过/驳回 | 校验审批权限、是否填写意见 | 记录审批意见 |
| 4 | 厂家负责人 | 审批通过/驳回 | 校验审批权限、是否填写意见 | 记录审批意见 |
| 5 | 系统 | 汇总双人审批结果 | 两人都通过则当前生效，任一驳回则当前结束 | 状态变更、消息通知 |

#### 异常分支表

| 异常场景 | 触发条件 | 处理方式 | 页面反馈 |
|---|---|---|---|
| 其中一人已审批，另一人未审批 | 厂家经理已审，厂家负责人未审 | 等待另一人审批 | 详情页显示「等待某某裁决」 |
| 两人审批结果冲突 | 一人通过、一人驳回 | 按「任一驳回即结束」规则处理 | 当前意向结束，原意向保持生效 |
| 未填写裁决意见 | 点击通过/驳回时意见为空 | 前端校验拦截 | XqToast 提示「请填写裁决意见」 |

### 3.6 流程三：招标项目完整流程

#### 流程目标

招标项目从生效跟进到挂网、授权、结果、送货审批直至结束。

#### 参与角色

报单人、厂家经理、送货审批人。

#### 流程图

```
生效中（意向跟进）
    │
    v
挂网（报单人录入挂网信息）
    │
    v
授权申请 --> 厂家经理审批
    │
    ├── 驳回 --> 退回报单人
    │
    └── 通过 --> 进入招标结果判断
                    │
        ┌───────────┴───────────┐
        │                       │
    未中标                     中标
        │                       │
        v                       v
    结束分类              送货审批人审批
                                │
                        ┌───────┴───────┐
                        │               │
                    驳回            通过
                        │               │
                        v               v
                    退回报单人       结束分类
                                        │
                                        v
                                    已结束
```

#### 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 报单人 | 录入挂网信息（挂网时间、招标公司、招标编号） | 校验必填项 | 阶段变为「招标挂网」 |
| 2 | 报单人 | 发起授权申请 | 校验是否已挂网 | 推送厂家经理审批 |
| 3 | 厂家经理 | 授权审批通过/驳回 | 校验审批权限、意见 | 阶段变为「招标结果」或退回 |
| 4 | 报单人 | 录入招标结果（中标/未中标） | 校验是否已通过授权 | 未中标进入结束分类，中标进入送货审批 |
| 5 | 送货审批人 | 送货审批通过/驳回 | 校验审批权限、意见 | 通过后进入结束分类 |
| 6 | 报单人 | 结束分类 | 校验是否填写结束类型 | 状态变为「已结束」 |

### 3.7 流程四：非招标常规流程

```
生效中（意向跟进）
    │
    v
报单人录入常规跟进（说明、附件）
    │
    v
达到结束条件 --> 结束分类
    │
    v
已结束
```

### 3.8 流程五：TP/捐赠流程

```
生效中（意向跟进）
    │
    v
报单人录入 TP/捐赠信息（仪器型号、协议编号、预估消耗）
    │
    v
是否已装机？
    │
    ├── 是 --> 结束分类 --> 已结束
    │
    └── 否 --> 强制填写未装机原因 --> 结束分类 --> 已结束
```

### 3.9 流程六：驳回后重新提交流程

```
审批节点驳回
    │
    v
单据退回报单人，状态变为「被驳回」
    │
    v
报单人查看驳回人、驳回时间、驳回意见
    │
    v
点击「重新提交」--> 打开编辑弹窗，预填原单所有字段
    │
    v
修改后重新提交 --> 重新走完整审批链路
```

---

## 四、页面清单与路由

### 4.1 核心功能清单

| 功能编码 | 功能名称 | 优先级 | 功能说明 | 原型对应 |
|---|---|---|---|---|
| F_INTENTION_001 | 新建意向 | 必要 | 报单人按三步流程填写客户、项目、业务信息并提交审批 | 新建/编辑意向弹窗 |
| F_INTENTION_002 | 保存草稿 | 必要 | 新建过程中可随时保存为草稿，后续继续编辑 | 新建意向弹窗-保存草稿 |
| F_INTENTION_003 | 冲突检测 | 必要 | 提交时自动检测同客户同项目有效意向，提示冲突 | 冲突检测弹窗 |
| F_INTENTION_004 | 冲突裁决 | 必要 | 厂家经理与厂家负责人并行裁决撞单意向 | 冲突裁决弹窗 |
| F_INTENTION_005 | 意向审批 | 必要 | 上级领导/厂家经理/送货审批人按节点审批 | 审批弹窗 |
| F_INTENTION_006 | 录入跟进 | 必要 | 生效中意向按业务类型录入招标/TP/常规跟进 | 录入跟进弹窗 |
| F_INTENTION_007 | 结束分类 | 必要 | 项目到达终点时选择正常结束/否单/其他 | 结束分类弹窗 |
| F_INTENTION_008 | 意向详情查看 | 必要 | 查看意向基础信息、附件、审批时间线、当前待办 | 详情弹窗 |
| F_INTENTION_009 | 列表搜索筛选 | 重要 | 按关键词、业务类型、时间范围、状态筛选意向 | 意向列表页筛选区 |
| F_INTENTION_010 | 视图切换 | 重要 | 列表视图与卡片视图切换，适配不同数据量 | 意向列表页视图切换 |
| F_INTENTION_011 | 统计看板 | 重要 | 顶部展示本月意向数、生效中、待审批等核心指标 | 意向列表页统计卡片 |
| F_INTENTION_012 | 重新提交 | 重要 | 被驳回意向修改后重新提交审批 | 新建/编辑意向弹窗 |

### 4.2 功能优先级说明

| 优先级 | 说明 |
|---|---|
| 必要 | 模块核心功能，必须实现 |
| 重要 | 高频使用功能，建议首期实现 |
| 可选 | 增强体验功能，可后续迭代 |

---

### 4.3 页面清单

| 页面编码 | 页面名称 | 页面类型 | 路由路径 | 说明 |
|---|---|---|---|---|
| INTENTION_LIST | 意向列表页 | 列表页 | /intention/list | 意向管理主入口 |
| INTENTION_DETAIL | 意向详情弹窗 | 弹窗 | - | 点击卡片/行打开，含基础信息、附件、时间线、操作 |
| INTENTION_CREATE | 新建/编辑意向弹窗 | 分步弹窗 | - | 三步：客户项目 / 业务信息 / 确认提交 |
| INTENTION_CONFLICT | 冲突检测弹窗 | 确认弹窗 | - | 检测到冲突时提示 |
| INTENTION_CONFLICT_RESOLVE | 冲突裁决弹窗 | 审批弹窗 | - | 厂家经理/负责人并行裁决 |
| INTENTION_APPROVE | 审批弹窗 | 审批弹窗 | - | 上级领导/厂家经理/送货审批人审批 |
| INTENTION_FOLLOW | 录入跟进弹窗 | 表单弹窗 | - | 根据业务类型显示不同表单 |
| INTENTION_END | 结束分类弹窗 | 表单弹窗 | - | 选择结束类型和原因 |

### 4.4 页面入口

| 入口 | 路径 |
|---|---|
| 应用中心 | 应用中心 -> 业务管理 -> 意向管理 |
| 工作台快捷入口 | 工作台 -> 待审批 / 我的意向 |
| 消息通知 | 点击审批提醒消息跳转详情 |
| 客户 360° | 客户详情页 -> 意向记录 -> 查看 |

---

## 五、页面结构与布局

### 5.1 意向列表页

#### 5.1.1 页面布局（PC 端）

```
┌─────────────────────────────────────────────────────────────┐
│ 意向管理                                          [+ 新建意向] │
├─────────────────────────────────────────────────────────────┤
│ 本月意向数 │ 生效中 │ 待审批 │ 冲突待裁决 │ 本月成交率         │
├─────────────────────────────────────────────────────────────┤
│ [全部][草稿][审批中][生效中][已结束][被驳回]  [列表/卡片]      │
├─────────────────────────────────────────────────────────────┤
│ 搜索客户/项目    筛选业务类型    筛选时间范围                  │
├─────────────────────────────────────────────────────────────┤
│ 项目 │ 客户 │ 类型 │ 金额 │ 当前阶段 │ 保护期 │ 负责人 │ 状态 │ 操作 │
│ 或：4 列卡片网格                                             │
├─────────────────────────────────────────────────────────────┤
│ 分页组件                                                      │
└─────────────────────────────────────────────────────────────┘
```

#### 5.1.2 组件清单

| 组件名 | 位置 | 用途 | Props |
|---|---|---|---|
| XqPageLayout | 页面根容器 | 统一五层结构容器 | title="意向管理", showBack=false, showStats=true, showFilter=true |
| XqKpiCard | 统计卡片区 | 展示 5 个核心指标 | title, value, color, clickable |
| XqNavTabs | 操作区左侧 | 按流程状态切换 | tabs=[{key, label, count}], type='card', showCount=true |
| XqViewSwitch | 操作区中部 | 列表/卡片视图切换 | value, options=[{label, value, icon}] |
| a-button | 操作区右侧 | 「+ 新建意向」主操作 | type='primary', icon='XqIconPlus' |
| XqSearchBar | 筛选栏左侧 | 关键词搜索 | placeholder='搜索客户、项目、负责人、业务类型...', pinyinSearch=true, width='320px' |
| XqFilterBar | 筛选栏 | 业务类型、时间范围筛选 | filters, values, showReset=true, maxVisible=5 |
| XqCardGrid | 内容区 | 卡片视图 | dataSource, columns=4, gap='16px', renderItem |
| XqDataTable | 内容区 | 列表视图 | columns, dataSource, loading, pagination, rowKey='intentionId' |
| XqStatusBadge | 卡片/表格内 | 状态、阶段、业务类型标签 | status, statusMap, size |
| XqEmptyState | 无数据时 | 空态提示 | type='empty', title, description, action |
| a-pagination | 页面底部 | 分页 | current, pageSize, total, showSizeChanger=false |

#### 5.1.3 统计卡片

| 卡片编码 | 中文名称 | 数据来源 | 颜色 |
|---|---|---|---|
| monthIntentionCount | 本月意向数 | 本月新建报备数量 | 默认 |
| activeCount | 生效中 | 状态为「生效中」的数量 | blue |
| pendingCount | 待审批 | 状态为「审批中」的数量 | orange |
| conflictCount | 冲突待裁决 | 阶段为「冲突待判定」的数量 | red |
| monthWinRate | 本月成交率 | 本月正常结束数 / 本月结束总数 | green |

#### 5.1.4 Tab 分组

| Tab 编码 | Tab 名称 | 过滤条件 |
|---|---|---|
| all | 全部 | 无 |
| draft | 草稿 | status = 0 |
| pending | 审批中 | status = 1 |
| active | 生效中 | status = 2 |
| ended | 已结束 | status = 5 |
| rejected | 被驳回 | status = 4 |

#### 5.1.5 列表视图列配置

| 列编码 | 列标题 | 字段编码 | 宽度 | 固定 | 渲染方式 |
|---|---|---|---|---|---|
| intentionName | 项目 | intentionName | 200px | left | XqText，可点击跳转详情 |
| customerName | 客户 | customerName | 160px | - | XqText |
| businessType | 类型 | businessType | 120px | - | XqStatusBadge |
| amount | 金额 | amount | 100px | - | XqMoney |
| currentStage | 当前阶段 | currentStage | 120px | - | XqStatusBadge |
| protectDeadline | 保护期 | protectDeadline | 100px | - | XqCountdown |
| ownerName | 负责人 | ownerName | 100px | - | XqUser |
| status | 状态 | status | 100px | - | XqStatusBadge |
| action | 操作 | - | 120px | right | 文本按钮 |

#### 5.1.6 卡片视图字段

| 字段顺序 | 字段编码 | 位置 |
|---|---|---|
| 1 | intentionName | 顶部标题 |
| 2 | status | 顶部右上角标签 |
| 3 | customerName | 主体 |
| 4 | businessType | 中部标签区 |
| 5 | amount | 中部标签区 |
| 6 | currentStage | 底部左卡片 |
| 7 | protectDeadline | 底部右卡片 |
| 8 | ownerName | 底部左侧 |
| 9 | action | 底部右侧 |

#### 5.1.7 分页规则

- 卡片视图每页 8 条，列表视图每页 12 条。
- 底部分页组件显示：左侧「1 / 5 页，共 56 条」，右侧「上一页」 [1] [2] [3] ... [5] 「下一页」。
- 切换视图、切换 Tab、执行筛选后，自动重置到第 1 页。

### 5.2 新建/编辑意向弹窗

#### 5.2.1 触发方式

- 列表页点击「+ 新建意向」
- 被驳回行点击「重新提交」
- 草稿行点击「编辑」

#### 5.2.2 弹窗标题

| 场景 | 标题 |
|---|---|
| 新建 | 新建意向 |
| 重新提交 | 重新提交意向 |
| 编辑草稿 | 编辑意向 |

#### 5.2.3 组件

| 组件名 | 用途 | Props |
|---|---|---|
| XqFormDrawer | 表单抽屉容器 | title, width='720px', showFooter=true |
| a-steps | 步骤条 | current, items=[{title, description}] |
| XqSelect | 客户选择 | showSearch=true, remote=true |
| a-input | 文本输入 | allowClear=true |
| a-input-number | 金额、数量 | min=0, precision=2 |
| a-radio-group | 业务类型、TP/捐赠 | options |
| a-date-picker | 预计采购时间 | picker='month' |
| a-checkbox-group | 关键决策人 | options |
| XqTextarea | 竞品情况/备注 | rows=2 |

#### 5.2.4 步骤一：客户与项目

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| customerId | 选择客户 | Long | 是 | 关联客户主数据 | 无 | 关联客户 | 报单人可编辑 | XqSelect，支持搜索医院名称/客户编码 |
| intentionName | 项目名称 | String | 是 | 最长 100 字符 | 无 | 手工录入 | 报单人可编辑 | a-input |
| brandId | 关联品牌 | Long | 否 | 关联品牌字典 | 无 | 字典 | 报单人可编辑 | XqSelect |
| productModelId | 产品型号 | Long | 否 | 关联产品型号 | 无 | 字典联动 | 报单人可编辑 | XqSelect，根据 brandId 联动 |
| amount | 预计金额 | Money | 否 | 18,2，单位元 | 0 | 手工录入 | 报单人可编辑 | a-input-number |
| quantity | 预计数量 | Integer | 否 | 最小 1 | 1 | 手工录入 | 报单人可编辑 | a-input-number |

#### 5.2.5 步骤二：业务信息

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| businessType | 业务类型 | Enum | 是 | 1:招标, 2:非招标 | 1 | 字典 | 报单人可编辑 | a-radio-group |
| tpType | 是否 TP/捐赠 | Enum | 是 | 0:否, 1:TP/投放, 2:捐赠 | 0 | 字典 | 报单人可编辑 | a-radio-group |
| purchaseTime | 预计采购时间 | Date | 是 | yyyy-MM | 无 | 手工录入 | 报单人可编辑 | a-date-picker，月份选择 |
| decisionMakers | 关键决策人 | Array | 否 | 多选 | [] | 字典 | 报单人可编辑 | a-checkbox-group |
| remark | 竞品情况/备注 | String | 否 | 最长 500 字符 | 无 | 手工录入 | 报单人可编辑 | XqTextarea |

#### 5.2.6 步骤三：确认提交

| 组件 | 用途 |
|---|---|
| a-card | 信息确认卡，展示客户、项目、类型、TP/捐赠、预计采购时间 |
| 审批路径预览区 | 预计审批路径预览，根据 businessType + tpType 动态生成 |
| a-button | 「保存草稿」「确认提交」 |

#### 5.2.7 交互规则

- 点击步骤一「下一步」时调用冲突校验接口 checkConflict。
- 若存在冲突，弹出冲突检测弹窗 XqConfirmDialog。
- 用户可选择「查看原意向」或「继续提交，走冲突审批」。
- 选择 TP/捐赠时显示提示文案：「TP = 免费给设备、靠试剂赚钱；捐赠 = 公益赠机、禁止绑定试剂」。

### 5.3 冲突检测弹窗

#### 5.3.1 触发条件

提交意向时，系统检测到同客户同项目存在未结束生效意向。

#### 5.3.2 组件

| 组件名 | 用途 |
|---|---|
| XqConfirmDialog | 冲突提示弹窗，dangerous=false |
| a-card | 冲突信息展示 |

#### 5.3.3 字段

| 字段编码 | 中文名称 | 说明 |
|---|---|---|
| conflictCustomerName | 客户 | 当前报备客户名称 |
| conflictIntentionName | 项目 | 当前报备项目名称 |
| originalReporterName | 原报备人 | 已存在意向的报单人 |
| originalSubmitTime | 原报备时间 | 已存在意向的提交时间 |
| remainingDays | 保护期剩余 | 已存在意向的剩余保护期 |

#### 5.3.4 操作按钮

| 按钮 | 操作 |
|---|---|
| 查看原意向 | 打开原意向详情弹窗 |
| 继续提交，走冲突审批 | 进入步骤二/三，提交后状态为「冲突待判定」 |

### 5.4 详情弹窗

#### 5.4.1 触发方式

点击列表行或卡片进入详情弹窗。

#### 5.4.2 组件

| 组件名 | 用途 | Props |
|---|---|---|
| XqDrawer | 详情抽屉容器 | width='720px', maskClosable=true, showFooter=true |
| a-progress | 顶部进度条 | steps, current |
| a-card | 基础信息 | title='基础信息' |
| 附件列表 | 附件列表 | files |
| XqApprovalPanel | 当前待办操作区 | record, actions, requireComment=true |
| XqTimeline | 审批时间线 | data, mode='left' |

#### 5.4.3 布局

```
┌─────────────────────────────────────────────────────────────┐
│ 项目名称                                [X]                   │
│ 客户 · 负责人 · 提交时间                                       │
├─────────────────────────────────────────────────────────────┤
│ 提交 -> 初审 -> 跟进 -> 授权 -> 结果 -> 送货 -> 结束（进度条）     │
├───────────────────────────────┬─────────────────────────────┤
│ 基础信息                       │ 当前待办                     │
│ 客户/项目/类型/TP/金额/时间    │ 待办说明                     │
│ 关键决策人/负责人              │ [录入跟进] [关闭]            │
├───────────────────────────────┤─────────────────────────────┤
│ 附件与资料                     │ 审批时间线                   │
│ 文件列表                       │ 时间、操作人、动作、意见     │
└───────────────────────────────┴─────────────────────────────┘
```

#### 5.4.4 交互规则

- 不同角色看到不同的操作按钮。
- 被驳回的详情显示红色驳回标识和驳回意见。
- 时间线按时间倒序排列。
- 项目/客户名称可点击跳转客户 360° 详情。

### 5.5 审批弹窗

#### 5.5.1 适用场景

- 上级领导初审
- 厂家经理招标授权审批
- 送货审批人送货审批

#### 5.5.2 组件

| 组件名 | 用途 |
|---|---|
| XqFormDrawer | 审批弹窗容器 |
| a-card | 申请信息只读展示 |
| XqTextarea | 审批意见输入 |
| a-button | 通过/驳回按钮 |

#### 5.5.3 字段

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| reporterName | 申请人 | String | 是 | - | 无 | 系统生成 | 只读 | XqText |
| submitTime | 申请时间 | DateTime | 是 | yyyy-MM-dd HH:mm:ss | 无 | 系统生成 | 只读 | XqDate |
| approvalItem | 审批事项 | String | 是 | - | 无 | 系统生成 | 只读 | XqText |
| amount | 金额 | Money | 否 | 18,2 | 0 | 系统生成 | 只读 | XqMoney |
| approvalComment | 审批意见 | String | 是 | 最长 500 字符，至少 3 个字 | 无 | 手工录入 | 审批人可编辑 | XqTextarea |

#### 5.5.4 操作按钮

| 按钮 | 操作 |
|---|---|
| 驳回 | 流程终止，单据退回报单人 |
| 通过 | 进入下一节点 |

#### 5.5.5 交互规则

- 审批意见未填写或少于 3 个字时，点击按钮提示「请填写审批意见」。
- 驳回时强制要求详细理由。
- 通过后记录审批日志并推送消息给报单人。

### 5.6 冲突裁决弹窗

#### 5.6.1 适用场景

存在撞单时，厂家经理和厂家负责人并行裁决。

#### 5.6.2 组件

| 组件名 | 用途 |
|---|---|
| XqDrawer | 裁决弹窗容器 |
| XqIntentionCard | 冲突双方对比卡片 |
| a-alert | 系统建议 |
| XqTextarea | 裁决意见输入 |
| a-button | 通过/驳回按钮 |

#### 5.6.3 布局

```
┌─────────────────────────────────────────────────────────────┐
│ 冲突裁决                                    [X]               │
├─────────────────────────────────────────────────────────────┤
│ ┌───────────────┐  ┌───────────────┐                        │
│ │ 当前申请      │  │ 原生效意向    │                        │
│ │ 项目/报备人   │  │ 项目/报备人   │                        │
│ │ 时间/进展     │  │ 时间/进展     │                        │
│ └───────────────┘  └───────────────┘                        │
├─────────────────────────────────────────────────────────────┤
│ 系统建议：原意向已进入挂网阶段，建议维持原意向。             │
├─────────────────────────────────────────────────────────────┤
│ 裁决意见 *                                                   │
│ [                                                          ] │
├─────────────────────────────────────────────────────────────┤
│ [驳回当前申请]              [通过当前申请]                   │
└─────────────────────────────────────────────────────────────┘
```

#### 5.6.4 操作按钮

| 按钮 | 操作 |
|---|---|
| 驳回当前申请 | 当前意向结束 |
| 通过当前申请 | 当前意向生效，原意向置为「冲突无效」 |

#### 5.6.5 交互规则

- 必须填写裁决意见。
- 厂家经理和厂家负责人均通过后，系统才判定生效。
- 任一驳回，当前意向结束。

### 5.7 录入跟进弹窗

#### 5.7.1 触发方式

生效中状态的意向点击「跟进」。

#### 5.7.2 组件

| 组件名 | 用途 |
|---|---|
| XqFormDrawer | 跟进弹窗容器 |
| a-date-picker | 日期选择 |
| a-input | 文本输入 |
| XqTextarea | 多行文本 |
| XqUpload | 文件上传 |
| a-radio-group | 单选 |
| a-button | 发起授权申请 |

#### 5.7.3 招标项目跟进字段

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| publishDate | 挂网时间 | Date | 是 | yyyy-MM-dd | 无 | 手工录入 | 报单人可编辑 | a-date-picker |
| biddingCompany | 招标公司 | String | 是 | 最长 100 字符 | 无 | 手工录入 | 报单人可编辑 | a-input |
| biddingCode | 招标编号 | String | 是 | 最长 50 字符 | 无 | 手工录入 | 报单人可编辑 | a-input |
| biddingFiles | 招标文件 | Array | 否 | PDF/图片 | [] | 手工上传 | 报单人可编辑 | XqUpload |
| authorizeApply | 授权申请 | Boolean | 否 | - | false | 手工触发 | 报单人可点击 | a-button |
| bidResult | 招标结果 | Enum | 否 | 1:中标, 2:未中标 | 无 | 手工录入 | 报单人可编辑 | a-radio-group |

#### 5.7.4 TP/捐赠跟进字段

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| deviceModel | 仪器型号 | String | 是 | 最长 50 字符 | 无 | 手工录入 | 报单人可编辑 | a-input |
| monthlyReagentVolume | 预估月试剂消耗量 | Number | 是 | 最小 0 | 0 | 手工录入 | 报单人可编辑 | a-input-number |
| agreementCode | 投放/捐赠协议编号 | String | 是 | 最长 50 字符 | 无 | 手工录入 | 报单人可编辑 | a-input |
| agreementFiles | 协议扫描件 | Array | 是 | PDF/图片 | [] | 手工上传 | 报单人可编辑 | XqUpload |
| installedFlag | 是否已装机 | Enum | 是 | 0:未装机, 1:已装机 | 无 | 手工录入 | 报单人可编辑 | a-radio-group |
| noInstallReasonType | 未装机原因类型 | Enum | 条件必填 | 1:院方搁置, 2:场地不足, 3:预算变更, 4:其他 | 无 | 字典 | 报单人可编辑 | XqSelect |
| noInstallReason | 未装机详细说明 | String | 条件必填 | 最长 200 字符 | 无 | 手工录入 | 报单人可编辑 | XqTextarea |

#### 5.7.5 非招标常规跟进字段

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| followContent | 跟进说明 | String | 是 | 最长 500 字符 | 无 | 手工录入 | 报单人可编辑 | XqTextarea |
| followAttachments | 附件 | Array | 否 | 任意文件 | [] | 手工上传 | 报单人可编辑 | XqUpload |

#### 5.7.6 交互规则

- 招标项目选择「未中标」后直接进入结束分类。
- 招标项目选择「中标」后进入送货审批流程。
- 招标项目未授权时禁用「招标结果」选择，提示「请先完成授权申请并通过审批」。
- TP/捐赠选择「已装机」后直接结束分类。
- TP/捐赠选择「未装机」必须填写原因类型和详细说明。

### 5.8 结束分类弹窗

#### 5.8.1 触发条件

- 招标项目未中标
- 招标项目中标并完成送货审批
- TP/捐赠已装机或未装机填写原因后
- 非招标项目完成所有必要跟进后

#### 5.8.2 组件

| 组件名 | 用途 |
|---|---|
| XqFormDrawer | 结束分类弹窗容器 |
| a-radio-group | 结束类型选择 |
| XqTextarea | 备注输入 |

#### 5.8.3 字段

| 字段编码 | 中文名称 | 数据类型 | 是否必填 | 长度/格式约束 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| endType | 结束类型 | Enum | 是 | 1:正常结束, 2:否单, 3:其他 | 无 | 字典 | 报单人可编辑 | a-radio-group |
| endRemark | 备注/丢单原因 | String | 条件必填 | 最长 500 字符 | 无 | 手工录入 | 报单人可编辑 | XqTextarea |

#### 5.8.4 交互规则

- 选择「正常结束」备注可空。
- 选择「否单」或「其他」必须填写备注。
- 结束后状态变为「已结束」，不可再编辑。

---

## 六、核心字段规范

### 6.1 意向主表字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| intentionId | 意向 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| intentionCode | 意向编号 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见 | 列表/详情 |
| intentionName | 项目名称 | String | 100 | 是 | 无 | 手工录入 | - | 全员可见 | 列表/详情/表单 |
| customerId | 客户 ID | Long | - | 是 | 无 | 关联客户 | - | 全员可见 | 表单下拉 |
| customerName | 客户名称 | String | 100 | 是 | 无 | 关联客户 | - | 全员可见 | 列表/详情 |
| brandId | 关联品牌 ID | Long | - | 否 | 无 | 字典 | - | 全员可见 | 表单下拉 |
| brandName | 关联品牌名称 | String | 50 | 否 | 无 | 字典 | - | 全员可见 | 详情 |
| productModelId | 产品型号 ID | Long | - | 否 | 无 | 字典联动 | - | 全员可见 | 表单下拉 |
| productModelName | 产品型号名称 | String | 50 | 否 | 无 | 字典联动 | - | 全员可见 | 详情 |
| businessType | 业务类型 | Enum | - | 是 | 1 | 字典 | 1:招标, 2:非招标 | 全员可见 | 状态标签 |
| tpType | 是否 TP/捐赠 | Enum | - | 是 | 0 | 字典 | 0:否, 1:TP/投放, 2:捐赠 | 全员可见 | 状态标签 |
| amount | 预计金额 | Money | 18,2 | 否 | 0 | 手工录入 | - | 全员可见 | 列表/详情 |
| quantity | 预计数量 | Integer | - | 否 | 1 | 手工录入 | - | 详情/表单 | 数字输入 |
| purchaseTime | 预计采购时间 | Date | yyyy-MM | 是 | 无 | 手工录入 | - | 全员可见 | 列表/详情/表单 |
| decisionMakers | 关键决策人 | Array | - | 否 | [] | 字典 | 1:检验科主任, 2:设备科, 3:分管院长, 4:财务科 | 全员可见 | 详情 |
| status | 意向状态 | Enum | - | 是 | 0 | 系统生成 | 0:草稿, 1:审批中, 2:生效中, 3:冲突无效, 4:被驳回, 5:已结束 | 全员可见 | 状态标签 |
| currentStage | 当前阶段 | Enum | - | 是 | DRAFT | 系统生成 | 见 3.2 业务子状态 | 全员可见 | 状态标签 |
| protectDeadline | 保护期截止 | Date | yyyy-MM-dd | 否 | 无 | 计算 | - | 全员可见 | 倒计时 |
| reporterId | 报单人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 只读 | 不显示 |
| reporterName | 报单人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | - | 全员可见 | 列表/详情 |
| ownerId | 负责人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 全员可见 | 不显示 |
| ownerName | 负责人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | - | 全员可见 | 列表/详情 |
| submitTime | 提交时间 | DateTime | yyyy-MM-dd HH:mm:ss | 否 | 无 | 系统生成 | - | 全员可见 | 详情 |
| endType | 结束类型 | Enum | - | 否 | 无 | 手工录入 | 1:正常结束, 2:否单, 3:其他 | 全员可见 | 状态标签 |
| endRemark | 结束备注 | String | 500 | 否 | 无 | 手工录入 | - | 全员可见 | 详情 |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情 |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情 |
| createBy | 创建人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 只读 | 不显示 |
| updateBy | 更新人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 只读 | 不显示 |
| tenantId | 租户 ID | Integer | - | 是 | 当前租户 | 系统生成 | - | 不可见 | 不显示 |
| orgId | 组织 ID | Integer | - | 是 | 当前组织 | 系统生成 | - | 不可见 | 不显示 |
| deleted | 删除标记 | Boolean | - | 是 | false | 系统生成 | - | 不可见 | 不显示 |

### 6.2 意向跟进记录字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| followId | 跟进记录 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| intentionId | 意向 ID | Long | - | 是 | 无 | 关联意向 | - | 只读 | 不显示 |
| followType | 跟进类型 | Enum | - | 是 | 无 | 系统生成 | 1:招标跟进, 2:TP/捐赠跟进, 3:常规跟进 | 全员可见 | 状态标签 |
| publishDate | 挂网时间 | Date | yyyy-MM-dd | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| biddingCompany | 招标公司 | String | 100 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| biddingCode | 招标编号 | String | 50 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| biddingFiles | 招标文件 | Array | - | 否 | [] | 手工上传 | - | 全员可见 | 文件列表 |
| bidResult | 招标结果 | Enum | - | 否 | 无 | 手工录入 | 1:中标, 2:未中标 | 全员可见 | 状态标签 |
| deviceModel | 仪器型号 | String | 50 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| monthlyReagentVolume | 预估月试剂消耗量 | Number | - | 条件必填 | 0 | 手工录入 | - | 全员可见 | 详情 |
| agreementCode | 协议编号 | String | 50 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| agreementFiles | 协议扫描件 | Array | - | 条件必填 | [] | 手工上传 | - | 全员可见 | 文件列表 |
| installedFlag | 是否已装机 | Enum | - | 条件必填 | 无 | 手工录入 | 0:未装机, 1:已装机 | 全员可见 | 状态标签 |
| noInstallReasonType | 未装机原因类型 | Enum | - | 条件必填 | 无 | 字典 | 1:院方搁置, 2:场地不足, 3:预算变更, 4:其他 | 全员可见 | 详情 |
| noInstallReason | 未装机详细说明 | String | 200 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| followContent | 跟进说明 | String | 500 | 条件必填 | 无 | 手工录入 | - | 全员可见 | 详情 |
| followAttachments | 跟进附件 | Array | - | 否 | [] | 手工上传 | - | 全员可见 | 文件列表 |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 只读 | 详情 |
| createByName | 创建人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | - | 只读 | 详情 |

### 6.3 审批记录字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| approvalId | 审批记录 ID | Long | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| intentionId | 意向 ID | Long | - | 是 | 无 | 关联意向 | - | 只读 | 不显示 |
| approvalNode | 审批节点 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见 | 详情 |
| approverId | 审批人 ID | Integer | - | 是 | 无 | 系统生成 | - | 只读 | 不显示 |
| approverName | 审批人姓名 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见 | 详情 |
| approvalAction | 审批动作 | Enum | - | 是 | 无 | 系统生成 | 1:通过, 2:驳回, 3:转交 | 全员可见 | 状态标签 |
| approvalComment | 审批意见 | String | 500 | 是 | 无 | 手工录入 | - | 全员可见 | 详情 |
| approvalTime | 审批时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 无 | 系统生成 | - | 只读 | 详情 |

### 6.4 枚举字段完整定义

#### 6.4.1 业务类型 businessType

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 1 | 招标 | --badge-blue | 公开/院内招标 |
| 2 | 非招标 | --badge-gray | 常规销售 |

#### 6.4.2 TP/捐赠类型 tpType

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 否 | --badge-gray | 常规销售 |
| 1 | TP/投放 | --badge-purple | 免费给设备、靠试剂赚钱 |
| 2 | 捐赠 | --badge-purple | 公益赠机、禁止绑定试剂 |

#### 6.4.3 意向状态 status

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 草稿 | --badge-gray | 未提交 |
| 1 | 审批中 | --badge-orange | 等待审批 |
| 2 | 生效中 | --badge-green | 已通过，进入跟进 |
| 3 | 冲突无效 | --badge-gray | 冲突审批后原冲突意向被置为无效 |
| 4 | 被驳回 | --badge-red | 审批不通过 |
| 5 | 已结束 | --badge-dark | 流程闭环 |

#### 6.4.4 结束类型 endType

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 1 | 正常结束 | --badge-green | 项目成交或按预期完成闭环 |
| 2 | 否单 | --badge-red | 项目没做成 |
| 3 | 其他 | --badge-orange | 需备注说明 |

---

## 七、交互设计要点

### 7.1 搜索与筛选

| 组件 | 位置 | 规则 |
|---|---|---|
| XqSearchBar | 筛选栏最左侧 | placeholder='搜索客户、项目、负责人、业务类型...'，支持拼音首字母，debounce 300ms，allowClear=true |
| XqFilterBar | 筛选栏 | 业务类型、时间范围筛选，下拉选择后即时触发筛选，显示「重置」按钮 |

#### 联动规则

- 搜索和筛选条件可同时生效。
- 切换视图（卡片/列表）时保留搜索和筛选条件。
- 切换 Tab 时，搜索条件保留，业务类型筛选条件保留，时间范围筛选条件保留。
- 筛选后无结果时显示 XqEmptyState type='search'。

### 7.2 操作反馈

| 操作 | 成功反馈 | 失败反馈 |
|---|---|---|
| 新建意向提交 | XqToast success「提交成功，等待审批」 | XqToast error + 表单高亮错误 |
| 保存草稿 | XqToast success「草稿已保存」 | XqToast error「保存失败，请重试」 |
| 审批通过 | XqToast success「审批通过」 | XqToast error「请填写审批意见」 |
| 审批驳回 | XqToast success「已驳回」 | XqToast error「请填写驳回理由」 |
| 录入跟进 | XqToast success「跟进已保存」 | XqToast error + 表单校验提示 |
| 结束分类 | XqToast success「意向已结束」 | XqToast error + 校验提示 |
| 冲突裁决 | XqToast success「裁决已完成」 | XqToast error「请填写裁决意见」 |

### 7.3 空态设计

| 场景 | 组件 | 文案 |
|---|---|---|
| 列表无数据 | XqEmptyState type='empty' | 标题「暂无意向」，描述「点击下方按钮创建第一条意向」，操作「+ 新建意向」 |
| 搜索无结果 | XqEmptyState type='search' | 标题「未找到相关意向」，描述「请调整搜索词或筛选条件」 |
| 无权限 | XqEmptyState type='permission' | 标题「暂无权限」，描述「请联系管理员开通意向管理权限」 |
| 加载失败 | XqEmptyState type='error' | 标题「加载失败」，操作「重新加载」 |

### 7.4 快捷操作

#### 7.4.1 卡片/行操作按钮（按状态）

| 状态 | 显示按钮 | 对应操作 |
|---|---|---|
| 草稿 | 编辑、删除、提交 | openCreateModal / deleteIntention / submitIntention |
| 审批中 | 审批、查看 | openApproveModal / openDetailModal |
| 冲突待判定 | 裁决、查看 | openConflictModal / openDetailModal |
| 生效中 | 跟进、查看 | openFollowModal / openDetailModal |
| 被驳回 | 重新提交、查看 | openCreateModal / openDetailModal |
| 已结束 | 查看、导出 | openDetailModal / exportIntention |

#### 7.4.2 操作按钮事件冒泡

- 点击卡片/行进入详情弹窗。
- 点击操作按钮阻止行点击事件冒泡。

### 7.5 异常处理

| 异常场景 | 处理方式 | 页面反馈 |
|---|---|---|
| 网络中断提交失败 | 本地保存草稿，恢复网络后自动提示「有未提交的草稿，是否继续？」 | XqConfirmDialog |
| 提交时已被他人抢先报备 | 实时刷新冲突状态，提示「该项目刚刚被他人报备，请确认」 | XqToast warning |
| 审批人离职/未配置 | 提交时校验审批链，缺失时拦截 | XqToast error「请联系管理员配置上级领导」 |
| 保护期到期未汇报 | 提前 3 天、1 天、当天分别推送提醒，到期自动标记为「待终止」 | 消息通知 + 列表标记 |
| 招标项目未授权就录入结果 | 禁用「招标结果」选择，提示「请先完成授权申请并通过审批」 | a-tooltip + 禁用态 |
| TP/捐赠未填装机原因 | 表单校验不通过，红字提示「未装机必须填写原因」 | 字段边框变红 |
| 重复提交 | 按钮提交后立即置为 loading，防止重复点击 | a-button loading=true |

---

## 八、响应式双端设计

### 8.1 断点规则

| 断点名 | 宽度范围 | 设备类型 | 布局策略 |
|---|---|---|---|
| xs | < 768px | 手机 | 单列，底部固定操作，抽屉全屏 |
| sm | 768px - 1023px | 平板 | 双列或单列，侧边栏可折叠 |
| md | 1024px - 1439px | PC 标准 | 多列，左侧固定导航 |
| lg | >= 1440px | PC 宽屏 | 多列，更宽内容区 |

### 8.2 意向列表页

#### 8.2.1 PC 端布局（md/lg）

- 页面标题在顶部左侧，「+ 新建意向」按钮在顶部右侧。
- 统计卡片横向排列 5 个，均分宽度。
- Tab 标签组在操作区左侧，视图切换在右侧。
- 搜索框宽度 320px，筛选条件横向排列。
- 卡片视图 4 列（lg）/ 3 列（md）。
- 列表视图完整 9 列展示，操作列固定右侧。
- 底部分页组件完整显示。

#### 8.2.2 移动端布局（xs）

```
┌─────────────────────────────────┐
| <- 意向管理              [+]     │
├─────────────────────────────────┤
│ [搜索框 =================] [筛选] │
├─────────────────────────────────┤
│ Tab 横向滚动                    │
├─────────────────────────────────┤
│ 统计卡片横向滚动                │
├─────────────────────────────────┤
│ 意向卡片 单列                   │
│ 意向卡片 单列                   │
│ 意向卡片 单列                   │
├─────────────────────────────────┤
│ [加载更多]                      │
└─────────────────────────────────┘
```

- 标题区域固定顶部，左侧返回按钮，右侧「+」新建按钮。
- 搜索框宽度 100%，吸顶显示，筛选入口在搜索右侧。
- Tab 横向滚动，不超过屏幕宽度时居中。
- 统计卡片横向滚动，每个卡片最小宽度 120px。
- 卡片强制 1 列。
- 分页简化为「加载更多」或显示「1/10 页」。
- 手势：左滑卡片显示快捷操作（跟进/审批/分配），下拉刷新列表，上拉加载更多。

### 8.3 新建/编辑意向弹窗

#### 8.3.1 PC 端

- 使用 XqFormDrawer，宽度 720px。
- 步骤条在顶部居中。
- 表单一列或两列布局。
- 底部固定操作栏：取消、上一步、下一步/保存草稿/确认提交。

#### 8.3.2 移动端

- 弹窗变为全屏页面。
- 步骤条简化为数字步骤。
- 表单字段单列。
- 必填项用红色星号标识。
- 底部固定操作按钮，不遮挡内容。
- 键盘弹出时输入框可见。

### 8.4 详情弹窗

#### 8.4.1 PC 端

- 使用 XqDrawer，宽度 720px。
- 左侧 2/3 展示基础信息 + 附件。
- 右侧 1/3 展示当前待办 + 审批时间线。

#### 8.4.2 移动端

- 弹窗变为全屏页面。
- 顶部固定返回按钮 + 标题。
- 内容区垂直滚动。
- 底部固定主操作按钮（如「录入跟进」）。
- 时间线改为单列垂直排列。

### 8.5 审批/冲突裁决弹窗

#### 8.5.1 PC 端

- 使用 XqDrawer，宽度 560px。
- 表单字段单列。
- 底部操作按钮：驳回、通过。

#### 8.5.2 移动端

- 弹窗变为全屏页面。
- 冲突双方对比卡片上下排列。
- 底部固定操作按钮。

### 8.6 移动端手势规范

| 手势 | 触发场景 | 操作 |
|---|---|---|
| 下拉刷新 | 移动端列表 | 刷新当前列表 |
| 上拉加载 | 移动端列表 | 加载下一页 |
| 左滑卡片 | 移动端卡片 | 显示快捷操作（跟进/审批/裁决） |
| 长按 | 移动端列表项 | 弹出操作菜单 |
| 点击 | 通用 | 进入详情/触发操作 |

---

## 九、接口契约（前端视角）

### 9.1 接口清单

| 接口编码 | 接口名 | 请求方式 | 路径 | 场景 |
|---|---|---|---|---|
| INTENTION_001 | getIntentionList | POST | /api/v1/intention/list | 获取意向列表 |
| INTENTION_002 | getIntentionDetail | GET | /api/v1/intention/{intentionId} | 获取意向详情 |
| INTENTION_003 | createIntention | POST | /api/v1/intention/create | 新建意向 |
| INTENTION_004 | updateIntention | POST | /api/v1/intention/update | 更新意向 |
| INTENTION_005 | submitIntention | POST | /api/v1/intention/submit | 提交意向审批 |
| INTENTION_006 | saveIntentionDraft | POST | /api/v1/intention/draft | 保存草稿 |
| INTENTION_007 | deleteIntention | POST | /api/v1/intention/delete | 删除草稿 |
| INTENTION_008 | checkConflict | POST | /api/v1/intention/check-conflict | 冲突检测 |
| INTENTION_009 | approveIntention | POST | /api/v1/intention/approve | 审批操作 |
| INTENTION_010 | resolveConflict | POST | /api/v1/intention/resolve-conflict | 冲突裁决 |
| INTENTION_011 | followIntention | POST | /api/v1/intention/follow | 录入跟进 |
| INTENTION_012 | endIntention | POST | /api/v1/intention/end | 结束分类 |
| INTENTION_013 | getIntentionStats | GET | /api/v1/intention/stats | 获取统计指标 |
| INTENTION_014 | getApprovalHistory | GET | /api/v1/intention/{intentionId}/approval-history | 获取审批历史 |

### 9.2 获取意向列表

- **接口名**：getIntentionList
- **请求方式**：POST
- **路径**：/api/v1/intention/list
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | Number | 是 | 页码，默认 1 |
| pageSize | Number | 是 | 每页条数，卡片 8 / 列表 12 |
| keyword | String | 否 | 搜索关键词，支持客户、项目、负责人、业务类型模糊搜索 |
| status | Number | 否 | 主状态筛选：0草稿/1审批中/2生效中/4被驳回/5已结束 |
| businessType | Number | 否 | 业务类型筛选：1招标/2非招标 |
| tpType | Number | 否 | TP/捐赠筛选：0否/1TP/投放/2捐赠 |
| timeRange | String | 否 | 时间范围：week本周/month本月/quarter本季度 |
| viewType | String | 否 | 视图类型：card/list |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<Object> | 意向列表 |
| total | Number | 总条数 |
| pageNum | Number | 当前页码 |
| pageSize | Number | 每页大小 |
| stats | Object | 顶部统计指标 |

- **list 项字段**：intentionId, intentionCode, intentionName, customerId, customerName, brandName, productModelName, businessType, tpType, amount, quantity, purchaseTime, decisionMakers, status, currentStage, protectDeadline, reporterId, reporterName, ownerId, ownerName, submitTime, createTime, updateTime

### 9.3 获取意向详情

- **接口名**：getIntentionDetail
- **请求方式**：GET
- **路径**：/api/v1/intention/{intentionId}
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID，路径参数 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 意向 ID |
| intentionCode | String | 意向编号 |
| intentionName | String | 项目名称 |
| customerId | Long | 客户 ID |
| customerName | String | 客户名称 |
| brandId | Long | 品牌 ID |
| brandName | String | 品牌名称 |
| productModelId | Long | 产品型号 ID |
| productModelName | String | 产品型号名称 |
| businessType | Number | 业务类型 |
| tpType | Number | TP/捐赠类型 |
| amount | Money | 预计金额 |
| quantity | Integer | 预计数量 |
| purchaseTime | Date | 预计采购时间 |
| decisionMakers | Array | 关键决策人 |
| status | Number | 意向状态 |
| currentStage | String | 当前阶段 |
| protectDeadline | Date | 保护期截止 |
| reporterId | Integer | 报单人 ID |
| reporterName | String | 报单人姓名 |
| ownerId | Integer | 负责人 ID |
| ownerName | String | 负责人姓名 |
| submitTime | DateTime | 提交时间 |
| endType | Number | 结束类型 |
| endRemark | String | 结束备注 |
| attachments | Array | 附件列表 |
| approvalHistory | Array | 审批历史 |
| followRecords | Array | 跟进记录 |
| createTime | DateTime | 创建时间 |
| updateTime | DateTime | 更新时间 |

### 9.4 新建/更新意向

- **接口名**：createIntention / updateIntention
- **请求方式**：POST
- **路径**：/api/v1/intention/create / /api/v1/intention/update
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 条件必填 | update 时必填 |
| intentionName | String | 是 | 项目名称 |
| customerId | Long | 是 | 客户 ID |
| brandId | Long | 否 | 品牌 ID |
| productModelId | Long | 否 | 产品型号 ID |
| businessType | Number | 是 | 业务类型 |
| tpType | Number | 是 | TP/捐赠类型 |
| amount | Money | 否 | 预计金额 |
| quantity | Integer | 否 | 预计数量 |
| purchaseTime | Date | 是 | 预计采购时间 |
| decisionMakers | Array | 否 | 关键决策人 |
| remark | String | 否 | 备注 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 意向 ID |
| intentionCode | String | 意向编号 |
| status | Number | 保存后的状态 |

### 9.5 提交意向审批

- **接口名**：submitIntention
- **请求方式**：POST
- **路径**：/api/v1/intention/submit
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID |
| isConflict | Boolean | 否 | 是否走冲突审批，默认 false |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 意向 ID |
| status | Number | 提交后的状态 |
| conflictInfo | Object | 冲突信息（isConflict=true 且存在冲突时返回） |

### 9.6 冲突检测

- **接口名**：checkConflict
- **请求方式**：POST
- **路径**：/api/v1/intention/check-conflict
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| customerId | Long | 是 | 客户 ID |
| intentionName | String | 是 | 项目名称 |
| excludeIntentionId | Long | 否 | 排除当前意向 ID |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| hasConflict | Boolean | 是否存在冲突 |
| conflictList | Array<Object> | 冲突意向列表 |

### 9.7 审批操作

- **接口名**：approveIntention
- **请求方式**：POST
- **路径**：/api/v1/intention/approve
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID |
| approvalAction | Number | 是 | 1:通过, 2:驳回 |
| approvalComment | String | 是 | 审批意见 |
| approvalNode | String | 是 | 审批节点 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 意向 ID |
| status | Number | 审批后的状态 |

### 9.8 冲突裁决

- **接口名**：resolveConflict
- **请求方式**：POST
- **路径**：/api/v1/intention/resolve-conflict
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 当前意向 ID |
| originalIntentionId | Long | 是 | 原生效意向 ID |
| approvalAction | Number | 是 | 1:通过, 2:驳回 |
| approvalComment | String | 是 | 裁决意见 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 当前意向 ID |
| status | Number | 当前意向审批后的状态 |
| originalStatus | Number | 原意向状态 |

### 9.9 录入跟进

- **接口名**：followIntention
- **请求方式**：POST
- **路径**：/api/v1/intention/follow
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID |
| followType | Number | 是 | 1:招标跟进, 2:TP/捐赠跟进, 3:常规跟进 |
| publishDate | Date | 条件必填 | followType=1 时必填 |
| biddingCompany | String | 条件必填 | followType=1 时必填 |
| biddingCode | String | 条件必填 | followType=1 时必填 |
| biddingFiles | Array | 否 | followType=1 时可选 |
| bidResult | Number | 否 | followType=1 时可选，1:中标, 2:未中标 |
| deviceModel | String | 条件必填 | followType=2 时必填 |
| monthlyReagentVolume | Number | 条件必填 | followType=2 时必填 |
| agreementCode | String | 条件必填 | followType=2 时必填 |
| agreementFiles | Array | 条件必填 | followType=2 时必填 |
| installedFlag | Number | 条件必填 | followType=2 时必填 |
| noInstallReasonType | Number | 条件必填 | followType=2 且 installedFlag=0 时必填 |
| noInstallReason | String | 条件必填 | followType=2 且 installedFlag=0 时必填 |
| followContent | String | 条件必填 | followType=3 时必填 |
| followAttachments | Array | 否 | followType=3 时可选 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| followId | Long | 跟进记录 ID |
| intentionId | Long | 意向 ID |
| status | Number | 跟进后的意向状态 |
| currentStage | String | 跟进后的当前阶段 |

### 9.10 结束分类

- **接口名**：endIntention
- **请求方式**：POST
- **路径**：/api/v1/intention/end
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID |
| endType | Number | 是 | 1:正常结束, 2:否单, 3:其他 |
| endRemark | String | 条件必填 | endType=2 或 3 时必填 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 意向 ID |
| status | Number | 结束后的状态 |

### 9.11 获取统计指标

- **接口名**：getIntentionStats
- **请求方式**：GET
- **路径**：/api/v1/intention/stats
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| timeRange | String | 否 | 时间范围：week/month/quarter |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| monthIntentionCount | Number | 本月意向数 |
| activeCount | Number | 生效中数量 |
| pendingCount | Number | 待审批数量 |
| conflictCount | Number | 冲突待裁决数量 |
| monthWinRate | Number | 本月成交率 |

### 9.12 获取审批历史

- **接口名**：getApprovalHistory
- **请求方式**：GET
- **路径**：/api/v1/intention/{intentionId}/approval-history
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| intentionId | Long | 是 | 意向 ID，路径参数 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<Object> | 审批记录列表 |

- **list 项字段**：approvalId, approvalNode, approverId, approverName, approvalAction, approvalComment, approvalTime

---

## 十、验收断言清单

### 10.1 功能：新建意向

#### 断言 1：新建意向成功

- **验证对象**：意向新建功能
- **前置条件**：用户角色为报单人，已进入意向列表页
- **操作步骤**：
  1. 点击「+ 新建意向」
  2. 步骤一选择客户、填写项目名称
  3. 步骤二选择业务类型「招标」、TP/捐赠「否」、预计采购时间
  4. 步骤三点击「确认提交」
- **预期结果**：
  1. 弹窗关闭
  2. 列表顶部出现新建的意向记录
  3. 意向状态为「审批中」
  4. 系统提示「提交成功，等待审批」
- **验收方式**：自动化测试

#### 断言 2：新建意向必填校验

- **验证对象**：意向新建表单
- **前置条件**：用户已进入新建意向弹窗
- **操作步骤**：
  1. 不填写任何字段
  2. 点击「下一步」
- **预期结果**：
  1. 客户名称、项目名称字段边框变红
  2. 字段下方显示「此项为必填项」
  3. 冲突检测接口未被调用
- **验收方式**：自动化测试

#### 断言 3：新建意向冲突检测

- **验证对象**：意向冲突检测功能
- **前置条件**：系统中已存在客户 A 项目 B 的生效中意向
- **操作步骤**：
  1. 点击「+ 新建意向」
  2. 选择客户 A、填写项目 B
  3. 点击「下一步」
- **预期结果**：
  1. 弹出冲突检测弹窗
  2. 显示原报备人、报备时间、保护期剩余
  3. 提供「查看原意向」和「继续提交，走冲突审批」两个按钮
- **验收方式**：自动化测试

### 10.2 功能：意向审批

#### 断言 4：上级领导初审通过

- **验证对象**：上级领导初审功能
- **前置条件**：用户角色为上级领导，列表中存在状态为「审批中」的意向
- **操作步骤**：
  1. 点击列表中「审批中」意向的「审批」按钮
  2. 填写审批意见「同意」
  3. 点击「通过」
- **预期结果**：
  1. 弹窗关闭
  2. 该意向状态变为「生效中」
  3. 审批历史新增一条「上级领导初审通过」记录
  4. 报单人收到审批通过消息通知
- **验收方式**：自动化测试

#### 断言 5：审批驳回必填意见

- **验证对象**：审批弹窗校验
- **前置条件**：用户角色为上级领导，已进入审批弹窗
- **操作步骤**：
  1. 不填写审批意见
  2. 点击「驳回」
- **预期结果**：
  1. 审批意见输入框边框变红
  2. 显示提示「请填写审批意见」
  3. 驳回接口未被调用
- **验收方式**：自动化测试

#### 断言 6：被驳回后重新提交

- **验证对象**：重新提交功能
- **前置条件**：存在一条状态为「被驳回」的意向
- **操作步骤**：
  1. 点击「被驳回」意向的「重新提交」按钮
  2. 修改项目名称
  3. 点击「确认提交」
- **预期结果**：
  1. 弹窗关闭
  2. 意向状态变为「审批中」
  3. 重新走完整审批链路
- **验收方式**：自动化测试

### 10.3 功能：冲突裁决

#### 断言 7：冲突裁决双人通过

- **验证对象**：冲突裁决功能
- **前置条件**：存在一条状态为「冲突待判定」的意向，厂家经理和厂家负责人账号均可访问
- **操作步骤**：
  1. 厂家经理点击「裁决」，填写意见，点击「通过当前申请」
  2. 厂家负责人点击「裁决」，填写意见，点击「通过当前申请」
- **预期结果**：
  1. 当前意向状态变为「生效中」
  2. 原冲突意向状态变为「冲突无效」
  3. 双方报单人收到裁决结果消息
- **验收方式**：自动化测试

#### 断言 8：冲突裁决任一驳回

- **验证对象**：冲突裁决功能
- **前置条件**：存在一条状态为「冲突待判定」的意向
- **操作步骤**：
  1. 厂家经理点击「裁决」，填写意见，点击「驳回当前申请」
- **预期结果**：
  1. 当前意向状态变为「已结束」
  2. 原冲突意向保持「生效中」
  3. 当前意向报单人收到驳回消息
- **验收方式**：自动化测试

#### 断言 9：冲突裁决必填意见

- **验证对象**：冲突裁决弹窗校验
- **前置条件**：用户角色为厂家经理，已进入冲突裁决弹窗
- **操作步骤**：
  1. 不填写裁决意见
  2. 点击「通过当前申请」
- **预期结果**：
  1. 裁决意见输入框边框变红
  2. 显示提示「请填写裁决意见」
  3. 裁决接口未被调用
- **验收方式**：自动化测试

### 10.4 功能：录入跟进

#### 断言 10：招标项目跟进并中标

- **验证对象**：招标项目跟进功能
- **前置条件**：存在状态为「生效中」的招标项目意向，且已通过授权审批
- **操作步骤**：
  1. 点击「跟进」
  2. 填写挂网时间、招标公司、招标编号
  3. 选择招标结果「中标」
  4. 点击「保存并继续」
- **预期结果**：
  1. 弹窗关闭
  2. 阶段变为「送货审批」
  3. 送货审批人收到待审消息
- **验收方式**：自动化测试

#### 断言 11：TP/捐赠未装机必填原因

- **验证对象**：TP/捐赠跟进表单校验
- **前置条件**：存在状态为「生效中」的 TP/投放意向
- **操作步骤**：
  1. 点击「跟进」
  2. 填写仪器型号、预估月试剂消耗量、协议编号
  3. 选择「未装机」
  4. 不填写未装机原因，点击「保存并继续」
- **预期结果**：
  1. 未装机原因字段边框变红
  2. 显示提示「未装机必须填写原因」
  3. 保存接口未被调用
- **验收方式**：自动化测试

#### 断言 12：招标项目未授权禁用结果选择

- **验证对象**：招标授权与结果联动
- **前置条件**：存在状态为「生效中」的招标项目意向，尚未发起授权申请
- **操作步骤**：
  1. 点击「跟进」
  2. 不发起授权申请
  3. 尝试选择招标结果
- **预期结果**：
  1. 「招标结果」单选框为禁用态
  2. 鼠标悬停显示提示「请先完成授权申请并通过审批」
- **验收方式**：自动化测试

### 10.5 功能：搜索筛选

#### 断言 13：意向搜索支持拼音首字母

- **验证对象**：意向列表搜索框
- **前置条件**：列表中存在负责人为「张三」的意向
- **操作步骤**：
  1. 在搜索框输入「zs」
  2. 按回车
- **预期结果**：
  1. 列表只显示负责人包含「张三」的意向
  2. 总条数更新为匹配数量
  3. 清空搜索后列表恢复
- **验收方式**：自动化测试

#### 断言 14：业务类型筛选生效

- **验证对象**：业务类型筛选
- **前置条件**：列表中存在招标和非招标意向
- **操作步骤**：
  1. 选择业务类型「招标」
- **预期结果**：
  1. 列表只显示业务类型为「招标」的意向
  2. 分页重置到第 1 页
  3. 总条数更新为招标意向数量
- **验收方式**：自动化测试

#### 断言 15：Tab 切换过滤

- **验证对象**：状态 Tab 切换
- **前置条件**：列表中存在多种状态的意向
- **操作步骤**：
  1. 点击「生效中」Tab
- **预期结果**：
  1. 列表只显示状态为「生效中」的意向
  2. 分页重置到第 1 页
  3. 其他筛选条件保留
- **验收方式**：自动化测试

### 10.6 功能：权限与数据范围

#### 断言 16：报单人只能查看本人意向

- **验证对象**：意向数据范围
- **前置条件**：系统中存在 A 销售和 B 销售的意向
- **操作步骤**：
  1. 使用 A 销售账号登录
  2. 进入意向管理
- **预期结果**：
  1. 列表只显示 A 销售的意向
  2. 不显示 B 销售的意向
  3. URL 中无权限参数时系统默认按当前用户过滤
- **验收方式**：自动化测试

#### 断言 17：上级领导查看下属意向

- **验证对象**：上级领导数据范围
- **前置条件**：系统中存在上级领导及其下属的意向
- **操作步骤**：
  1. 使用上级领导账号登录
  2. 进入意向管理
- **预期结果**：
  1. 列表显示本人及所有下属报单人提交的意向
  2. 可操作待审批意向的「审批」按钮
- **验收方式**：自动化测试

#### 断言 18：审批人只能审批指定节点

- **验证对象**：审批权限控制
- **前置条件**：存在待上级领导初审的意向和待厂家经理授权的意向
- **操作步骤**：
  1. 使用上级领导账号登录
  2. 查看待厂家经理授权的意向
- **预期结果**：
  1. 列表中该意向不显示「审批」按钮
  2. 点击详情不显示审批操作区
- **验收方式**：自动化测试

### 10.7 功能：响应式布局

#### 断言 19：移动端列表底部按钮固定

- **验证对象**：移动端意向列表页
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 进入意向列表页
  2. 向下滚动列表
- **预期结果**：
  1. 「+ 新建意向」按钮固定在屏幕右下角
  2. 按钮不随列表滚动消失
  3. 点击按钮正常打开新建弹窗
- **验收方式**：截图对比 / 自动化测试

#### 断言 20：移动端卡片左滑显示快捷操作

- **验证对象**：移动端意向卡片
- **前置条件**：使用 iPhone 14 尺寸模拟器，列表存在生效中意向
- **操作步骤**：
  1. 左滑生效中意向卡片
- **预期结果**：
  1. 卡片右侧滑出「跟进」快捷操作按钮
  2. 点击「跟进」打开跟进弹窗
  3. 点击其他区域收起快捷操作
- **验收方式**：自动化测试

#### 断言 21：移动端筛选底部抽屉

- **验证对象**：移动端筛选
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 点击搜索框右侧「筛选」按钮
- **预期结果**：
  1. 从底部弹出筛选面板
  2. 面板内包含业务类型、时间范围筛选
  3. 点击「确定」后列表刷新
- **验收方式**：截图对比 / 自动化测试

---

## 十一、关联与扩展

### 11.1 与其它模块关联

| 关联模块 | 数据流向 | 关联说明 |
|---|---|---|
| 线索管理 | 线索转意向时带入客户、科室、产品线、摘要 | 线索是意向的前置入口 |
| 客户 360° | 报备时选择客户，报备记录写入客户时间线 | 客户档案聚合 |
| 产品库 | 选择品牌/型号时调用产品库数据 | 产品中心提供字典 |
| 经销商协同 | 经销商/渠道商可提交报备，受权限和区域限制 | 渠道协同 |
| 经营驾驶舱 | 报备数、成交率、审批时效等数据汇入驾驶舱 | 数据决策 |
| 目标绩效 | 成交金额、成交数量等数据用于绩效计算 | 绩效计算 |
| 合规风控 | 大额学术活动、样品赠送等关联合规记录 | 合规审计 |
| 消息中心 | 提交、审批、冲突、保护期提醒等消息推送 | 平台级通知 |
| 日历模块 | 保护期到期、预计采购时间可生成日程提醒 | 日程管理 |

### 11.2 易遗漏点

| 序号 | 易遗漏点 | 说明 |
|---|---|---|
| 1 | 冲突检测时机 | 不仅在提交时检测，重新提交时也要重新检测 |
| 2 | 保护期计算 | 不同业务类型保护期长度可能不同，需在审批通过后按规则计算 |
| 3 | 双人并行裁决 | 厂家经理和厂家负责人任意顺序审批，系统需等待两人均完成才汇总结果 |
| 4 | 原意向状态变更 | 冲突裁决通过后，原生效意向需置为「冲突无效」，不可继续跟进 |
| 5 | 招标结果前置校验 | 未授权时禁止录入招标结果，需在前端禁用并给出明确提示 |
| 6 | 附件上传限制 | 招标文件、协议扫描件需限制格式（PDF/图片）和大小 |
| 7 | 审批意见长度 | 审批意见最少 3 个字，最大 500 字符 |
| 8 | 重新提交预填 | 重新提交弹窗需完整预填原单所有字段，包括附件 |
| 9 | 列表默认视图 | 意向总数 <=20 默认卡片视图，>20 默认列表视图 |
| 10 | 数据脱敏 | 低权限用户查看负责人、金额等字段时按需脱敏 |
| 11 | 移动端键盘遮挡 | 表单页输入时键盘弹出需保证输入框可见 |
| 12 | 审批链缺失兜底 | 提交时若审批人未配置，需明确提示而非静默失败 |

### 11.3 待确认问题

| 序号 | 问题 | 建议 |
|---|---|---|
| 1 | 保护期具体天数 | 需业务确认 A/B/C 类项目保护期规则 |
| 2 | 招标授权是否必经 | 需业务确认所有招标项目是否都必须厂家授权 |
| 3 | 送货审批人角色范围 | 需业务确认送货审批人是否唯一角色或可按区域配置 |
| 4 | 冲突裁决是否允许加签 | 需业务确认是否支持冲突裁决中加签或转交 |
| 5 | 意向结束后是否允许恢复 | 需业务确认已结束意向是否支持重新激活 |

### 11.4 术语表

| 术语 | 通俗解释 |
|---|---|
| 招标 | 医院用自有资金或财政拨款，走公开采购流程买设备 |
| TP/投放 | 厂家或经销商免费把设备放进医院，靠长期试剂复购盈利 |
| 捐赠 | 企业无偿赠与医院设备，公益行为，禁止捆绑试剂采购 |
| 保护期 | 某个销售或渠道对某项目的独家跟进时间窗口 |
| 撞单 | 两个或以上销售/渠道同时报同一客户同一项目 |
| 否单 | 项目没做成 |
| 正常结束 | 项目按预期成交或完成闭环 |

---

*本文档是鑫渠 CRM 意向管理模块的 AI 可执行设计文档，开发前必须完整阅读并遵守所有约束。*
