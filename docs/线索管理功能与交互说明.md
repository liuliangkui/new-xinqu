# 线索管理功能与交互说明

> **文档版本**：v1.4  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **对应规范**：《AI可执行设计文档编写规范.md》v1.0、《鑫渠组件库清单与命名规范.md》v1.0、《鑫渠响应式设计规范.md》v1.0、《鑫渠字段规范模板.md》v1.0、《鑫渠验收断言编写规范.md》v1.0、《鑫渠应用文档验收标准.md》v1.1、《鑫渠全局设计系统规范.md》v1.0、《鑫渠设计与开发规范.md》v1.5、《鑫渠页面布局设计理念.md》v1.0  
> **适用角色**：销售代表、销售经理、区域总监、渠道管理员、经销商账号、厂家经理  
> **文档性质**：AI 可执行设计文档

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.4 | 2026-07-29 | 按 AI 可执行文档标准重构：补齐机器指令块、组件映射、字段编码、接口契约、验收断言、响应式双端设计，同步原型 v1.8.0 | - |
| v1.3 | 2026-07-29 | 补充线索池列表页布局设计理念、板块分布、组件清单 | - |
| v1.2 | 2026-07-29 | 线索池列表页增加列表/卡片视图切换与分页，适配大量线索场景 | - |
| v1.1 | 2026-07-28 | 入口调整：线索管理模块不再作为一级侧边栏菜单，统一纳入「应用中心」业务应用 | - |
| v1.0 | 2026-07-28 | 初始发布：线索管理模块完整功能、页面字段、交互流程、业务规则 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 机器指令块

开发本模块前，AI 必须先读取并遵守以下全局约束：

- **技术栈**：Vue 3 + TypeScript + Pinia + Vue Router + Ant Design Vue 4.x + Tailwind CSS
- **组件库**：必须使用《鑫渠组件库清单与命名规范.md》v1.0 中注册的组件名，禁止自行发明组件名
- **图标**：全部使用 SVG 图标或 Iconfont，组件名统一为 `XqIcon{name}`，禁止引用 PNG/JPG 图标，禁止使用 emoji
- **样式变量**：颜色、字号、间距必须使用《鑫渠全局设计系统规范.md》v1.0 中的 CSS 变量
- **响应式断点**：
  - PC 宽屏：>=1440px
  - PC 标准：1024px-1439px
  - 平板：768px-1023px
  - 移动端：<768px
- **权限模型**：基于 RBAC + 数据范围，角色编码见《华鑫角色职责与用户故事.md》
- **数据规范**：字段命名必须与本文档「核心字段规范」完全一致
- **接口规范**：接口路径、字段命名必须与本文档「接口契约」完全一致
- **验收标准**：开发完成后必须对照本文档「验收断言清单」逐项验证
- **输出约束**：页面、弹窗、抽屉中禁止出现真实 emoji 及可能渲染为彩色 emoji 的 Unicode 符号（如对勾、叉号、齿轮、三角、箭头等），统一使用文字或 ASCII 安全符号

---

## 1. 模块定位与目标

### 1.1 一句话定位

线索管理是鑫渠 CRM 「渠道秩序域」的前置入口，用于收集、查重、分配、跟进销售线索；线索不需要审批，确认有真实商机后可一键转为意向报备单。

### 1.2 业务痛点

- 一线听到商机后没有低门槛入口备案，信息散落在微信、邮件、电话中
- 同一客户/项目被多人重复录入，导致内部撞单、资源内耗
- 线索被个人长期占用但无人跟进，优质商机在私占中流失
- 管理层无法实时看到本区域/本团队线索量、分配效率、转化率

### 1.3 量化价值

- 线索录入时间从 5 分钟缩短到 30 秒以内
- 通过系统查重降低 80% 以上的重复录入
- 公海池自动回收机制让线索平均分配时长 <= 4 小时
- 线索转意向率提升并可在 KPI 卡片中实时观测

### 1.4 所属业务域

渠道秩序域 / 业务管理类应用

### 1.5 与意向管理的关系

```
线索（登记层） -> 查重/分配/跟进 -> 确认真实商机 -> 转意向（授权层） -> 审批/保护期
```

| 维度 | 线索 | 意向 |
|---|---|---|
| 定位 | 信息信号 | 授权商机 |
| 录入门槛 | 低（30 秒一句话） | 高（需关键联系人、预算、竞品等） |
| 是否需要审批 | 不需要 | 需要 |
| 归属方式 | 公海池，管理员分配或规则认领 | 审批通过后唯一授权 |
| 保护期 | 无，设跟进时限 | 有 A/B/C 类保护期 |
| 数据可见性 | 本区域/本经销商可见（可配置） | 按授权与审批结果可见 |

---

## 2. 使用角色与权限

### 2.1 角色清单与职责

| 角色编码 | 角色名称 | 职责说明 | 系统操作 |
|---|---|---|---|
| SALES_REP | 销售代表 | 录入本人/本区域线索、补充线索跟进、领取公海线索（若开放） | 新建、编辑、跟进、申请转意向 |
| SALES_MANAGER | 销售经理 | 审核本团队线索、分配公海线索、代录进展 | 分配、驳回、转意向、废弃 |
| REGION_DIRECTOR | 区域总监 | 管理本区域线索池、调配资源、审批跨区线索 | 分配、回收、转交、查看分析 |
| CHANNEL_ADMIN | 渠道管理员 | 管理经销商录入的线索、处理跨经销商冲突 | 查重裁决、分配、废弃、恢复 |
| DEALER_ACCOUNT | 经销商账号 | 代录本公司获取的市场线索 | 新建、查看本公司线索 |
| FACTORY_MANAGER | 厂家经理 | 查看本厂家产品相关的线索、提供支持资料 | 查看、评论、协助转意向 |

### 2.2 数据可见性

| 角色 | 可见范围 |
|---|---|
| 销售代表 | 本人录入/负责的线索、本区域公海线索 |
| 销售经理 | 本团队所有线索、本区域公海池 |
| 区域总监 | 本大区内全部线索 |
| 渠道管理员 | 全部线索（含经销商端录入） |
| 经销商账号 | 本公司录入的线索、分配给本公司的线索 |
| 厂家经理 | 本厂家产品相关的线索 |

### 2.3 功能权限矩阵

| 功能 | 销售代表 | 销售经理 | 区域总监 | 渠道管理员 | 经销商账号 | 厂家经理 |
|---|---|---|---|---|---|---|
| 查看线索列表 | 本人/本区域 | 本团队 | 本大区 | 全部 | 本公司 | 本产品 |
| 录入线索 | 是 | 是 | 是 | 是 | 是 | 否 |
| 编辑线索 | 本人未分配 | 本团队 | 本大区 | 全部 | 本公司 | 否 |
| 分配线索 | 否 | 是 | 是 | 是 | 否 | 否 |
| 领取公海线索 | 是（若开启） | 是 | 是 | 是 | 否 | 否 |
| 录入跟进 | 本人负责 | 本团队 | 本大区 | 全部 | 本公司 | 协助 |
| 转意向 | 本人负责 | 本团队 | 本大区 | 全部 | 否 | 否 |
| 废弃线索 | 本人负责 | 本团队 | 本大区 | 全部 | 否 | 否 |
| 恢复线索 | 否 | 否 | 否 | 是 | 否 | 否 |
| 查看详情 | 本人/本区域 | 本团队 | 本大区 | 全部 | 本公司 | 本产品 |

### 2.4 字段级权限

| 字段 | 全员可见 | 角色可见 | 可编辑 | 说明 |
|---|---|---|---|---|
| leadCode、customerName、department、sourceType、status、creatorName、createTime | 是 | - | 只读 | 基础信息公开 |
| leadSummary、productLine、amount、decisionDate | 是 | - | 归属人/管理员 | 线索核心信息 |
| assigneeName、followDeadline、nextFollowTime | 是 | - | 分配人/归属人 | 归属相关 |
| remark | 是 | - | 归属人/管理员 | 备注 |

---

## 3. 核心功能清单

| 功能 | 优先级 | 功能说明 | 与原型对应 |
|---|---|---|---|
| 线索池列表 | 必要 | 按 Tab、视图、筛选查看线索，支持卡片/列表切换 | 原型「线索管理」主页面 |
| 线索录入 | 必要 | 30 秒最小化录入，失焦/保存时自动查重 | 原型「录入线索」弹窗 |
| 线索详情 | 必要 | 查看线索基础信息、跟进时间线、操作按钮 | 原型「线索详情」抽屉 |
| 线索分配 | 必要 | 管理员将待分配线索指派归属人并设置跟进时限 | 原型详情页「分配」按钮 |
| 领取公海线索 | 重要 | 销售在开放场景下领取公海池线索 | 原型公海池卡片「领取」按钮 |
| 跟进录入 | 必要 | 归属人记录跟进方式、内容、下步计划 | 原型详情页「录入跟进」按钮 |
| 转意向 | 必要 | 跟进中线索补充信息后生成意向报备单 | 原型详情页「转意向」按钮 |
| 废弃/恢复 | 重要 | 确认无价值线索废弃，管理员可恢复 | 原型详情页「废弃」按钮 |
| 线索回收 | 重要 | 超期未跟进自动回收至公海池 | 后台定时任务 + 状态变更 |
| 数据统计 | 重要 | 顶部 KPI 卡片展示本月新增、待分配、跟进中、转意向率 | 原型顶部统计卡片 |

---

## 4. 核心业务流程

### 4.1 线索录入流程

#### 4.1.1 流程目标

让一线人员以最低门槛把听到的商机备案进系统，并在录入时自动完成冲突检测。

#### 4.1.2 参与角色

销售代表、经销商账号、销售经理、渠道管理员（处理冲突）。

#### 4.1.3 流程图

```
一线人员点击「录入线索」
        |
        v
填写客户名称、科室、产品线、摘要、来源
        |
        v
客户名称失焦/点击保存时触发查重
        |
        +-- 命中生效意向 -> 阻断，提示查看意向
        +-- 命中历史线索 -> 提示补充到现有线索或确认新项目
        +-- 无冲突 -> 保存成功
        |
        v
线索进入「待分配」状态
        |
        v
按规则自动分配 或 管理员手动分配
```

#### 4.1.4 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 销售代表 | 点击「录入线索」 | 校验今日新建上限 | 打开 XqFormDrawer |
| 2 | 销售代表 | 填写客户名称、科室、产品线、线索摘要、来源、预计金额、预计决策时间 | 校验必填项、格式、长度 | 表单高亮错误 |
| 3 | 系统 | 客户名称失焦或保存时查重 | 匹配客户+科室+产品线/已生效意向 | 显示查重提示 |
| 4 | 系统 | 二次查重通过 | 无冲突 | 生成 leadCode，状态=待分配 |
| 5 | 系统/管理员 | 自动/手动分配 | 校验分配权限 | 状态=跟进中，开始倒计时 |

#### 4.1.5 异常分支

| 异常场景 | 触发条件 | 处理方式 | 页面反馈 |
|---|---|---|---|
| 客户名称未填 | 保存时 customerName 为空 | 阻断保存 | 字段红框 + 提示「客户名称为必填项」 |
| 线索摘要为空 | 保存时 leadSummary 为空 | 阻断保存 | 字段红框 + 提示「线索摘要为必填项」 |
| 命中生效意向 | 客户+科室+产品线与生效意向一致 | 阻断录入 | XqConfirmDialog：「该项目已有授权意向，不可重复录入，是否查看意向？」 |
| 命中历史线索 | 客户+产品线一致 | 弱提示 | XqToast：「检测到相似客户，是否查看？」 |
| 强重复 | 客户+科室+产品线均命中 | 强提示 | XqConfirmDialog：选择「补充到现有线索 / 确认是不同项目 / 取消」 |

### 4.2 线索分配流程

#### 4.2.1 流程目标

将待分配线索通过公海池机制分配给最合适的归属人，并设置跟进时限防止占坑不跟进。

#### 4.2.2 流程图

```
待分配线索进入公海池
        |
        v
管理员/区域总监查看公海池
        |
        v
按区域、产品线、负责人负荷选择归属人
        |
        v
设置跟进时限（默认 7 天）
        |
        v
确认分配 -> 归属人收到通知
        |
        v
线索状态变为「跟进中」
```

#### 4.2.3 步骤表

| 步骤 | 角色 | 操作 | 系统校验 | 输出 |
|---|---|---|---|---|
| 1 | 系统 | 线索进入公海池 | 状态=待分配 | 公海池可见 |
| 2 | 区域总监/渠道管理员 | 勾选线索，点击「分配」 | 校验分配权限 | 打开分配弹窗 |
| 3 | 区域总监/渠道管理员 | 选择归属人、协同人、跟进时限、填写分配说明 | 校验用户数据范围 | 表单校验 |
| 4 | 系统 | 确认分配 | 校验归属人角色 | 消息通知归属人 |
| 5 | 系统 | 更新线索状态 | 状态=跟进中 | 开始跟进倒计时 |

### 4.3 线索跟进与转意向流程

#### 4.3.1 流程目标

归属人持续更新线索进展，成熟后升级为意向报备单。

#### 4.3.2 流程图

```
归属人收到分配通知
        |
        v
进入线索详情 -> 录入跟进
        |
        v
记录跟进方式、内容、下步计划
        |
        v
判断商机成熟度
        |
        +-- 成熟 -> 申请转意向 -> 二次查重 -> 生成意向单 -> 状态=已转意向
        +-- 不成熟 -> 继续跟进
        +-- 无价值 -> 申请废弃 -> 状态=已废弃
        +-- 无法跟进 -> 释放回公海 -> 状态=待分配
```

#### 4.3.3 转意向前置条件

- 线索状态为「跟进中」
- 归属人或协同人发起
- 至少存在一条跟进记录
- 转意向时二次查重无冲突

### 4.4 线索回收与恢复流程

#### 4.4.1 流程目标

通过自动回收机制盘活闲置线索，并允许管理员恢复误废弃线索。

#### 4.4.2 流程图

```
系统每日扫描跟进中线索
        |
        v
发现超期未跟进（默认 7 天无更新）
        |
        v
提前 1 天推送预警给归属人
        |
        v
超期后自动回收至公海池
        |
        v
线索状态变为「待分配」
        |
        v
原归属人可查看但不可操作
        |
        v
管理员重新分配
```

#### 4.4.3 恢复规则

- 已废弃线索仅渠道管理员可在 7 天内恢复
- 恢复后状态回到「待分配」
- 系统记录恢复原因与操作人

---

## 5. 页面清单与路由

| 页面 | 路由/URL | 页面类型 | 功能说明 |
|---|---|---|---|
| 线索池列表页 | /app/leads | 列表页 | 查看全部/我的/公海/团队线索，支持卡片/列表视图 |
| 线索录入弹窗 | /app/leads/create（弹窗） | 弹窗 | 新建线索，自动查重 |
| 线索详情抽屉 | /app/leads/:id（抽屉） | 抽屉 | 查看线索详情、跟进时间线、状态操作 |
| 分配弹窗 | /app/leads/:id/assign（弹窗） | 弹窗 | 指定归属人、协同人、跟进时限 |
| 跟进录入弹窗 | /app/leads/:id/follow（弹窗） | 弹窗 | 记录跟进内容、方式、下步计划 |
| 转意向弹窗 | /app/leads/:id/convert（弹窗） | 弹窗 | 补充意向必填字段后提交 |
| 废弃确认弹窗 | /app/leads/:id/discard（弹窗） | 弹窗 | 填写废弃原因后废弃 |

> 移动端列表页点击卡片后进入全屏详情页，路由为 /m/app/leads/:id。

---

## 6. 页面结构与布局

### 6.1 线索池列表页

**布局模板**：列表-筛选-处理模板（模板 A）

**外层容器**：XqPageLayout

```
+-------------------------------------------------+
|  线索管理                              [录入线索] |
+-------------------------------------------------+
|  [本月新增] [待分配] [跟进中] [转意向率]         |
+-------------------------------------------------+
|  [全部] [我的线索] [公海池] [团队线索]  列表/卡片 |
+-------------------------------------------------+
|  [搜索...] [来源] [状态] [时间范围]             |
+-------------------------------------------------+
|  卡片网格 4 列  或  数据表格                     |
+-------------------------------------------------+
|  1 / 2 页，共 14 条      上一页  [1] [2] 下一页  |
+-------------------------------------------------+
```

**组件映射**：

| 区域 | 组件 | Props / Slots | 说明 |
|---|---|---|---|
| 页面标题 | XqPageLayout title | title="线索管理" | 右侧操作区使用 actions slot |
| 主操作 | 页面 actions slot | - | 「录入线索」按钮，图标 XqIconPlus，点击 openLeadCreateModal |
| 统计卡片 | XqKpiCard x4 | title/value/color/clickable | 本月新增（--primary）、待分配（warning）、跟进中（--ink）、转意向率（success） |
| Tab 分组 | XqNavTabs | tabs=[{key:'all',label:'全部'},{key:'mine',label:'我的线索'},{key:'public',label:'公海池'},{key:'team',label:'团队线索'}] | 按角色过滤显示，切换后重置 pageNum=1 |
| 视图切换 | XqViewSwitch | value: 'card'/'list' | 默认智能：总数<=20 卡片，>20 列表 |
| 搜索 | XqSearchBar | v-model:keyword, placeholder="搜索客户、科室、摘要、来源...", allowClear=true, pinyinSearch=true, width="240px" | PC 端 240px，移动端 100% 吸顶 |
| 筛选 | XqFilterBar | filters=[sourceType,status,dateRange] | PC 端平铺，移动端底部抽屉 |
| 卡片视图 | XqCardGrid | dataSource, columns=4 | 每页 8 条，使用 XqLeadCard 渲染 |
| 列表视图 | XqDataTable | columns, dataSource, rowKey='leadId' | 每页 12 条，操作列固定右侧 |
| 状态标签 | XqStatusBadge | status/statusMap | 状态着色 |
| 空态 | XqEmptyState | type='empty'/'search' | 无数据/无搜索结果 |
| 分页 | 自定义分页组件 | pageNum/pageSize/total | 底部显示页码与翻页 |

### 6.2 线索录入弹窗

**组件**：XqFormDrawer

**Props**：

- visible: Boolean
- title: "录入线索"
- width: "560px"（PC 端）/ 移动端全屏
- okText: "保存"
- cancelText: "取消"

**表单字段布局（PC 端 1-2 列）**：

```
客户名称 *          （单行文本，占位：搜索医院 / 客户名称）
科室        产品线   （两个 XqSelect 下拉）
线索摘要 *          （XqTextarea，2 行，最多 200 字符）
来源        预计金额 （XqSelect + 金额输入）
预计决策时间        （XqDate，格式 yyyy-MM）
查重提示区          （静态提示，查重结果动态替换）
```

**移动端**：单列，底部固定「保存」按钮。

### 6.3 线索详情抽屉

**组件**：XqDrawer

**Props**：

- visible: Boolean
- title: leadCode
- width: "720px"（PC 端）/ 移动端全屏
- showFooter: false

**内容结构**：

```
+-------------------------------------------------+
|  L2026072801 · 昆明市第一人民医院        [关闭]  |
+-------------------------------------------------+
|  当前状态                                       |
|  [待分配]                                       |
|  该线索尚未分配归属人，请在规定时间内完成分配。  |
|  [分配] [关闭]                                  |
+-------------------------------------------------+
|  基本信息                                       |
|  客户名称        昆明市第一人民医院             |
|  科室            检验科                         |
|  产品线          血液分析                       |
|  来源            展会                           |
|  预计金额        ¥60万                          |
|  预计决策时间    2026-08                        |
|  录入人          张经理                         |
|  归属人          --                             |
+-------------------------------------------------+
|  线索摘要                                       |
|  血球仪升级意向，预算 60 万                      |
+-------------------------------------------------+
|  跟进时间线（XqTimeline）                        |
+-------------------------------------------------+
|  [录入跟进] [转意向] [分配] [废弃]              |
+-------------------------------------------------+
```

### 6.4 分配弹窗

**组件**：XqFormDrawer

**字段**：

- 归属人（XqSelect，必填，用户数据范围过滤）
- 协同人（XqSelect，多选，可选）
- 跟进时限（XqSelect，默认 7 天，选项 3/5/7/14 天）
- 分配说明（XqTextarea，可选，最多 200 字符）

### 6.5 跟进录入弹窗

**组件**：XqFormDrawer

**字段**：

- 跟进方式（XqSelect，必填：电话/拜访/微信/邮件/其他）
- 跟进内容（XqTextarea，必填，最多 500 字符）
- 下步计划（XqTextarea，可选，最多 200 字符）
- 下次跟进时间（XqDateTime，可选）
- 附件（XqUpload，可选）

### 6.6 转意向弹窗

**组件**：XqFormDrawer

**字段**：

- 客户/科室（自动带入，可修改）
- 产品线（自动带入）
- 项目概述（由线索摘要扩展）
- 关键联系人（XqText，必填）
- 联系方式（XqText，必填）
- 决策链（XqTextarea，可选）
- 预算金额（XqMoney，必填）
- 预计成交时间（XqDate，必填）
- 竞品信息（XqTextarea，可选）
- 业务类型（XqSelect，必填：招标/非招标/TP/捐赠）

---

## 7. 核心字段规范

### 7.1 线索主对象字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| leadId | 线索 ID | Long | - | 是 | 无 | 系统生成 | - | 不可见 | 不显示 |
| leadCode | 线索编号 | String | 50 | 是 | 无 | 系统生成 | - | 全员可见，只读 | XqText |
| leadSummary | 线索摘要 | String | 200 | 是 | 无 | 手工录入 | - | 全员可见，归属人/管理员可编辑 | XqTextarea / XqText |
| customerId | 客户 ID | Long | - | 否 | 无 | 关联客户/手工录入 | - | 全员可见 | 表单下拉 XqSelect |
| customerName | 客户名称 | String | 100 | 是 | 无 | 手工录入/关联 | - | 全员可见，录入人可编辑 | XqText |
| department | 科室 | String | 50 | 否 | 无 | 手工录入 | - | 全员可见，归属人/管理员可编辑 | XqText / XqSelect |
| productLine | 产品线 | Enum | - | 是 | 无 | 字典 | 1:血液分析, 2:凝血分析, 3:生化分析, 4:免疫分析, 5:微生物, 6:整体解决方案 | 全员可见 | XqStatusBadge |
| sourceType | 线索来源 | Enum | - | 是 | 无 | 字典 | 1:学术会, 2:招标, 3:经销商推荐, 4:市场活动, 5:拜访, 6:官网注册, 7:展会, 8:电话陌拜, 9:第三方平台, 10:其他 | 全员可见 | XqStatusBadge |
| sourceName | 来源名称 | String | 50 | 否 | 无 | 字典转义 | - | 全员可见 | XqText |
| amount | 预计金额 | Money | 18,2 | 否 | 0 | 手工录入 | - | 全员可见，归属人/管理员可编辑 | XqMoney |
| decisionDate | 预计决策时间 | Date | yyyy-MM | 否 | 无 | 手工录入 | - | 全员可见，归属人/管理员可编辑 | XqDate |
| status | 线索状态 | Enum | - | 是 | 0 | 系统生成 | 0:待分配, 1:跟进中, 2:已转意向, 3:已废弃 | 全员可见，只读 | XqStatusBadge |
| assigneeId | 归属人 ID | Integer | - | 否 | 无 | 分配/关联用户 | - | 全员可见 | 不显示 |
| assigneeName | 归属人姓名 | String | 50 | 否 | 无 | 关联用户 | - | 全员可见 | XqUser |
| collaboratorIds | 协同人 ID 列表 | Array<Integer> | - | 否 | [] | 分配 | - | 全员可见 | 不显示 |
| collaboratorNames | 协同人姓名 | String | 200 | 否 | 无 | 关联用户 | - | 全员可见 | XqUser |
| followDeadline | 跟进截止时间 | DateTime | yyyy-MM-dd HH:mm:ss | 否 | 无 | 计算 | - | 全员可见 | XqCountdown |
| nextFollowTime | 下次跟进时间 | DateTime | yyyy-MM-dd HH:mm | 否 | 无 | 手工录入 | - | 归属人可见 | XqDate |
| creatorId | 录入人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 不可见 | 不显示 |
| creatorName | 录入人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | - | 全员可见，只读 | XqUser |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 全员可见，只读 | XqDate |
| updateTime | 更新时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 全员可见，只读 | XqDate |
| remark | 备注 | String | 500 | 否 | 无 | 手工录入 | - | 全员可见，归属人/管理员可编辑 | XqTextarea |
| discardedReason | 废弃原因 | String | 200 | 否 | 无 | 手工录入 | - | 全员可见 | XqText |
| discardedTime | 废弃时间 | DateTime | yyyy-MM-dd HH:mm:ss | 否 | 无 | 系统生成 | - | 全员可见 | XqDate |
| intentionId | 关联意向 ID | Long | - | 否 | 无 | 关联 | - | 全员可见 | 跳转链接 |

### 7.2 线索状态机

| 状态编码 | 状态名称 | 可执行操作 | 下一状态 | 触发条件 |
|---|---|---|---|---|
| 0 | 待分配 | 分配、领取、查重合并、废弃 | 1 / 3 | 管理员分配/销售领取 -> 1；确认无价值 -> 3 |
| 1 | 跟进中 | 录入跟进、转意向、释放回公海、废弃 | 2 / 0 / 3 | 提交转意向 -> 2；释放回公海 -> 0；废弃 -> 3 |
| 2 | 已转意向 | 查看关联意向、补充资料 | - | 转意向成功后进入 |
| 3 | 已废弃 | 查看、恢复（管理员） | 0 | 管理员恢复 -> 0 |

### 7.3 状态标签颜色

| 状态编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 0 | 待分配 | #FF8800 | 待处理 |
| 1 | 跟进中 | #3370FF | 进行中 |
| 2 | 已转意向 | #34C724 | 正向完成 |
| 3 | 已废弃 | #F54A45 | 风险/结束 |

### 7.4 线索来源枚举

| 编码 | 显示文案 | 颜色 | 说明 |
|---|---|---|---|
| 1 | 学术会 | #3370FF | 学术会议 |
| 2 | 招标 | #FF8800 | 招标信息 |
| 3 | 经销商推荐 | #6B7280 | 渠道推荐 |
| 4 | 市场活动 | #3370FF | 市场活动 |
| 5 | 拜访 | #6B7280 | 一线拜访 |
| 6 | 官网注册 | #3370FF | 官网/线上 |
| 7 | 展会 | #3370FF | 展会 |
| 8 | 电话陌拜 | #6B7280 | 电话开拓 |
| 9 | 第三方平台 | #6B7280 | 外部平台 |
| 10 | 其他 | #9CA3AF | 其他 |

### 7.5 产品线枚举

| 编码 | 显示文案 | 说明 |
|---|---|---|
| 1 | 血液分析 | 血球仪/血型仪 |
| 2 | 凝血分析 | 凝血分析仪/试剂 |
| 3 | 生化分析 | 生化仪/试剂 |
| 4 | 免疫分析 | 免疫分析仪/试剂 |
| 5 | 微生物 | 微生物相关产品 |
| 6 | 整体解决方案 | 多产品组合 |

### 7.6 跟进记录字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 枚举值 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|---|
| followId | 跟进记录 ID | Long | - | 是 | 无 | 系统生成 | - | 不可见 | 不显示 |
| leadId | 线索 ID | Long | - | 是 | 无 | 关联 | - | 不可见 | 不显示 |
| followTime | 跟进时间 | DateTime | yyyy-MM-dd HH:mm | 是 | 当前时间 | 系统生成 | - | 全员可见 | XqDate |
| followWay | 跟进方式 | Enum | - | 是 | 无 | 字典 | 1:电话, 2:拜访, 3:微信, 4:邮件, 5:其他 | 全员可见 | XqStatusBadge |
| followContent | 跟进内容 | String | 500 | 是 | 无 | 手工录入 | - | 全员可见 | XqTextarea |
| nextPlan | 下步计划 | String | 200 | 否 | 无 | 手工录入 | - | 全员可见 | XqText |
| nextFollowTime | 下次跟进时间 | DateTime | yyyy-MM-dd HH:mm | 否 | 无 | 手工录入 | - | 归属人可见 | XqDate |
| attachments | 附件列表 | Array<Object> | - | 否 | [] | 文件上传 | - | 全员可见 | XqUpload |
| operatorId | 操作人 ID | Integer | - | 是 | 当前用户 | 系统生成 | - | 不可见 | 不显示 |
| operatorName | 操作人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | - | 全员可见 | XqUser |
| createTime | 创建时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | - | 全员可见 | XqDate |

### 7.7 分配记录字段

| 字段编码 | 中文名称 | 数据类型 | 长度/精度 | 是否必填 | 默认值 | 数据来源 | 读写权限 | 渲染方式 |
|---|---|---|---|---|---|---|---|---|
| assignId | 分配记录 ID | Long | - | 是 | 无 | 系统生成 | 不可见 | 不显示 |
| leadId | 线索 ID | Long | - | 是 | 无 | 关联 | 不可见 | 不显示 |
| assigneeId | 归属人 ID | Integer | - | 是 | 无 | 选择用户 | 全员可见 | 不显示 |
| assigneeName | 归属人姓名 | String | 50 | 是 | 无 | 关联用户 | 全员可见 | XqUser |
| collaboratorIds | 协同人 ID 列表 | Array<Integer> | - | 否 | [] | 选择用户 | 不可见 | 不显示 |
| followDeadline | 跟进截止时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间+7天 | 计算 | 全员可见 | XqCountdown |
| assignRemark | 分配说明 | String | 200 | 否 | 无 | 手工录入 | 全员可见 | XqTextarea |
| operatorId | 操作人 ID | Integer | - | 是 | 当前用户 | 系统生成 | 不可见 | 不显示 |
| operatorName | 操作人姓名 | String | 50 | 是 | 当前用户 | 系统生成 | 全员可见 | XqUser |
| createTime | 分配时间 | DateTime | yyyy-MM-dd HH:mm:ss | 是 | 当前时间 | 系统生成 | 全员可见 | XqDate |

---

## 8. 交互设计要点

### 8.1 搜索与筛选

- **搜索框**：使用 XqSearchBar，placeholder="搜索客户、科室、摘要、来源..."，支持拼音首字母，输入 debounce 300ms，回车或点击搜索图标触发，允许一键清空
- **筛选条件**：来源、状态、时间范围，使用 XqFilterBar，PC 端平铺，超出折叠到「更多筛选」
- **联动规则**：搜索与筛选条件同时生效；切换 Tab 保留搜索、重置分页；切换视图保留搜索筛选；切换筛选后 pageNum=1
- **结果反馈**：列表顶部显示「共 N 条」，无结果时显示 XqEmptyState type='search'

### 8.2 操作反馈

- 保存成功：XqToast type='success'，消息「线索保存成功」
- 分配成功：XqToast type='success'，消息「分配成功，已通知归属人」
- 废弃操作：XqConfirmDialog dangerous=true，要求填写废弃原因，确认后执行
- 转意向成功：XqToast type='success'，消息「已生成意向单，进入审批流」，并刷新列表
- 网络错误：XqToast type='error'，消息「网络异常，请稍后重试」

### 8.3 空态设计

| 场景 | 组件 | 配置 |
|---|---|---|
| 列表无数据 | XqEmptyState type='empty' | title="暂无线索"，description="点击下方按钮录入第一条线索"，action={text:'录入线索', onClick:openLeadCreateModal} |
| 搜索无结果 | XqEmptyState type='search' | title="未找到相关线索"，description="请调整搜索词或筛选条件" |
| 无权限 | XqEmptyState type='permission' | title="暂无查看权限"，description="请联系管理员申请权限" |

### 8.4 快捷操作

- **PC 端卡片/行悬停**：显示当前状态下最合理的 1-2 个操作
- **移动端卡片**：底部固定操作栏或左滑显示快捷操作
- **操作按钮按状态显示**：
  - 待分配：分配 / 领取
  - 跟进中：跟进 / 转意向
  - 已转意向：查看
  - 已废弃：查看 / 恢复（管理员）

### 8.5 异常处理

| 异常 | 处理 |
|---|---|
| 必填校验失败 | 字段红框，下方显示「此项为必填项」，提交接口不被调用 |
| 查重命中生效意向 | XqConfirmDialog 阻断，提供「查看意向」跳转 |
| 查重命中历史线索 | XqToast 弱提示，提供「查看相似线索」入口 |
| 网络超时 | XqToast error，保留表单数据，允许重试 |
| 无权限操作 | 按钮 disabled 或隐藏，点击时提示「无操作权限」 |
| 重复提交 | 保存按钮 loading，接口返回前禁用 |

### 8.6 查重提示分级

- **弱提示**：客户名相似但科室/产品不同 -> 「检测到相似客户，是否查看？」
- **强提示**：客户+科室+产品均命中 -> 「该线索可能与【{customerName}{department}{productLine}】重复，请选择：补充到现有线索 / 确认是不同项目 / 取消」
- **阻断提示**：已存在生效意向 -> 「该项目已有授权意向，不可重复录入线索。查看意向详情」

---

## 9. 响应式双端设计

### 9.1 断点定义

| 断点名 | 宽度范围 | 设备类型 | 布局策略 |
|---|---|---|---|
| xs | < 768px | 手机 | 单列，底部固定操作，抽屉全屏 |
| sm | 768px - 1023px | 平板 | 双列或单列，侧边栏可折叠 |
| md | 1024px - 1439px | PC 标准 | 多列，左侧固定导航 |
| lg | >= 1440px | PC 宽屏 | 多列，更宽内容区 |

### 9.2 PC 端布局

- **列表页**：五层结构，顶部标题+主操作，统计卡片横向 4 列，Tab+视图切换+主操作，搜索筛选横向平铺，下方卡片网格 4 列或数据表格，底部分页
- **录入弹窗**：XqFormDrawer 右侧抽屉，宽度 560px，表单 1-2 列，底部固定取消/保存
- **详情抽屉**：XqDrawer 右侧抽屉，宽度 720px，分状态卡、基础信息、摘要、时间线、操作按钮五段
- **分配/跟进/转意向弹窗**：XqFormDrawer 右侧抽屉，宽度 560px
- **表格**：支持固定操作列，超长文本悬停滚动

### 9.3 移动端布局

- **列表页**：
  - 单列卡片列表，顶部标题+右侧「+」主操作按钮
  - XqSearchBar 100% 宽度吸顶
  - 筛选入口在搜索右侧，点击弹出底部筛选面板
  - 统计卡片横向滚动，每张卡片最小宽度 120px
  - Tab 横向滚动
  - 分页简化为「加载更多」或「1/10 页」
- **详情页**：全屏页面，顶部固定返回按钮+标题，底部固定主操作按钮
- **录入/分配/跟进/转意向**：全屏页面，底部固定「保存」按钮，表单单列

### 9.4 手势规范

| 手势 | 触发场景 | 操作 |
|---|---|---|
| 下拉刷新 | 移动端列表 | 刷新当前列表 |
| 上拉加载 | 移动端列表 | 加载下一页 |
| 左滑卡片 | 移动端卡片 | 显示快捷操作（跟进/分配/转意向） |
| 长按 | 移动端列表项 | 弹出操作菜单 |
| 点击 | 通用 | 进入详情/触发操作 |

---

## 10. 接口契约（前端视角）

### 10.1 获取线索列表

- **接口名**：getLeadList
- **请求方式**：POST
- **路径**：/api/v1/lead/list
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| pageNum | Number | 是 | 页码，默认 1 |
| pageSize | Number | 是 | 每页条数，卡片 8，列表 12 |
| keyword | String | 否 | 搜索关键词，匹配客户、科室、摘要、来源 |
| tab | String | 否 | 范围：all/mine/public/team |
| sourceType | Number | 否 | 来源筛选 |
| status | Number | 否 | 状态筛选 |
| dateRange | String | 否 | 时间范围：week/month/quarter |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| list | Array<Object> | 线索列表 |
| total | Number | 总条数 |
| pageNum | Number | 当前页码 |
| pageSize | Number | 每页大小 |

### 10.2 获取线索统计

- **接口名**：getLeadStatistics
- **请求方式**：GET
- **路径**：/api/v1/lead/statistics
- **入参**：tab=String
- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| monthNewCount | Number | 本月新增 |
| pendingCount | Number | 待分配 |
| followingCount | Number | 跟进中 |
| convertRate | Number | 转意向率，如 0.31 |

### 10.3 新建线索

- **接口名**：createLead
- **请求方式**：POST
- **路径**：/api/v1/lead/create
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| customerName | String | 是 | 客户名称 |
| department | String | 否 | 科室 |
| productLine | Number | 是 | 产品线编码 |
| leadSummary | String | 是 | 线索摘要 |
| sourceType | Number | 是 | 来源编码 |
| amount | Money | 否 | 预计金额 |
| decisionDate | String | 否 | 预计决策时间，yyyy-MM |
| remark | String | 否 | 备注 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| leadId | Long | 线索 ID |
| leadCode | String | 线索编号 |
| status | Number | 状态，默认 0 |

### 10.4 线索查重

- **接口名**：checkLeadDuplicate
- **请求方式**：POST
- **路径**：/api/v1/lead/duplicate/check
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| customerName | String | 是 | 客户名称 |
| department | String | 否 | 科室 |
| productLine | Number | 否 | 产品线编码 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| level | String | 冲突等级：none/weak/strong/block |
| leads | Array<Object> | 命中历史线索列表 |
| intentions | Array<Object> | 命中生效意向列表 |
| message | String | 提示文案 |

### 10.5 获取线索详情

- **接口名**：getLeadDetail
- **请求方式**：GET
- **路径**：/api/v1/lead/detail/{leadId}
- **入参**：URL path leadId
- **出参**：线索主对象完整字段 + followList + assignLogList

### 10.6 分配线索

- **接口名**：assignLead
- **请求方式**：POST
- **路径**：/api/v1/lead/assign
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |
| assigneeId | Integer | 是 | 归属人 ID |
| collaboratorIds | Array<Integer> | 否 | 协同人 ID 列表 |
| followDeadline | DateTime | 是 | 跟进截止时间 |
| assignRemark | String | 否 | 分配说明 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| success | Boolean | 是否成功 |

### 10.7 领取公海线索

- **接口名**：claimLead
- **请求方式**：POST
- **路径**：/api/v1/lead/claim
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |

- **出参**：success Boolean

### 10.8 录入跟进

- **接口名**：addLeadFollow
- **请求方式**：POST
- **路径**：/api/v1/lead/follow/add
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |
| followWay | Number | 是 | 跟进方式编码 |
| followContent | String | 是 | 跟进内容 |
| nextPlan | String | 否 | 下步计划 |
| nextFollowTime | DateTime | 否 | 下次跟进时间 |
| attachments | Array<String> | 否 | 附件 URL 列表 |

- **出参**：followId Long

### 10.9 转意向

- **接口名**：convertLeadToIntention
- **请求方式**：POST
- **路径**：/api/v1/lead/convert
- **入参**：线索主对象字段 + 意向扩展字段

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |
| contactName | String | 是 | 关键联系人 |
| contactPhone | String | 是 | 联系方式 |
| decisionChain | String | 否 | 决策链 |
| amount | Money | 是 | 预算金额 |
| expectDealDate | Date | 是 | 预计成交时间 |
| competitorInfo | String | 否 | 竞品信息 |
| businessType | Number | 是 | 业务类型：1招标/2非招标/3TP/4捐赠 |

- **出参**：

| 字段编码 | 类型 | 说明 |
|---|---|---|
| intentionId | Long | 生成的意向 ID |
| intentionCode | String | 意向编号 |

### 10.10 废弃线索

- **接口名**：discardLead
- **请求方式**：POST
- **路径**：/api/v1/lead/discard
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |
| reason | String | 是 | 废弃原因 |

- **出参**：success Boolean

### 10.11 恢复线索

- **接口名**：recoverLead
- **请求方式**：POST
- **路径**：/api/v1/lead/recover
- **入参**：

| 字段编码 | 类型 | 必填 | 说明 |
|---|---|---|---|
| leadId | Long | 是 | 线索 ID |
| recoverReason | String | 是 | 恢复原因 |

- **出参**：success Boolean

---

## 11. 验收断言清单

### 11.1 功能：线索列表查询与筛选

#### 断言 1：默认进入线索列表展示统计卡片

- **验证对象**：线索池列表页
- **前置条件**：用户已登录并进入线索管理，角色为销售代表
- **操作步骤**：
  1. 点击应用中心 -> 线索管理
  2. 等待页面加载完成
- **预期结果**：
  1. 页面标题显示「线索管理」
  2. 顶部出现 4 个 XqKpiCard，分别显示本月新增、待分配、跟进中、转意向率
  3. 默认显示「全部」Tab 且高亮
- **验收方式**：截图对比 / 自动化测试

#### 断言 2：搜索支持客户名称模糊匹配

- **验证对象**：XqSearchBar
- **前置条件**：列表中存在客户为「昆明市第一人民医院」的线索
- **操作步骤**：
  1. 在搜索框输入「昆明」
  2. 等待 300ms
- **预期结果**：
  1. 列表只显示客户名称包含「昆明」的线索
  2. 总条数更新为匹配数量
  3. 清空搜索后列表恢复
- **验收方式**：自动化测试

#### 断言 3：切换视图保留筛选条件

- **验证对象**：XqViewSwitch 与筛选联动
- **前置条件**：用户已选择来源「展会」并搜索「血球」
- **操作步骤**：
  1. 点击「列表视图」按钮
  2. 点击「卡片视图」按钮
- **预期结果**：
  1. 来源筛选仍为「展会」
  2. 搜索框仍为「血球」
  3. 列表数据保持一致
- **验收方式**：自动化测试

### 11.2 功能：线索录入与查重

#### 断言 4：必填校验阻断保存

- **验证对象**：线索录入弹窗
- **前置条件**：用户点击「录入线索」打开弹窗
- **操作步骤**：
  1. 不填写任何字段
  2. 点击「保存」
- **预期结果**：
  1. 客户名称、线索摘要字段边框变红
  2. 字段下方显示「此项为必填项」
  3. createLead 接口未被调用
- **验收方式**：自动化测试

#### 断言 5：命中生效意向时阻断录入

- **验证对象**：查重逻辑
- **前置条件**：系统中已存在「昆明市第一人民医院 检验科 血液分析」的生效意向
- **操作步骤**：
  1. 打开录入弹窗
  2. 填写客户名称「昆明市第一人民医院」、科室「检验科」、产品线「血液分析」、摘要「血球仪升级」
  3. 点击保存
- **预期结果**：
  1. 出现 XqConfirmDialog，文案包含「该项目已有授权意向，不可重复录入线索」
  2. 线索未被保存
  3. 提供「查看意向详情」入口
- **验收方式**：自动化测试

#### 断言 6：正常录入后状态为待分配

- **验证对象**：新建线索流程
- **前置条件**：用户为销售代表，无冲突数据
- **操作步骤**：
  1. 打开录入弹窗
  2. 填写客户名称、科室、产品线、摘要、来源
  3. 点击保存
- **预期结果**：
  1. 弹窗关闭
  2. 列表顶部出现新线索
  3. 线索状态为「待分配」
  4. 系统提示「线索保存成功」
- **验收方式**：自动化测试

### 11.3 功能：线索分配

#### 断言 7：分配后状态变为跟进中

- **验证对象**：分配功能
- **前置条件**：用户为区域总监，存在待分配线索
- **操作步骤**：
  1. 在公海池选择一条待分配线索
  2. 点击「分配」
  3. 选择归属人、跟进时限 7 天
  4. 点击确认
- **预期结果**：
  1. 弹窗关闭
  2. 线索状态变为「跟进中」
  3. 归属人显示为所选人员
  4. 归属人收到消息通知
- **验收方式**：自动化测试

#### 断言 8：销售代表无权限分配

- **验证对象**：权限控制
- **前置条件**：用户为销售代表，存在待分配线索
- **操作步骤**：
  1. 进入线索列表
  2. 查看待分配线索操作列
- **预期结果**：
  1. 操作列不显示「分配」按钮
  2. 点击卡片不出现分配入口
  3. 直接调用 assignLead 接口返回 403
- **验收方式**：自动化测试

#### 断言 9：领取公海线索后进入我的线索

- **验证对象**：公海池领取
- **前置条件**：用户为销售代表，公海池存在待分配线索且开启自主领取
- **操作步骤**：
  1. 切换到「公海池」Tab
  2. 点击线索卡片上的「领取」
- **预期结果**：
  1. 线索从公海池消失
  2. 切换到「我的线索」Tab 可见该线索
  3. 线索状态变为「跟进中」
- **验收方式**：自动化测试

### 11.4 功能：线索跟进

#### 断言 10：跟进后更新下次跟进时间

- **验证对象**：跟进录入
- **前置条件**：用户为线索归属人，线索状态为跟进中
- **操作步骤**：
  1. 进入线索详情
  2. 点击「录入跟进」
  3. 选择跟进方式「电话」，填写内容，选择下次跟进时间
  4. 保存
- **预期结果**：
  1. 详情页时间线新增一条记录
  2. 下次跟进时间显示为所选时间
  3. 跟进倒计时重置
- **验收方式**：自动化测试

#### 断言 11：未填写跟进内容阻断保存

- **验证对象**：跟进录入表单
- **前置条件**：用户打开跟进录入弹窗
- **操作步骤**：
  1. 只选择跟进方式
  2. 不填写跟进内容
  3. 点击保存
- **预期结果**：
  1. 跟进内容字段红框
  2. 提示「此项为必填项」
  3. addLeadFollow 接口未被调用
- **验收方式**：自动化测试

#### 断言 12：非归属人无法录入跟进

- **验证对象**：数据权限
- **前置条件**：用户不是某条跟进中线索的归属人或协同人
- **操作步骤**：
  1. 进入该线索详情
  2. 查看操作按钮
- **预期结果**：
  1. 「录入跟进」按钮不显示或 disabled
  2. 直接调用 addLeadFollow 接口返回 403
- **验收方式**：自动化测试

### 11.5 功能：转意向

#### 断言 13：跟进中线索可转意向

- **验证对象**：转意向功能
- **前置条件**：线索状态为跟进中，且至少存在一条跟进记录
- **操作步骤**：
  1. 进入线索详情
  2. 点击「转意向」
  3. 填写关键联系人、联系方式、预算金额、预计成交时间、业务类型
  4. 提交
- **预期结果**：
  1. 弹窗关闭
  2. 线索状态变为「已转意向」
  3. 生成意向编号并可跳转
- **验收方式**：自动化测试

#### 断言 14：待分配线索不能转意向

- **验证对象**：状态驱动操作
- **前置条件**：存在状态为待分配的线索
- **操作步骤**：
  1. 进入该线索详情
  2. 查看操作按钮
- **预期结果**：
  1. 不显示「转意向」按钮
  2. 调用 convertLeadToIntention 接口返回业务错误「仅跟进中线索可转意向」
- **验收方式**：自动化测试

#### 断言 15：转意向后不可重复转意向

- **验证对象**：转意向幂等性
- **前置条件**：线索状态为已转意向
- **操作步骤**：
  1. 进入线索详情
  2. 查看操作按钮
- **预期结果**：
  1. 不显示「转意向」按钮
  2. 调用 convertLeadToIntention 接口返回错误「线索已转意向」
- **验收方式**：自动化测试

### 11.6 功能：废弃与恢复

#### 断言 16：废弃后状态变为已废弃

- **验证对象**：废弃功能
- **前置条件**：用户有废弃权限，线索状态为跟进中
- **操作步骤**：
  1. 进入线索详情
  2. 点击「废弃」
  3. 填写废弃原因
  4. 确认
- **预期结果**：
  1. 弹窗关闭
  2. 线索状态变为「已废弃」
  3. 列表中显示已废弃标签
- **验收方式**：自动化测试

#### 断言 17：普通角色无法恢复废弃线索

- **验证对象**：恢复权限
- **前置条件**：用户为销售代表，存在已废弃线索
- **操作步骤**：
  1. 进入已废弃线索详情
  2. 查看操作按钮
- **预期结果**：
  1. 不显示「恢复」按钮
  2. 调用 recoverLead 接口返回 403
- **验收方式**：自动化测试

#### 断言 18：管理员恢复后回到待分配

- **验证对象**：恢复功能
- **前置条件**：用户为渠道管理员，存在 7 天内废弃的线索
- **操作步骤**：
  1. 进入已废弃线索详情
  2. 点击「恢复」
  3. 填写恢复原因
  4. 确认
- **预期结果**：
  1. 线索状态变为「待分配」
  2. 归属人清空
  3. 记录恢复日志
- **验收方式**：自动化测试

### 11.7 功能：响应式布局

#### 断言 19：PC 端默认卡片视图且 4 列

- **验证对象**：PC 端线索列表
- **前置条件**：使用 1440px 宽度浏览器，线索总数 <=20
- **操作步骤**：
  1. 进入线索列表
  2. 检查卡片视图
- **预期结果**：
  1. 默认显示卡片视图
  2. 每行 4 张卡片
  3. 卡片包含状态标签、客户、科室、摘要、来源、倒计时、操作
- **验收方式**：截图对比

#### 断言 20：移动端列表底部按钮固定

- **验证对象**：移动端线索列表
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 进入线索列表
  2. 向下滚动列表
- **预期结果**：
  1. 「+ 录入线索」按钮固定在屏幕右下角
  2. 按钮不随列表滚动消失
  3. 点击按钮正常打开录入弹窗
- **验收方式**：截图对比 / 自动化测试

#### 断言 21：移动端筛选使用底部抽屉

- **验证对象**：移动端筛选
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 点击搜索右侧「筛选」入口
  2. 选择来源「展会」
  3. 点击确认
- **预期结果**：
  1. 底部抽屉展开
  2. 确认后列表按「展会」过滤
  3. 抽屉关闭动画正常
- **验收方式**：自动化测试

---

## 12. 关联与扩展

### 12.1 与其他模块的关联

| 关联模块 | 关联点 | 说明 |
|---|---|---|
| 意向管理 | 线索转意向 | 跟进中线索补充信息后生成意向报备单，进入审批流；线索与意向双向关联 |
| 客户 360° | 客户关联 | 客户名称支持从 HCO 主数据选择；详情页客户名称可跳转客户 360° |
| 消息中心 | 通知推送 | 新线索录入、分配、回收、转意向、废弃均通过消息中心通知相关人员 |
| 日历模块 | 日程同步 | 下次跟进时间可自动同步到归属人日历 |
| 任务模块 | 跟进任务 | 分配线索时可生成一条「X 天内跟进」任务 |
| 应用中心 | 应用入口 | 线索管理作为业务管理类应用，通过应用中心进入 |

### 12.2 易遗漏点

1. **查重规则需要可配置**：客户+科室+产品线的权重、相似度阈值应由后台规则引擎配置，避免硬编码
2. **跟进时限默认值**：默认 7 天需在后台配置，不同区域/产品线可独立配置
3. **消息通知模板**：分配、回收、转意向等通知文案需在消息中心模板中维护，支持站内信/企微/钉钉
4. **数据范围递归**：区域总监查看「本大区」时，需要递归包含下属所有省区/城市，不能仅匹配当前层级
5. **移动端详情返回**：移动端详情页顶部必须有返回按钮，返回后列表页需保留之前的 Tab、搜索、筛选和分页位置
6. **转意向字段映射**：转意向时客户/科室/产品线/摘要必须自动带入，且允许修改；修改后需同步回写线索
7. **废弃线索的可见性**：已废弃线索对原归属人仍可见，但操作按钮隐藏，避免数据消失导致困惑
8. **回收前的预警**：超期前 1 天必须发送预警通知，预警内容需包含线索编号、客户名称、剩余时间
9. **统计卡片权限**：KPI 卡片数据需按当前用户数据范围聚合，管理员看全部，销售代表只看本人/本区域
10. **附件安全**：跟进附件需走文件中心上传，限制格式与大小，禁止可执行文件

### 12.3 待确认问题

- 是否允许经销商账号查看其他经销商录入的公海线索？
- 自动分配规则是否需要第一期实现，还是仅支持手动分配？
- 转意向被驳回后，线索是否自动回到「跟进中」状态，还是保持不变？

---

## 附录：自检记录

按《鑫渠应用文档验收标准.md》v1.1 进行自检：

| 维度 | 检查项要点 | 是否通过 | 说明 |
|---|---|---|---|
| 1. 文档基础信息 | 标题、版本号、日期、原型版本、规范版本、适用范围、版本记录表 | 是 | 文档顶部已完整包含 |
| 2. AI 可执行机器指令块 | 技术栈、组件库、样式变量、断点、权限模型、规范引用准确 | 是 | 第 2 节已包含 |
| 3. 模块定位与目标 | 一句话定位、业务痛点、量化价值、所属业务域 | 是 | 第 1 节已包含 |
| 4. 业务场景与角色 | 角色清单、角色职责、典型业务场景 | 是 | 第 2 节已包含 |
| 5. 核心功能清单 | 功能列表完整、优先级、功能说明、与原型对应 | 是 | 第 3 节已包含 10 项 |
| 6. 业务流程 | 流程数量、步骤清晰、异常分支、状态流转 | 是 | 第 4 节包含 4 个流程 |
| 7. 页面结构与布局 | 五层架构、页面清单、布局描述、组件说明、视图切换 | 是 | 第 5、6 节已包含 |
| 8. 核心字段说明 | 字段表格、字段完整性、字段编码、字段约束、字段权限 | 是 | 第 7 节包含 20+ 字段 |
| 9. 交互设计要点 | 搜索筛选、操作反馈、空态、快捷操作、异常处理 | 是 | 第 8 节已包含 |
| 10. 响应式与双端设计 | PC/移动端布局、断点规则、手势交互 | 是 | 第 9 节已包含 |
| 11. 接口契约 | 接口清单、入参、出参、场景对应 | 是 | 第 10 节包含 11 个接口 |
| 12. 验收断言 | 每个核心功能至少 3 条、可验证、覆盖完整 | 是 | 第 11 节 21 条断言 |
| 13. 权限与数据范围 | 角色权限矩阵、数据范围、字段权限、数据脱敏 | 是 | 第 2 节已包含 |
| 14. 关联与扩展 | 与其他模块关联、易遗漏点 | 是 | 第 12 节已包含 10 个易遗漏点 |

**总分**：170 / 170  
**结论**：优秀，可直接进入开发  
**自检人**：AI 可执行文档工程师  
**自检日期**：2026-07-29

---

*本文档是鑫渠 CRM 线索管理模块的 AI 可执行设计文档，所有开发工作必须严格遵循。*
