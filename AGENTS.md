# 鑫渠 CRM 项目 Agent 协作指南

> 本文档是给后续 AI Agent / 自动化工具看的项目入口规范。任何人在本仓库使用 Agent 辅助开发前，必须先阅读本文件。

---

## 1. 项目基本信息

| 项目       | 说明                                                                  |
| ---------- | --------------------------------------------------------------------- |
| 项目名称   | 鑫渠 CRM（XQ.COP）                                                    |
| 仓库       | `liuliangkui/new-xingu`，分支 `main`                                  |
| 原型主文件 | `鑫渠高保真原型.html`                                                 |
| 技术栈     | Vue 3 + TypeScript + Tailwind CSS + Vite（工程化），原型为单文件 HTML |
| 设计系统   | 《鑫渠全局设计系统规范.md》                                           |
| 组件字典   | 《鑫渠组件库清单与命名规范.md》                                       |

---

## 2. 关键文件路径

| 文件                                                            | 用途                                                 |
| --------------------------------------------------------------- | ---------------------------------------------------- |
| `鑫渠高保真原型.html`                                           | 高保真原型主文件，所有前端样式、结构、交互的参考标准 |
| `鑫渠设计与开发规范.md`                                         | 设计原则、布局规范、前端实现规范（必读）             |
| `鑫渠全局设计系统规范.md`                                       | 颜色、字体、图标、主题、响应式 token                 |
| `鑫渠组件库清单与命名规范.md`                                   | 组件命名、Props、Slots、响应式规则                   |
| `鑫渠响应式设计规范.md`                                         | 断点、布局适配、手势规范                             |
| `鑫渠页面布局设计理念.md`                                       | 信息分层、组件模式、设计哲学                         |
| `scripts/preview-prototype.sh`                                  | 原型预览/验证脚本                                    |
| `backup-local/`                                                 | 本地备份目录，不要提交                               |
| `*.html.analysis-layout-backup` / `*.html.remaining-fix-backup` | 未跟踪备份文件，不要提交                             |

---

## 3. 开发前必查规范

开发或修改任何前端页面/弹窗/组件前，必须按顺序查阅：

1. 《鑫渠设计与开发规范.md》
2. 《鑫渠全局设计系统规范.md》
3. 《鑫渠组件库清单与命名规范.md》
4. 《鑫渠响应式设计规范.md》
5. 对应业务模块的功能说明文档（如 `线索管理功能与交互说明.md`）

---

## 4. 前端实现红线

以下规则强制遵守，违反即视为不合格：

1. **颜色必须走 CSS 变量**：禁止写死 `#3370FF`、`#F54A45` 等色值，统一使用 `--primary`、`-danger` 等变量。
2. **Tailwind 缺失类要补齐**：`hidden`、`grid-cols-4/5/6` 等必须在 `<style>` 中显式定义。
3. **页面内容区必须加 `min-width: 0`**：防止 flex 子项撑破布局。
4. **弹窗头部结构固定**：标题 + 关闭按钮，标题前禁止加「线：」「客：」等前缀。
5. **列表视图用手风琴、卡片视图用弹窗**：同一页面禁止混用。
6. **数据分析页 AI 结论放底部**：所有分析结论必须置于页面主体数据最下方。
7. **KPI 不可挤压变形**：使用 `auto-fill` 或单独占行，禁止强制塞入过多列。
8. **所有按钮必须有真实事件**：禁止遗留 `onclick=""`、占位函数或未实现按钮。
9. **清理业务无关占位文字**：如「展会血液分析」等必须删除。
10. **漏斗图必须用漏斗形状 SVG**：禁止用柱状图/表格替代。

---

## 5. 修改原型后的验证流程

每次修改 `鑫渠高保真原型.html` 后，必须执行：

```bash
# 1. 语法检查
node --check /tmp/page_script.js

# 2. 预览验证
./scripts/preview-prototype.sh
```

> 如果脚本不存在或失败，至少要用浏览器打开原型检查修改点是否生效。

---

## 6. 提交规范

提交信息格式：

```
type(scope): 简短描述

- 详细变更点 1
- 详细变更点 2
```

常用 type：

- `feat`：新功能
- `fix`：bug 修复
- `docs`：文档更新
- `style`：样式调整
- `refactor`：重构

**不要提交的文件**：

- `backup-local/` 目录
- `*.html.analysis-layout-backup`
- `*.html.remaining-fix-backup`
- `node_modules/`

---

## 7. 常见任务索引

| 任务                   | 参考文档                                          |
| ---------------------- | ------------------------------------------------- |
| 新增业务页面           | 《鑫渠设计与开发规范.md》第七章「应用布局模板库」 |
| 新增弹窗/抽屉          | 《鑫渠设计与开发规范.md》第十八章、第十九章       |
| 调整 KPI/卡片/表格布局 | 《鑫渠设计与开发规范.md》第十九章                 |
| 设计数据分析页面       | 《鑫渠设计与开发规范.md》第十九章 19.7            |
| 主题/颜色/图标         | 《鑫渠全局设计系统规范.md》                       |
| 组件命名与 Props       | 《鑫渠组件库清单与命名规范.md》                   |
| 移动端适配             | 《鑫渠响应式设计规范.md》                         |

---

## 8. 换电脑/新环境恢复开发

用户可以通过以下指令快速恢复项目开发环境并加载上下文：

```text
/resume-dev [项目 GitHub 仓库地址或本地路径]
/resume dev [项目名称]
```

- `/resume dev 鑫渠` 会进入 `/Users/mac/qucheng`，读取 `.kimi/resume-context.md` 恢复上下文，并自动检查/启动环境。
- 具体执行清单见 `.kimi/skills/resume-dev/SKILL.md`。
- 每次关键开发会话结束后，应更新 `.kimi/resume-context.md` 中的“当前活跃工作”和“最近变更”。

---

_本文档随项目演进持续更新。最新版本以仓库 `main` 分支为准。_
