# 鑫渠组件库清单与命名规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **适用范围**：鑫渠 CRM 全部前端页面开发  
> **文档性质**：AI 开发时的组件字典，禁止私自定义组件名

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：定义鑫渠 CRM 通用组件清单、命名规则、Props/Events/Slots、响应式适配规则 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、命名规则

1. **组件名统一使用大驼峰（PascalCase）**，前缀为 `Xq`。
2. **业务组件**在 `Xq` 后接业务语义，如 `XqIntentionCard`。
3. **基础组件**在 `Xq` 后接基础语义，如 `XqSearchBar`。
4. **禁止**使用 `My`、`Custom`、`Temp` 等无意义前缀。
5. **同一组件在不同模块中禁止改名**，必须复用本清单中的名称。

---

## 二、布局组件

### 2.1 XqPageLayout 页面布局

**用途**：统一所有列表型页面的五层结构容器。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | String | - | 页面标题 |
| showBack | Boolean | false | 是否显示返回按钮 |
| showStats | Boolean | true | 是否显示统计卡片区 |
| showFilter | Boolean | true | 是否显示筛选区 |
| padding | String | '16px' | 页面内边距 |

**Slots**：

| 插槽名 | 说明 |
|---|---|
| stats | 统计卡片区内容 |
| actions | 标题右侧操作区 |
| filter | 筛选区内容 |
| content | 列表/卡片内容 |
| footer | 底部分页区 |

**响应式规则**：
- PC 端：正常五层结构。
- 移动端：title 区域固定顶部，content 区可滚动。

---

### 2.2 XqTwoColumnLayout 双栏布局

**用途**：左侧列表、右侧详情的双态布局（如客户 360°）。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| leftWidth | String | '360px' | PC 端左侧宽度 |
| showRight | Boolean | false | 是否显示右侧详情 |
| rightType | String | 'drawer' | 右侧呈现方式：drawer / page |

**响应式规则**：
- PC 端：左侧固定宽度，右侧填充剩余空间。
- 移动端：左侧全屏列表，点击后进入详情页（rightType='page'）。

---

### 2.3 XqDrawer 抽屉面板

**用途**：从右侧滑出的详情/编辑面板。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| visible | Boolean | false | 是否显示 |
| title | String | - | 抽屉标题 |
| width | String | '720px' | PC 端宽度 |
| maskClosable | Boolean | true | 点击遮罩是否关闭 |
| showFooter | Boolean | true | 是否显示底部操作区 |

**响应式规则**：
- PC 端：右侧抽屉，宽度 720px / 560px / 480px 三档可选。
- 移动端：全屏抽屉，底部固定操作按钮。

---

## 三、数据展示组件

### 3.1 XqKpiCard 统计指标卡片

**用途**：页面顶部统计数字展示。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | String | - | 指标名称 |
| value | Number/String | - | 指标值 |
| unit | String | '' | 单位 |
| trend | String | '' | 趋势：up / down / flat |
| color | String | 'primary' | 主题色：primary / success / warning / danger |
| clickable | Boolean | false | 是否可点击切换筛选 |

**响应式规则**：
- PC 端：横向排列 3-5 个，宽度均分。
- 移动端：横向滚动，每个卡片最小宽度 120px。

---

### 3.2 XqDataTable 数据表格

**用途**：列表视图下的表格展示。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| columns | Array | - | 列配置 |
| dataSource | Array | - | 数据源 |
| loading | Boolean | false | 加载状态 |
| pagination | Object | - | 分页配置 |
| rowKey | String | 'id' | 行唯一键 |
| rowSelection | Object | null | 多选配置 |

**列配置字段**：

| 字段 | 类型 | 说明 |
|---|---|---|
| title | String | 列标题 |
| dataIndex | String | 字段编码 |
| width | String/Number | 列宽 |
| fixed | String | 固定列：left / right |
| render | Function | 自定义渲染函数 |
| mobileHidden | Boolean | 移动端是否隐藏 |

**响应式规则**：
- PC 端：完整列展示，支持固定列、横向滚动。
- 移动端：默认切换为卡片视图，如需表格则横向滚动。

---

### 3.3 XqCardGrid 卡片网格

**用途**：卡片视图下的网格布局。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| dataSource | Array | - | 数据源 |
| columns | Number | 4 | PC 端列数：2/3/4 |
| gap | String | '16px' | 间距 |
| renderItem | Function | - | 卡片渲染函数 |

**响应式规则**：
- >=1440px：4 列
- 1024px-1439px：3 列
- 768px-1023px：2 列
- <768px：1 列

---

### 3.4 XqStatusBadge 状态标签

**用途**：状态、类型等标签展示。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| status | String/Number | - | 状态值 |
| statusMap | Object | - | 状态映射表 {value: {text, color}} |
| size | String | 'default' | 尺寸：small / default / large |

---

### 3.5 XqEmptyState 空态

**用途**：无数据、无搜索结果、无权限时的空态。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| type | String | 'empty' | 类型：empty / search / permission / error |
| title | String | - | 主标题 |
| description | String | - | 描述 |
| action | Object | null | 操作按钮 {text, onClick} |

---

## 四、表单组件

### 4.1 XqSearchBar 搜索栏

**用途**：顶部关键词搜索。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| v-model | String | - | 绑定值 |
| placeholder | String | '搜索' | 占位文案 |
| allowClear | Boolean | true | 允许清空 |
| pinyinSearch | Boolean | false | 是否支持拼音首字母 |
| width | String | '240px' | PC 端宽度 |

**Events**：

| 事件名 | 说明 |
|---|---|
| search | 回车或点击搜索时触发 |
| reset | 清空时触发 |

**响应式规则**：
- PC 端：固定宽度 240px。
- 移动端：宽度 100%，吸顶显示。

---

### 4.2 XqFilterBar 筛选栏

**用途**：多条件筛选组合。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| filters | Array | - | 筛选条件配置 |
| values | Object | - | 当前筛选值 |
| showReset | Boolean | true | 显示重置按钮 |
| maxVisible | Number | 5 | PC 端平铺最大筛选数 |

**响应式规则**：
- PC 端：筛选条件横向平铺，超出折叠到「更多」。
- 移动端：筛选入口显示已选数量，点击弹出底部筛选面板。

---

### 4.3 XqFormDrawer 表单抽屉

**用途**：新建/编辑表单容器。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| visible | Boolean | false | 是否显示 |
| title | String | - | 标题 |
| width | String | '560px' | PC 端宽度 |
| loading | Boolean | false | 保存 loading |
| okText | String | '保存' | 确认按钮文字 |
| cancelText | String | '取消' | 取消按钮文字 |

**响应式规则**：
- PC 端：右侧抽屉 560px/720px。
- 移动端：全屏页面，底部固定保存按钮。

---

## 五、导航组件

### 5.1 XqNavTabs 导航标签

**用途**：页面内分类 Tab 切换。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| tabs | Array | - | 标签列表 [{key, label, count}] |
| activeKey | String/Number | - | 当前激活 |
| type | String | 'line' | 样式：line / card / pill |
| showCount | Boolean | true | 是否显示数量角标 |

---

### 5.2 XqViewSwitch 视图切换

**用途**：列表/卡片视图切换。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| value | String | 'card' | 当前视图：card / list |
| options | Array | - | 选项配置 |

---

### 5.3 XqBackButton 返回按钮

**用途**：详情页返回列表。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| text | String | '返回' | 按钮文字 |
| onBack | Function | - | 返回回调 |

---

## 六、反馈组件

### 6.1 XqToast 轻提示

**用途**：操作成功/失败的轻量提示。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| message | String | - | 提示内容 |
| type | String | 'info' | 类型：success / error / warning / info |
| duration | Number | 3000 | 显示时长（毫秒） |

---

### 6.2 XqConfirmDialog 确认弹窗

**用途**：删除、提交等二次确认。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | String | - | 标题 |
| content | String | - | 内容 |
| okText | String | '确认' | 确认按钮 |
| cancelText | String | '取消' | 取消按钮 |
| dangerous | Boolean | false | 危险操作（红色确认按钮） |

---

## 七、业务组件

### 7.1 XqIntentionCard 意向卡片

**用途**：意向列表卡片视图。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| data | Object | - | 意向数据对象 |
| showActions | Boolean | true | 是否显示操作按钮 |

**操作按钮按状态显示**：

| 状态 | 显示按钮 |
|---|---|
| 草稿 | 编辑、删除、提交 |
| 审批中 | 审批、查看 |
| 生效中 | 跟进、查看 |
| 被驳回 | 编辑、重新提交 |
| 已结束 | 查看 |

---

### 7.2 XqLeadCard 线索卡片

**用途**：线索列表卡片视图。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| data | Object | - | 线索数据对象 |

**操作按钮按状态显示**：

| 状态 | 显示按钮 |
|---|---|
| 待分配 | 分配、领取、废弃 |
| 跟进中 | 跟进、转意向、释放 |
| 已转意向 | 查看 |
| 已废弃 | 查看、恢复 |

---

### 7.3 XqCustomerCard 客户卡片

**用途**：客户 360° 列表卡片视图。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| data | Object | - | 客户数据对象 |

**必显字段**：医院名称、等级、区域、健康度、科室数、装机数、在跟意向数、负责人。

---

### 7.4 XqTimeline 时间线

**用途**：审批日志、客户动态、线索跟进记录。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| data | Array | - | 时间线数据 |
| mode | String | 'left' | 布局：left / alternate |

**数据字段**：

| 字段 | 说明 |
|---|---|
| time | 时间 |
| title | 标题 |
| content | 内容 |
| operator | 操作人 |
| status | 状态标签 |

---

### 7.5 XqApprovalPanel 审批面板

**用途**：审批操作区，统一审批通过/驳回/转交/评论。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| record | Object | - | 审批记录 |
| actions | Array | - | 可执行操作 |
| requireComment | Boolean | true | 是否强制填写意见 |

---

### 7.6 XqCalendar 日历组件

**用途**：工作台日历模块。

**Props**：

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| events | Array | - | 日程数据 |
| view | String | 'month' | 视图：month / week / day |
| colorTags | Array | - | 颜色分类标签 |

---

## 八、图标规范

1. 所有图标必须使用 SVG 或 Iconfont，禁止引用 PNG/JPG。
2. 图标组件名统一为 `XqIcon{name}`，如 `XqIconSearch`、`XqIconPlus`。
3. 图标尺寸使用 4 的倍数：12px、16px、20px、24px、32px。
4. 图标颜色继承当前文字颜色，支持通过 CSS `color` 变量变色。

---

## 九、组件使用红线

1. 禁止私自创建本清单之外的同名组件。
2. 禁止在业务代码中直接写 HTML 原生标签替代封装组件（特殊情况需说明）。
3. 所有组件必须支持主题切换（浅色/深色）。
4. 所有组件必须支持字号放大（跟随系统或用户设置）。
5. 所有组件在移动端不能出现按钮换行、内容遮挡、横向溢出等问题。

---

## 十、相关文档索引

- 《AI可执行设计文档编写规范.md》
- 《鑫渠响应式设计规范.md》
- 《鑫渠全局设计系统规范.md》
- 《鑫渠系统技术实现白皮书.html》

---

*本清单是鑫渠 CRM 前端组件的唯一命名字典，所有 AI 开发任务必须优先查询本清单。*
