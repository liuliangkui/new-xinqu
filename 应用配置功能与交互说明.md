# 应用配置功能与交互说明

> **文档版本**：v1.0
> **更新日期**：2026-07-29
> **对应原型**：《鑫渠高保真原型.html》v1.8.0
> **适用范围**：鑫渠 CRM 系统管理员、应用配置专员、业务架构师、研发实施工程师

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：应用配置模块定位、功能清单、业务流程、页面结构、字段说明、权限设计、关联关系与待补充事项 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、模块定位与目标

### 1.1 模块定位

应用配置是鑫渠 CRM 的**应用级元数据管理中心**，支撑「一个平台、多套业务应用、千企千面」的扩展模式。它以「业务应用」为配置单元，对字段、视图、流程、标签、权限、数据字典、集成接口等进行统一建模，实现业务能力的按需装配、版本受控与租户级隔离。**应用配置属于系统管理域。**

在鑫渠 v1.8.0 中，应用配置是以下能力的基础底座：

- 经销商协同应用：渠道授权规则、区域保护策略、返利计算模型、库存共享视图、任务协同看板。
- 合规风控应用：拜访合规校验、学术活动审批、样品全生命周期、飞检抽查任务、敏感词审计规则。
- 目标绩效应用：目标拆解模板、达成跟踪报表、同比环比计算、未达标预警阈值、多维度下钻路径。
- 经营驾驶舱应用：高管指标体系、实时刷新策略、异常预警推送、AI 问答入口预留。
- 后台设置应用：组织用户体系、角色权限矩阵、数据字典、日志审计、集成配置。
- 流程设计器应用：可视化拖拽画布、节点类型库、条件分支规则、版本管理与审批中心联动。

### 1.2 核心目标

| 目标 | 说明 |
|---|---|
| 降低交付成本 | 通过配置化替代硬编码，减少个性化需求二次开发量 |
| 支撑业务自治 | 允许业务管理员在权限范围内调整字段、视图、流程，无需发布版本 |
| 保障多租户隔离 | 每个租户可独立配置应用参数，互不污染 |
| 实现版本可追溯 | 应用配置的每次变更留痕，支持版本对比与一键回滚 |
| 打通审批与流程 | 应用配置中的流程定义可直接驱动审批中心运行 |

### 1.3 解决的问题与带来的价值

- **解决什么问题**
  - 业务规则分散在代码与数据库脚本中，调整周期长、风险高。
  - 不同经销商/产品线对字段、流程、报表要求差异大，难以统一。
  - 流程变更后无法追溯影响面，审批节点、表单字段、权限范围容易脱节。
  - 系统管理员缺乏统一的配置入口，常需在多个菜单间切换。

- **带来什么价值**
  - 将「改代码」转化为「改配置」，业务迭代周期从天级缩短到小时级。
  - 通过模板克隆、版本管理、沙箱预览，降低配置错误带来的生产事故。
  - 为经营驾驶舱、AI 问答等高级能力提供统一的元数据服务。

---

## 二、业务场景与角色

### 2.1 使用角色

| 角色 | 主要职责 | 高频操作 |
|---|---|---|
| **系统管理员** | 负责全局应用启停、租户级配置、集成配置、日志审计 | 新建应用、发布版本、配置集成网关、查看操作日志 |
| **应用配置专员** | 负责具体业务应用的字段、视图、流程、标签、权限配置 | 拖拽表单、配置流程节点、设置视图列、维护数据字典 |
| **业务架构师** | 制定应用配置规范，审批重大配置变更 | 评审配置方案、定义主数据标准、设计权限矩阵 |
| **安全合规专员** | 负责敏感字段、脱敏规则、审计策略、敏感词库 | 配置字段权限、设置脱敏规则、维护敏感词表 |
| **研发实施工程师** | 负责配置无法覆盖的扩展点开发、接口对接、问题排查 | 查看配置元数据、开发自定义节点、调试流程 |

### 2.2 典型场景（四要素格式）

#### 场景 1：上线新经销商协同应用

| 要素 | 内容 |
|---|---|
| **场景名称** | 上线新经销商协同应用 |
| **触发条件** | 新签约经销商需纳入鑫渠 CRM 协同管理，要求独立的渠道授权、区域保护、返利计算与库存共享视图。 |
| **操作角色** | 系统管理员、应用配置专员 |
| **预期结果** | 在应用配置中心启用「经销商协同」应用，完成授权规则、区域保护策略、返利模板、库存共享字段与视图的配置，并发布到指定租户。 |

#### 场景 2：调整拜访合规校验规则

| 要素 | 内容 |
|---|---|
| **场景名称** | 调整拜访合规校验规则 |
| **触发条件** | 合规部门要求加强外勤拜访的真实性校验，例如增加定位偏差阈值、照片水印、拜访时长下限。 |
| **操作角色** | 应用配置专员、安全合规专员 |
| **预期结果** | 在「合规风控」应用中修改拜访校验规则参数，配置字段级权限与异常触发流程，保存后即时生效或按灰度策略生效。 |

#### 场景 3：为目标绩效应用配置下钻分析

| 要素 | 内容 |
|---|---|
| **场景名称** | 为目标绩效应用配置下钻分析 |
| **触发条件** | 销售总监希望从区域维度下钻到团队、个人、产品、客户，查看目标达成与同比环比。 |
| **操作角色** | 应用配置专员、业务架构师 |
| **预期结果** | 在「目标绩效」应用中配置指标模型、维度层级、下钻路径、预警阈值与图表视图，供经营驾驶舱调用。 |

#### 场景 4：流程设计器新增学术活动审批流程

| 要素 | 内容 |
|---|---|
| **场景名称** | 流程设计器新增学术活动审批流程 |
| **触发条件** | 市场部发起学术会议/赞助活动，需要多级审批与费用预算控制。 |
| **操作角色** | 应用配置专员、业务架构师 |
| **预期结果** | 在流程设计器中拖拽创建「学术活动审批」流程，配置发起人、部门负责人、合规专员、财务审批节点及条件分支，发布后与审批中心联动。 |

#### 场景 5：经营驾驶舱预留 AI 问答入口

| 要素 | 内容 |
|---|---|
| **场景名称** | 经营驾驶舱预留 AI 问答入口 |
| **触发条件** | 高管希望在驾驶舱通过自然语言查询关键指标，系统需预留 AI 问答组件与元数据映射。 |
| **操作角色** | 系统管理员、应用配置专员 |
| **预期结果** | 在「经营驾驶舱」应用中配置 AI 问答入口的可见范围、可查询指标清单、安全脱敏策略与兜底话术。 |

---

## 三、核心功能清单

| 序号 | 功能名称 | 优先级 | 功能说明 | 原型对应 |
|---|---|---|---|---|
| 1 | 应用市场管理 | 必要 | 维护业务应用目录，支持启用/停用、版本切换、依赖校验与租户分配。 | 应用市场页 |
| 2 | 字段设计器 | 必要 | 按应用定义对象字段，支持文本、数字、日期、枚举、关联、公式等类型，配置必填、默认值、校验规则。 | 字段设计页 |
| 3 | 视图设计器 | 必要 | 按应用配置列表页、详情页、卡片视图的展示列、排序、分组、快速筛选与按钮操作。 | 视图设计页 |
| 4 | 流程设计器 | 必要 | 可视化拖拽绘制审批/业务流程，支持多种节点类型、条件分支、会签、子流程、版本管理与发布。 | 流程画布页 |
| 5 | 标签与分类管理 | 重要 | 维护应用级标签组、分类树、颜色标识，用于业务对象打标与快速筛选。 | 标签管理页 |
| 6 | 权限模型配置 | 必要 | 按应用配置角色权限矩阵、数据可见范围、字段级读写权限、按钮级操作权限。 | 权限配置页 |
| 7 | 数据字典管理 | 重要 | 维护应用级枚举、下拉选项、级联字典，支持版本控制与多语言。 | 数据字典页 |
| 8 | 集成配置管理 | 重要 | 配置应用与外部系统的接口、事件订阅、字段映射、转换规则与调用日志。 | 集成配置页 |
| 9 | 模板与克隆 | 重要 | 支持将已配置应用保存为模板，快速克隆到新租户或新环境，减少重复劳动。 | 模板库页 |
| 10 | 版本与发布管理 | 必要 | 对应用配置进行版本保存、对比、回滚、灰度发布与发布审批，确保变更可控。 | 版本管理页 |
| 11 | 配置变更审计 | 重要 | 记录谁在何时修改了哪项配置、修改前后值、操作 IP 与审批链路，支持导出。 | 审计日志页 |
| 12 | 沙箱预览 | 可选 | 在正式发布前模拟配置效果，支持以指定角色查看页面、流程与权限。 | 沙箱预览页 |

---

## 四、业务流程

### 4.1 流程 1：新建并发布一个业务应用

```
[创建应用] -> [配置基础信息] -> [设计字段] -> [设计视图] -> [设计流程] -> [配置权限] -> [沙箱预览] -> [发布审批] -> [正式发布]
```

1. 系统管理员进入「应用市场」，点击「新建应用」，填写应用编码、名称、所属域、版本号、依赖应用。
2. 系统自动校验应用编码全局唯一，生成应用 ID 与默认租户隔离标识。
3. 应用配置专员进入字段设计器，按业务对象新增字段、设置类型/必填/默认值/校验规则。
4. 进入视图设计器，配置列表页、详情页、卡片视图、筛选条件与快捷操作按钮。
5. 进入流程设计器，拖拽节点绘制流程，配置条件分支、审批人、表单字段、抄送规则。
6. 进入权限配置页，设置角色权限矩阵、数据范围、字段级权限、按钮级权限。
7. 可选：进入沙箱预览，选择模拟角色与数据，验证页面、流程、权限效果。
8. 提交发布审批，由业务架构师或系统管理员审批通过。
9. 正式发布，系统自动生成运行时元数据缓存，并同步到相关租户。

**异常分支**：

- E1：应用编码重复 -> 提示「应用编码已存在，请重新输入」，返回步骤 1。
- E2：依赖应用未启用 -> 提示「请先启用依赖应用 X」，并阻断发布。
- E3：流程节点未配置审批人 -> 提示「节点 X 审批人不能为空」，返回流程设计器。
- E4：发布审批被驳回 -> 记录驳回意见，返回配置态，修改后重新提交。
- E5：沙箱预览发现权限漏洞 -> 返回权限配置页修正，重新预览。

### 4.2 流程 2：调整应用字段并灰度发布

```
[进入版本管理] -> [创建新版本] -> [修改字段] -> [配置变更说明] -> [提交审批] -> [灰度发布] -> [观察指标] -> [全量发布/回滚]
```

1. 应用配置专员在「版本管理」中为已有应用创建新版本，系统自动复制当前配置。
2. 在字段设计器中新增/修改/停用字段，系统提示影响面（已存在数据、已引用视图、已引用流程）。
3. 填写变更说明与风险提示，提交版本审批。
4. 审批通过后，选择灰度策略：按租户、按角色、按区域或按用户白名单。
5. 灰度发布生效，系统监控相关页面访问、流程发起、报错日志。
6. 观察期结束后，如无异常则全量发布；如异常则一键回滚到上一版本。

**异常分支**：

- E1：变更字段被流程/视图引用 -> 提示「该字段被 X 处引用，请先解除引用或同步调整」。
- E2：灰度期间报错率超过阈值 -> 自动触发告警，建议立即回滚。
- E3：回滚时目标版本数据已过期 -> 提示「目标版本备份已清理，无法回滚」，需手动修复。

### 4.3 流程 3：流程设计器创建学术活动审批流程并联动审批中心

```
[进入流程设计器] -> [选择应用] -> [拖拽绘制节点] -> [配置节点属性] -> [设置条件分支] -> [保存草稿] -> [版本校验] -> [发布流程] -> [审批中心生效]
```

1. 应用配置专员进入流程设计器，选择「合规风控」应用，点击「新建流程」。
2. 从左侧节点库拖拽开始节点、用户任务节点、会签节点、条件分支节点、子流程节点、结束节点到画布。
3. 配置每个节点的名称、审批人表达式、表单字段权限、操作按钮、超时提醒。
4. 设置条件分支规则，例如：预算金额 > 5 万需财务审批，> 10 万需总经理审批。
5. 保存为草稿，系统自动校验流程连通性、孤立节点、必填属性。
6. 校验通过后发布流程，流程版本写入审批中心流程引擎。
7. 用户在业务端发起学术活动申请时，审批中心按最新发布版本驱动审批。

**异常分支**：

- E1：流程存在孤立节点 -> 提示「节点 X 未连线」，无法保存。
- E2：审批人表达式解析失败 -> 提示「表达式 X 语法错误」并高亮显示。
- E3：发布时审批中心同步失败 -> 记录失败日志，支持重试或回退到草稿态。
- E4：运行中流程版本更新 -> 已发起实例按原版本执行，新实例按新版本执行。

---

## 五、页面结构与布局

### 5.1 整体架构

应用配置模块采用「应用选择器 + 功能导航 + 画布/表单工作区」的三栏结构：

```
┌─────────────────────────────────────────────────────────────────────┐
│ 鑫渠 CRM 应用配置中心                                                │
├──────────────┬──────────────────────────────────────────────────────┤
│              │  顶部：应用选择器 + 当前应用名称 + 版本号 + 发布状态   │
│  左侧导航栏  ├──────────────────────────────────────────────────────┤
│              │  顶部 Tab：字段 / 视图 / 流程 / 标签 / 权限 / 字典    │
│  应用市场    │  / 集成 / 版本 / 审计                                │
│  字段设计器  ├──────────────────────────────────────────────────────┤
│  视图设计器  │                                                      │
│  流程设计器  │  工作区：                                            │
│  标签管理    │  ┌────────────────────────────────────────────────┐ │
│  权限配置    │  │  配置画布 / 表单 / 表格 / 设计器                 │ │
│  数据字典    │  │                                                  │ │
│  集成配置    │  │  （根据当前 Tab 动态渲染）                       │ │
│  版本管理    │  └────────────────────────────────────────────────┘ │
│  审计日志    │                                                      │
│              │  底部：保存草稿 / 预览 / 提交审批 / 发布            │
└──────────────┴──────────────────────────────────────────────────────┘
```

### 5.2 五层架构描述

应用配置各子页面统一遵循五层信息架构：

| 层级 | 名称 | 作用 | 典型内容 |
|---|---|---|---|
| 第一层 | 全局导航层 | 明确当前所在应用与功能模块 | 应用选择器、面包屑、版本状态、发布状态 |
| 第二层 | 操作与视图层 | 快速切换工作视角并执行主操作 | 顶部 Tab、新建/导入/克隆按钮、视图切换 |
| 第三层 | 筛选与搜索层 | 在海量配置项中定位目标 | 关键字搜索、类型筛选、状态筛选、标签筛选 |
| 第四层 | 配置内容层 | 展示具体配置项，支持增删改查 | 字段列表、视图画布、流程图、权限矩阵 |
| 第五层 | 状态与行动层 | 反馈配置状态并提供下一步操作 | 保存、校验、预览、审批、发布、回滚 |

为与全系统页面清单保持一致，上述五层架构在统一表述「统计 -> 导航/操作 -> 搜索筛选 -> 明细 -> 分页」中的映射关系如下：

| 统一表述 | 本文档五层名称 | 说明 |
|---|---|---|
| 统计 | 全局导航层 | 展示应用选择器、版本号、发布状态等全局定位信息 |
| 导航/操作 | 操作与视图层 | 顶部 Tab、新建/导入/克隆按钮、视图切换等操作入口 |
| 搜索筛选 | 筛选与搜索层 | 关键字搜索、类型/状态/标签筛选等定位配置项 |
| 明细 | 配置内容层 | 字段列表、视图画布、流程图、权限矩阵等具体配置明细 |
| 分页 | 状态与行动层 | 保存、校验、预览、审批、发布、回滚等底部行动区（含列表分页） |

### 5.3 应用市场列表页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 应用市场                                        [+ 新建应用] [导入]  │
├─────────────────────────────────────────────────────────────────────┤
│ [全部] [已启用] [未启用] [待发布]          [搜索应用名称/编码]       │
├─────────────────────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│ │ 经销商协同  │ │ 合规风控    │ │ 目标绩效    │ │ 经营驾驶舱  │   │
│ │ v1.8.0      │ │ v1.8.0      │ │ v1.8.0      │ │ v1.8.0      │   │
│ │ 已启用      │ │ 已启用      │ │ 已启用      │ │ 已启用      │   │
│ │ [配置][停用]│ │ [配置][停用]│ │ [配置][停用]│ │ [配置][停用]│   │
│ └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
│ ┌─────────────┐ ┌─────────────┐                                     │
│ │ 后台设置    │ │ 流程设计器  │                                     │
│ │ v1.8.0      │ │ v1.8.0      │                                     │
│ │ 已启用      │ │ 已启用      │                                     │
│ │ [配置][停用]│ │ [配置][停用]│                                     │
│ └─────────────┘ └─────────────┘                                     │
├─────────────────────────────────────────────────────────────────────┤
│ 分页组件                                                            │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.4 字段设计器页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 应用配置 / 经销商协同 / 字段设计器    当前版本 v1.8.0 [已发布]       │
├─────────────────────────────────────────────────────────────────────┤
│ [字段列表] [对象关系] [校验规则]          [+ 新建字段] [批量导入]   │
├─────────────────────────────────────────────────────────────────────┤
│ 对象：客户 │ 联系人 │ 意向 │ 设备 │ 返利 │ 库存共享              │
├─────────────────────────────────────────────────────────────────────┤
│ 搜索字段名称/编码 │ 类型筛选 │ 必填筛选 │ 状态筛选              │
├─────────────────────────────────────────────────────────────────────┤
│ 字段名称 │ 字段编码 │ 类型 │ 必填 │ 默认值 │ 状态 │ 引用数 │ 操作 │
│ ...      │ ...      │ ...  │ ...  │ ...    │ ...  │ ...    │ ...  │
├─────────────────────────────────────────────────────────────────────┤
│ 保存草稿 │ 校验 │ 预览 │ 提交审批 │ 发布                                │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.5 流程设计器画布页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 应用配置 / 合规风控 / 流程设计器        当前版本 v1.8.0 [草稿]       │
├────────────┬────────────────────────────────────────────────────────┤
│ 节点库     │  工具栏：保存 │ 撤销 │ 重做 │ 校验 │ 预览 │ 发布       │
│            ├────────────────────────────────────────────────────────┤
│ 开始节点   │                                                        │
│ 用户任务   │     ┌─────────┐                                        │
│ 会签节点   │     │  开始   │                                        │
│ 条件分支   │     └────┬────┘                                        │
│ 子流程     │          │                                             │
│ 抄送节点   │          v                                             │
│ 结束节点   │     ┌─────────┐                                        │
│            │     │ 发起人  │                                        │
│            │     │ 提交申请│                                        │
│            │     └────┬────┘                                        │
│            │          │                                             │
│            │          v                                             │
│            │     ┌─────────┐     是                                 │
│            │     │ 条件分支│────────-> 财务审批                       │
│            │     │金额>5万 │                                        │
│            │     └────┬────┘                                        │
│            │          │ 否                                          │
│            │          v                                             │
│            │     ┌─────────┐                                        │
│            │     │ 结束    │                                        │
│            │     └─────────┘                                        │
├────────────┴────────────────────────────────────────────────────────┤
│ 属性面板：选中节点后显示节点名称、审批人、表单字段权限、操作按钮等  │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.6 权限配置页

```
┌─────────────────────────────────────────────────────────────────────┐
│ 应用配置 / 目标绩效 / 权限配置                                       │
├─────────────────────────────────────────────────────────────────────┤
│ [角色权限矩阵] [数据范围] [字段权限] [按钮权限]                       │
├─────────────────────────────────────────────────────────────────────┤
│ 角色：全部 │ 销售代表 │ 销售经理 │ 区域总监 │ 高管 │ 系统管理员    │
├─────────────────────────────────────────────────────────────────────┤
│ 功能/数据项        │ 销售代表 │ 销售经理 │ 区域总监 │ 高管 │ 系统管理员 │
│ 查看目标总览       │  本区域  │  本团队  │  本大区  │  全部  │  全部    │
│ 编辑目标           │    -     │  本团队  │  本大区  │  全部  │  全部    │
│ 下钻到个人         │    -     │  本团队  │  本大区  │  全部  │  全部    │
│ 导出报表           │    -     │    -     │    是    │  是    │  是      │
│ 配置预警阈值       │    -     │    -     │    -     │  是    │  是      │
├─────────────────────────────────────────────────────────────────────┤
│ 保存 │ 预览 │ 发布                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.7 弹窗与抽屉

| 类型 | 使用场景 | 尺寸 |
|---|---|---|
| 字段编辑抽屉 | 新增/编辑字段详细属性 | 右侧抽屉，宽 640px |
| 视图列配置弹窗 | 配置列表展示列、顺序、宽度、排序 | 居中弹窗，宽 720px |
| 流程节点属性抽屉 | 配置节点审批人、表单字段、条件、超时 | 右侧抽屉，宽 560px |
| 版本对比弹窗 | 对比两个版本的配置差异 | 全屏弹窗 |
| 发布审批弹窗 | 填写变更说明、选择灰度策略、提交审批 | 居中弹窗，宽 560px |
| 沙箱预览弹窗 | 模拟指定角色查看业务页面效果 | 新窗口/全屏弹窗 |

### 5.8 页面清单

| 页面名称 | 页面类型 | 所属功能模块 | 对应原型 | 核心组件 |
|---|---|---|---|---|
| 应用市场列表页 | 列表页 | 应用市场管理 | 应用市场页 | 卡片/列表视图、搜索筛选、分页、批量操作 |
| 字段设计器页 | 列表页 / 抽屉 | 字段设计器 | 字段设计页 | 对象 Tab、字段表格、字段编辑抽屉 |
| 视图设计器页 | 列表页 / 弹窗 / 画布 | 视图设计器 | 视图设计页 | 列配置弹窗、视图预览画布、排序/分组设置 |
| 流程设计器画布页 | 画布 / 抽屉 | 流程设计器 | 流程画布页 | 节点库、流程画布、节点属性抽屉、工具栏 |
| 标签管理页 | 列表页 / 弹窗 | 标签与分类管理 | 标签管理页 | 分类树、标签色板、标签编辑弹窗 |
| 权限配置页 | 列表页 / 矩阵页 | 权限模型配置 | 权限配置页 | 角色 Tab、权限矩阵、数据范围设置 |
| 数据字典页 | 列表页 / 弹窗 | 数据字典管理 | 数据字典页 | 字典树、字典项表格、字典编辑弹窗 |
| 集成配置页 | 列表页 / 抽屉 | 集成配置管理 | 集成配置页 | 接口列表、字段映射抽屉、调用日志 |
| 模板库页 | 列表页 / 弹窗 | 模板与克隆 | 模板库页 | 模板卡片、克隆确认弹窗、模板分类 |
| 版本管理页 | 列表页 / 弹窗 | 版本与发布管理 | 版本管理页 | 版本列表、版本对比弹窗、回滚操作 |
| 审计日志页 | 列表页 | 配置变更审计 | 审计日志页 | 操作日志表格、筛选栏、导出按钮 |
| 沙箱预览页 | 弹窗 / 新窗口 | 沙箱预览 | 沙箱预览页 | 角色选择器、模拟页面、权限验证 |
| 字段编辑抽屉 | 抽屉 | 字段设计器 | 字段设计页 | 字段属性表单、校验规则设置、默认值设置 |
| 流程节点属性抽屉 | 抽屉 | 流程设计器 | 流程画布页 | 节点名称、审批人表达式、表单字段权限、超时设置 |
| 发布审批弹窗 | 弹窗 | 版本与发布管理 | 版本管理页 | 变更说明、灰度策略、审批人选择 |

---

## 六、核心字段说明

### 6.1 应用基础信息字段

| 字段名 | 类型 | 必填 | 默认值 | 长度/格式 | 枚举值 | 字段权限 | 说明 |
|---|---|---|---|---|---|---|---|
| 应用编码 | 文本 | 是 | - | 2-50 字符，英文/数字/下划线，全局唯一 | - | 系统管理员可编辑，其他人只读 | 应用的唯一标识，创建后不可修改 |
| 应用名称 | 文本 | 是 | - | 2-100 字符 | - | 系统管理员、应用配置专员可编辑 | 在应用市场展示的名称 |
| 应用类型 | 枚举 | 是 | 业务应用 | 业务应用 / 系统应用 / 扩展应用 | - | 系统管理员可编辑 | 决定应用的生命周期与可见范围 |
| 所属业务域 | 枚举 | 是 | - | 经销商协同 / 合规风控 / 目标绩效 / 经营驾驶舱 / 后台设置 / 流程设计器 / 其他 | - | 系统管理员可编辑 | 用于应用分类与权限模板推荐 |
| 当前版本号 | 文本 | 是 | 1.0.0 | 三段式版本号 | - | 系统自动维护 | 与发布审批联动，发布后自动提升 |
| 发布状态 | 枚举 | 是 | 草稿 | 草稿 / 审批中 / 已发布 / 已停用 | - | 系统自动维护 | 控制应用是否在运行时生效 |
| 依赖应用 | 关联 | 否 | - | 多选，关联其他应用编码 | - | 系统管理员可编辑 | 被依赖应用未启用时无法发布 |
| 租户隔离标识 | 文本 | 是 | 当前租户 | UUID | - | 系统生成，不可见 | 多租户场景下区分配置数据 |
| 创建时间 | 日期时间 | 是 | 系统当前时间 | yyyy-MM-dd HH:mm:ss | - | 只读 | 记录应用创建时间 |
| 更新时间 | 日期时间 | 是 | 系统当前时间 | yyyy-MM-dd HH:mm:ss | - | 只读 | 记录最后更新时间 |

#### 6.1.1 应用发布状态流转表

| 当前状态 | 触发条件 | 下一状态 | 操作人 |
|---|---|---|---|
| 草稿 | 提交发布审批 | 审批中 | 系统管理员 / 应用配置专员 |
| 审批中 | 审批通过 | 已发布 | 业务架构师 / 系统管理员 |
| 审批中 | 审批驳回 | 草稿 | 业务架构师 / 系统管理员 |
| 已发布 | 停用应用 | 已停用 | 系统管理员 |
| 已停用 | 重新启用 | 已发布 | 系统管理员 |
| 已发布 | 创建新版本（系统自动复制） | 草稿（新版本） | 系统管理员 / 应用配置专员 |

### 6.2 字段设计器字段

| 字段名 | 类型 | 必填 | 默认值 | 长度/格式 | 枚举值 | 字段权限 | 说明 |
|---|---|---|---|---|---|---|---|
| 字段编码 | 文本 | 是 | - | 2-50 字符，英文/数字/下划线，应用内唯一 | - | 应用配置专员可编辑 | 数据库列名与接口字段名依据 |
| 字段名称 | 文本 | 是 | - | 2-100 字符 | - | 应用配置专员可编辑 | 页面展示用中文名 |
| 字段类型 | 枚举 | 是 | 文本 | 文本 / 长文本 / 数字 / 金额 / 日期 / 日期时间 / 枚举 / 多选 / 关联对象 / 附件 / 图片 / 公式 / 部门 / 人员 | - | 应用配置专员可编辑 | 类型保存后不可随意修改 |
| 是否必填 | 布尔 | 是 | 否 | true / false | - | 应用配置专员可编辑 | 控制表单校验 |
| 默认值 | 视类型而定 | 否 | - | 按字段类型匹配 | - | 应用配置专员可编辑 | 新增记录时的默认填充值 |
| 长度限制 | 整数 | 否 | 按类型默认 | 1-4000 | - | 应用配置专员可编辑 | 文本/数字类型有效 |
| 小数位数 | 整数 | 否 | 0 | 0-6 | - | 应用配置专员可编辑 | 金额/数字类型有效 |
| 枚举值来源 | 枚举 | 否 | 自定义 | 自定义 / 数据字典 | - | 应用配置专员可编辑 | 选择数据字典时联动字典项 |
| 校验规则 | JSON | 否 | - | 正则/范围/唯一/依赖等 | - | 应用配置专员可编辑 | 支持多种校验规则组合 |
| 字段状态 | 枚举 | 是 | 启用 | 启用 / 停用 / 废弃 | - | 应用配置专员可编辑 | 停用后不再允许新建使用 |
| 引用数 | 整数 | 是 | 0 | >=0 | - | 只读 | 统计被视图/流程/公式引用次数 |

### 6.3 流程设计器字段

| 字段名 | 类型 | 必填 | 默认值 | 长度/格式 | 枚举值 | 字段权限 | 说明 |
|---|---|---|---|---|---|---|---|
| 流程编码 | 文本 | 是 | - | 2-50 字符，应用内唯一 | - | 应用配置专员可编辑 | 审批中心识别流程的依据 |
| 流程名称 | 文本 | 是 | - | 2-100 字符 | - | 应用配置专员可编辑 | 业务端展示名称 |
| 流程版本 | 文本 | 是 | 1.0.0 | 三段式版本号 | - | 系统自动维护 | 每次发布自动递增 |
| 节点类型 | 枚举 | 是 | - | 开始 / 用户任务 / 会签 / 条件分支 / 子流程 / 抄送 / 结束 | - | 应用配置专员可选 | 决定节点在画布上的形态与行为 |
| 节点名称 | 文本 | 是 | - | 2-50 字符 | - | 应用配置专员可编辑 | 画布与审批记录展示 |
| 审批人表达式 | 文本 | 条件必填 | - | 表达式语法 | - | 应用配置专员可编辑 | 支持角色/部门/上级/指定人等 |
| 条件表达式 | 文本 | 条件必填 | - | 表达式语法 | - | 应用配置专员可编辑 | 条件分支节点必填 |
| 表单字段权限 | JSON | 否 | 全部可见 | 可见/可编辑/隐藏/只读配置 | - | 应用配置专员可编辑 | 控制审批表单的字段权限 |
| 超时时间 | 整数 | 否 | 0 | 0-9999，单位小时 | - | 应用配置专员可编辑 | 0 表示不超时 |
| 超时提醒方式 | 多选 | 否 | 消息通知 | 消息通知 / 邮件 / 短信 / 企业微信 | - | 应用配置专员可编辑 | 可多选 |
| 流程状态 | 枚举 | 是 | 草稿 | 草稿 / 已发布 / 已停用 | - | 系统自动维护 | 发布后新实例生效 |

#### 6.3.1 流程版本状态流转表

| 当前状态 | 触发条件 | 下一状态 | 操作人 |
|---|---|---|---|
| 草稿 | 保存草稿 | 草稿 | 应用配置专员 |
| 草稿 | 校验通过并发布 | 已发布 | 应用配置专员 / 系统管理员 |
| 已发布 | 停用流程 | 已停用 | 系统管理员 / 应用配置专员 |
| 已停用 | 重新启用 | 已发布 | 系统管理员 / 应用配置专员 |
| 已发布 | 创建新版本（系统自动复制） | 草稿（新版本） | 应用配置专员 |

### 6.4 权限配置字段

| 字段名 | 类型 | 必填 | 默认值 | 长度/格式 | 枚举值 | 字段权限 | 说明 |
|---|---|---|---|---|---|---|---|
| 角色编码 | 文本 | 是 | - | 2-50 字符 | - | 系统管理员可编辑 | 关联组织角色体系 |
| 数据可见范围 | 枚举 | 是 | 本人 | 本人 / 本部门 / 本部门及下属 / 本区域 / 本大区 / 全部 / 自定义 | - | 应用配置专员可编辑 | 控制列表数据可见性 |
| 字段权限规则 | JSON | 是 | - | 字段级可见/编辑/隐藏/脱敏配置 | - | 应用配置专员可编辑 | 支持按角色配置字段权限 |
| 操作权限 | 多选 | 是 | 查看 | 查看 / 新建 / 编辑 / 删除 / 导出 / 导入 / 发布 / 审批 | - | 应用配置专员可编辑 | 控制页面按钮显隐 |
| 自定义数据范围表达式 | 文本 | 否 | - | 表达式语法 | - | 高级权限，需审批 | 满足复杂数据隔离场景 |
| 脱敏规则绑定 | 关联 | 否 | - | 关联脱敏规则 | - | 安全合规专员可编辑 | 对敏感字段按规则脱敏 |

### 6.5 复杂字段示例值

| 字段名 | 所属对象 | 示例值 | 说明 |
|---|---|---|---|
| 依赖应用 | 应用基础信息 | `["dealer_collab", "compliance_mgmt"]` | 多选关联其他应用编码，空数组表示无依赖 |
| 校验规则 | 字段设计器 | `{"type":"regex","pattern":"^1[3-9]\\d{9}$","message":"手机号格式错误"}` | JSON 对象，可组合正则、范围、唯一、依赖等多种规则 |
| 审批人表达式 | 流程设计器 | `${发起人.部门负责人}` | 支持角色、部门、上级、指定人等表达式语法 |
| 条件表达式 | 流程设计器 | `${预算金额} > 50000` | 条件分支节点判断规则，可引用表单字段与系统变量 |
| 表单字段权限 | 流程设计器 | `[{"field":"预算金额","permission":"editable"},{"field":"供应商","permission":"readonly"}]` | 控制审批表单中各字段的可见/可编辑/只读/隐藏权限 |
| 字段权限规则 | 权限配置 | `[{"field":"返利金额","permission":"desensitize"}]` | 字段级可见/编辑/隐藏/脱敏配置，按角色生效 |
| 自定义数据范围表达式 | 权限配置 | `${部门编码} IN ${当前用户.下属部门编码}` | 高级数据隔离场景，需经审批后启用 |
| 脱敏规则绑定 | 权限配置 | `mask_rule_001` | 关联脱敏规则编码，对敏感字段按规则脱敏 |

---

## 七、交互设计要点

### 7.1 搜索筛选

- 全局搜索框位于顶部，支持按应用名称、编码、字段名称、流程名称模糊搜索。
- 列表页支持按类型、状态、版本、业务域、更新时间等多维度筛选。
- 筛选条件支持保存为「常用视图」，下次进入默认加载。
- 复杂筛选（如流程节点条件表达式）提供「高级筛选」展开面板。

### 7.2 视图切换

- 应用市场支持卡片视图与列表视图切换，卡片视图展示应用图标、名称、版本、状态与操作按钮；列表视图展示完整字段与批量操作。
- 字段设计器支持「平铺列表」与「按对象分组」两种视图。
- 流程设计器支持「设计视图」与「XML/JSON 源码视图」切换，便于研发排查。

### 7.3 操作反馈

- 保存草稿后：右下角显示「草稿已保存」文字提示，并显示最后保存时间。
- 校验通过/失败：顶部显示校验结果摘要，失败项以红色文字高亮并支持一键定位。
- 发布成功/失败：弹窗展示发布结果，失败时提供错误码与日志入口。
- 删除字段/节点前：二次确认弹窗，提示引用数与影响面。

### 7.4 空态

- 应用市场无应用：展示「暂无应用」占位图与「新建应用」按钮。
- 字段列表为空：提示「当前对象下暂无字段，点击新建」。
- 流程画布为空：默认放置一个「开始」节点占位，引导用户拖拽。
- 审计日志为空：提示「暂无配置变更记录」。

### 7.5 快捷操作

- 字段设计器支持「批量导入」与「从模板复制」。
- 视图设计器支持「一键恢复默认视图」。
- 流程设计器支持「复制节点」「粘贴节点」「自动排版」「对齐参考线」。
- 权限配置页支持「从其他应用复制权限矩阵」。

### 7.6 异常处理

- 表单校验失败时，滚动到第一个错误字段并高亮边框。
- 网络异常时，自动重试 3 次，失败后提示「保存失败，请检查网络」，并提供「重新提交」按钮。
- 并发保存冲突时，提示「配置已被他人修改，请刷新后重试」。
- 发布失败时，保留草稿状态，允许用户查看详细错误日志并修正。

---

## 八、权限与数据范围设计

### 8.1 角色权限矩阵

| 功能模块 | 系统管理员 | 应用配置专员 | 业务架构师 | 安全合规专员 | 研发实施工程师 | 普通用户 |
|---|---|---|---|---|---|---|
| 应用市场管理 | 全部 | 查看 | 查看 | - | 查看 | - |
| 字段设计器 | 全部 | 全部 | 查看 | - | 查看 | - |
| 视图设计器 | 全部 | 全部 | 查看 | - | 查看 | - |
| 流程设计器 | 全部 | 全部 | 审批 | - | 查看/调试 | - |
| 标签与分类管理 | 全部 | 全部 | 查看 | 部分 | 查看 | - |
| 权限模型配置 | 全部 | 全部 | 审批 | 全部 | 查看 | - |
| 数据字典管理 | 全部 | 全部 | 审批 | 部分 | 查看 | 查看 |
| 集成配置管理 | 全部 | 查看 | 查看 | - | 全部 | - |
| 模板与克隆 | 全部 | 全部 | 审批 | - | 查看 | - |
| 版本与发布管理 | 全部 | 提交/查看 | 审批 | - | 查看 | - |
| 配置变更审计 | 全部 | 查看本人 | 查看 | 全部 | 查看 | - |
| 沙箱预览 | 全部 | 全部 | 全部 | - | 全部 | - |

### 8.2 数据可见性

| 数据类型 | 系统管理员 | 应用配置专员 | 业务架构师 | 安全合规专员 | 研发实施工程师 |
|---|---|---|---|---|---|
| 全部租户配置 | 可见 | 不可见 | 不可见 | 不可见 | 按项目授权 |
| 本租户应用配置 | 可见 | 可见 | 可见 | 可见 | 按项目授权 |
| 本应用配置明细 | 可见 | 可见 | 可见 | 可见 | 按项目授权 |
| 操作日志与审计 | 全部 | 本人操作 | 全部 | 全部 | 本人操作 |
| 敏感配置项（密钥、密码） | 可见（脱敏） | 不可见 | 不可见 | 可见（脱敏） | 不可见 |

### 8.3 字段级权限

| 字段类型 | 普通用户 | 业务管理员 | 安全合规专员 | 系统管理员 | 说明 |
|---|---|---|---|---|---|
| 普通字段 | 按角色可见/编辑 | 可见/编辑 | 可见 | 可见/编辑 | 常规业务字段 |
| 敏感字段 | 脱敏/隐藏 | 脱敏 | 可见 | 可见 | 如客户决策链、返利金额 |
| 系统字段 | 只读 | 只读 | 只读 | 可见/编辑 | 创建时间、版本号等 |
| 审计字段 | 不可见 | 部分可见 | 可见 | 可见 | 操作人、操作 IP、变更前后值 |
| 集成密钥字段 | 不可见 | 不可见 | 脱敏 | 脱敏 | 密钥展示为星号，仅支持重置 |

### 8.4 脱敏规则

| 脱敏对象 | 默认规则 | 可配置 | 应用场景 |
|---|---|---|---|
| 手机号 | 中间 4 位替换为 * | 是 | 联系人手机号 |
| 身份证号 | 显示前 3 后 4，其余 * | 是 | 飞检抽查人员信息 |
| 银行卡号 | 显示后 4 位 | 是 | 经销商返利打款账户 |
| 邮箱 | 显示前 2 字符与域名 | 是 | 联系人邮箱 |
| 金额 | 按区间模糊化 | 是 | 非高管查看大额返利 |
| 自定义字段 | 正则表达式替换 | 是 | 特殊敏感词或编号 |

---

## 九、核心设计原则与要点

### 9.1 核心设计原则

| 原则 | 说明 |
|---|---|
| **配置驱动** | 业务变化优先通过配置解决，减少代码变更与版本发布。 |
| **租户隔离** | 每个租户拥有独立的应用配置空间，避免配置相互覆盖。 |
| **版本可控** | 每次重大配置变更生成新版本，支持对比、回滚与灰度发布。 |
| **最小权限** | 默认只授予必要权限，敏感操作需二次确认或审批。 |
| **影响面可视化** | 修改字段/流程/权限前，系统主动提示引用数与受影响模块。 |
| **配置即代码** | 支持将配置导出为 JSON/YAML，纳入版本控制与 CI/CD。 |
| **前后端一致** | 配置元数据同时驱动前端渲染与后端校验，避免两端逻辑割裂。 |

### 9.2 关键设计要点

- 应用配置模块本身也是由元数据驱动的，便于后续自举扩展。
- 流程设计器与审批中心解耦：设计器负责建模，审批中心负责执行，通过发布动作同步。
- 字段设计器中的「公式类型」需由后端表达式引擎统一解析，避免前端单独计算。
- 视图设计器中的「列表/卡片视图切换」需与运行时页面一致，配置即所见。
- 权限配置采用「角色 + 数据范围 + 字段权限 + 按钮权限」四层模型，覆盖大多数业务场景。
- 所有配置变更必须记录审计日志，敏感操作（发布、删除字段、修改权限）触发审批。
- 集成配置中的密钥信息必须加密存储，界面展示脱敏，调用时通过安全网关透传。

---

## 十、与其它模块的关联

| 关联模块 | 关联方式 | 说明 |
|---|---|---|
| **组织用户管理** | 数据引用 | 应用配置中的角色权限矩阵引用组织用户模块的角色、部门、人员数据。 |
| **审批中心** | 流程发布联动 | 流程设计器发布流程后，审批中心按流程版本驱动审批实例。 |
| **客户 360°** | 字段/视图供给 | 客户 360° 页面的字段、视图、标签由应用配置模块提供元数据。 |
| **意向管理** | 字段/流程供给 | 意向报备的表单字段、审批流程、保护期规则由应用配置模块定义。 |
| **线索管理** | 字段/视图/分配规则供给 | 线索池的字段、视图、分配规则、回收规则由应用配置模块定义。 |
| **经销商协同** | 应用级配置 | 渠道授权、区域保护、返利引擎、库存共享、任务协同均为独立应用或应用内模块。 |
| **合规风控** | 应用级配置 | 拜访合规、学术活动审批、样品管理、飞检抽查、敏感词审计的配置源头。 |
| **目标绩效** | 指标与视图配置 | 目标拆解模板、达成跟踪视图、同比环比计算规则、预警阈值由应用配置模块定义。 |
| **经营驾驶舱** | 指标与权限配置 | 驾驶舱的指标模型、图表视图、实时刷新策略、AI 问答入口由应用配置模块定义。 |
| **后台设置** | 系统级配置 | 数据字典、日志审计、集成网关、系统参数由应用配置模块提供配置界面。 |
| **消息通知中心** | 事件订阅 | 流程超时、配置发布成功/失败、审批结果通过事件订阅通知相关人员。 |
| **数据字典** | 被引用 | 字段设计器中的枚举类型可引用数据字典，保证全系统枚举一致。 |

---

## 十一、未考虑到的问题补充

以下是从开发者视角梳理、当前文档尚未深入展开但在落地时需要关注的点：

1. **元数据缓存一致性**
   应用配置发布后，前端页面、后端接口、流程引擎均需重新加载元数据。需要明确缓存刷新机制（主动推送 / 定时刷新 / 懒加载），以及发布期间正在进行的业务流程如何处理。

2. **多环境配置迁移**
   开发环境、测试环境、生产环境的应用配置如何同步？是否支持导出 JSON/YAML 后通过 CI/CD 流水线自动化部署，还是仅允许在生产环境手动配置？

3. **字段类型变更的数据迁移**
   当字段类型从文本改为数字，或从单选改为多选时，历史数据如何转换？是否需要提供数据迁移脚本或转换规则配置界面？

4. **流程版本并发执行策略**
   流程发布后，旧版本实例继续按旧规则执行，新版本实例按新规则执行。需要明确实例迁移、强制升级、异常终止的策略与界面。

5. **权限表达式的性能边界**
   复杂的数据范围表达式（如「本部门及下属所有销售负责的客户」）在大数据量下可能导致查询性能问题。需要评估是否需要预计算权限视图或引入缓存。

6. **自定义节点的前后端协同**
   流程设计器支持扩展自定义节点时，前端画布、属性面板、后端执行逻辑、审批中心如何统一注册？是否需要定义节点 SDK 规范？

7. **应用依赖的循环依赖检测**
   应用之间可以设置依赖关系，需要在校验阶段检测循环依赖并阻断发布，否则可能导致运行时加载失败。

8. **沙箱预览的数据隔离**
   沙箱预览需要模拟真实角色与数据，但不能污染生产数据。需要明确沙箱是否使用影子数据、只读副本，还是虚拟租户。

9. **审计日志的存储与查询性能**
   应用配置变更频繁，审计日志数据量可能很大。需要设计日志归档策略、索引优化与查询权限控制。

10. **集成配置的失败重试与降级**
    应用配置中的集成接口在调用外部系统失败时，是否需要重试、降级、告警？是否需要支持熔断与限流配置？

11. **AI 问答入口的语义解析能力**
    经营驾驶舱预留的 AI 问答入口，需要预先定义指标语义库与同义词映射。当前文档仅提及入口预留，未涉及自然语言到指标查询的转换规则。

12. **移动端配置适配**
    应用配置中定义的视图、字段、流程是否需要在移动端自动适配？复杂表格、流程图在小程序/企业微信中的展示与交互方案需要单独设计。

---

## 附录：术语表

| 术语 | 说明 |
|---|---|
| 应用 | 鑫渠 CRM 中可独立启用、配置、发布的一个业务单元。 |
| 元数据 | 描述数据结构、业务规则、流程、权限的配置信息。 |
| 租户 | 鑫渠 CRM 的多客户隔离单位，每个租户拥有独立的配置与数据空间。 |
| 流程实例 | 流程发布后被业务端发起的一次具体审批/业务流程。 |
| 数据范围 | 控制用户可查看的数据集合，通常按本人/本部门/本区域/全部等维度划分。 |
| 字段级权限 | 控制用户对某个字段的可见、编辑、隐藏、脱敏等权限。 |
| 灰度发布 | 将新版本配置先对部分用户或租户生效，观察无问题后再全量发布。 |

---

> 文档结束。后续迭代请在「版本记录」中补充变更，并同步更新「文档基础信息」中的版本号。
