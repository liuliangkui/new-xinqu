#!/bin/sh
# Git pre-commit hook: 提交前检查 HTML 结构平衡
# 安装方式：git config core.hooksPath scripts/hooks

python3 scripts/check-html.py
if [ $? -ne 0 ]; then
    echo ""
    echo "提交被拦截：请先修复 HTML 结构问题。"
    exit 1
fi
