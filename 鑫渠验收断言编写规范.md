# 鑫渠验收断言编写规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **适用范围**：鑫渠 CRM 全部应用模块功能验收  
> **文档性质**：让 AI 开发完成后可自动或半自动验证功能正确性的断言标准

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：定义断言格式、分类、编写标准、示例库 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、断言编写原则

1. **可验证**：每条断言必须能通过代码执行、截图对比或人工操作验证。
2. **原子性**：一条断言只验证一个具体行为。
3. **无歧义**：预期结果必须量化、具体、无二义。
4. **覆盖完整**：每个核心功能至少 3 条断言，覆盖正常流、异常流、边界条件。
5. **双端覆盖**：PC 端和移动端分别编写断言，移动端增加手势相关断言。

---

## 二、断言格式

每条断言必须包含以下 5 个部分：

```markdown
#### 断言编号：断言名称

- **验证对象**：哪个功能/组件/页面
- **前置条件**：用户状态、数据状态、环境状态
- **操作步骤**：具体用户操作（1、2、3...）
- **预期结果**：具体的输出、页面变化、数据变化
- **验收方式**：自动化测试 / 截图对比 / 人工验证
```

---

## 三、断言分类

### 3.1 功能断言

验证业务功能是否按预期工作。

示例：

```markdown
#### 断言 1：新建意向成功

- **验证对象**：意向新建功能
- **前置条件**：用户角色为报单人，已进入意向列表页
- **操作步骤**：
  1. 点击「+ 新建意向」
  2. 填写客户名称、项目名称、业务类型
  3. 点击「提交」
- **预期结果**：
  1. 弹窗关闭
  2. 列表顶部出现新建的意向记录
  3. 状态为「审批中」
  4. 系统提示「提交成功」
- **验收方式**：自动化测试
```

### 3.2 表单校验断言

验证表单校验规则是否正确。

示例：

```markdown
#### 断言 2：新建意向必填校验

- **验证对象**：意向新建表单
- **前置条件**：用户已进入新建意向弹窗
- **操作步骤**：
  1. 不填写任何字段
  2. 点击「提交」
- **预期结果**：
  1. 客户名称、项目名称字段边框变红
  2. 字段下方显示「此项为必填项」
  3. 提交接口未被调用
- **验收方式**：自动化测试
```

### 3.3 搜索筛选断言

验证搜索、筛选、排序功能。

示例：

```markdown
#### 断言 3：意向搜索支持拼音首字母

- **验证对象**：意向列表搜索框
- **前置条件**：列表中存在负责人为「张三」的意向
- **操作步骤**：
  1. 在搜索框输入「zs」
  2. 按回车
- **预期结果**：
  1. 列表只显示负责人包含「张三」的意向
  2. 总条数更新为匹配数量
  3. 清空搜索后列表恢复
- **验收方式**：自动化测试
```

### 3.4 权限断言

验证不同角色可见范围和操作权限。

示例：

```markdown
#### 断言 4：销售代表只能查看本人意向

- **验证对象**：意向数据范围
- **前置条件**：系统中存在 A 销售和 B 销售的意向
- **操作步骤**：
  1. 使用 A 销售账号登录
  2. 进入意向管理
- **预期结果**：
  1. 列表只显示 A 销售的意向
  2. 不显示 B 销售的意向
  3. URL 中无权限参数时系统默认按当前用户过滤
- **验收方式**：自动化测试
```

### 3.5 响应式断言

验证 PC 端和移动端布局一致性。

示例：

```markdown
#### 断言 5：移动端意向列表底部按钮固定

- **验证对象**：移动端意向列表页
- **前置条件**：使用 iPhone 14 尺寸模拟器
- **操作步骤**：
  1. 进入意向列表页
  2. 向下滚动列表
- **预期结果**：
  1. 「+ 新建意向」按钮固定在屏幕右下角
  2. 按钮不随列表滚动消失
  3. 点击按钮正常打开新建弹窗
- **验收方式**：截图对比 / 自动化测试
```

### 3.6 性能断言

验证系统性能是否达标。

示例：

```markdown
#### 断言 6：意向列表加载时间

- **验证对象**：意向列表查询接口
- **前置条件**：数据库中有 1000 条意向记录
- **操作步骤**：
  1. 进入意向列表页
  2. 等待列表加载完成
- **预期结果**：
  1. 首屏渲染时间 <= 1.5 秒
  2. 接口响应时间 <= 800 毫秒
  3. 翻页响应时间 <= 500 毫秒
- **验收方式**：性能测试工具
```

---

## 四、断言验收方式

| 验收方式 | 适用场景 | 工具建议 |
|---|---|---|
| 自动化测试 | 功能、表单、搜索、权限 | Playwright / Cypress |
| 截图对比 | 视觉还原、响应式布局 | Playwright + pixelmatch |
| 接口测试 | 接口契约、数据正确性 | Jest + MSW / Postman |
| 性能测试 | 加载速度、并发能力 | Lighthouse / k6 |
| 人工验证 | 复杂交互、业务流程 | 人工按断言清单操作 |

---

## 五、断言编写红线

禁止出现以下写法：

| 错误写法 | 正确写法 |
|---|---|
| 「搜索应该好用」 | 「输入『zs』应匹配『张三』」 |
| 「页面加载快」 | 「首屏渲染时间 <= 1.5 秒」 |
| 「权限正确」 | 「A 销售账号看不到 B 销售的意向」 |
| 「移动端正常」 | 「iPhone 14 模拟器下底部按钮固定，不遮挡内容」 |
| 「保存成功」 | 「点击保存后弹窗关闭，列表数据更新，提示『保存成功』」 |

---

## 六、断言与用例对应关系

每个核心功能对应一个测试用例集合，结构如下：

```markdown
### 功能：意向新建

- 断言 1：正常新建成功
- 断言 2：必填校验
- 断言 3：客户不存在时新建
- 断言 4：重复提交拦截
- 断言 5：移动端新建
```

---

## 七、相关文档索引

- 《AI可执行设计文档编写规范.md》
- 《鑫渠应用文档验收标准.md》
- 《鑫渠系统技术实现白皮书.html》

---

*本规范是鑫渠 CRM 质量保证的基础设施，所有应用文档必须按此标准编写验收断言。*
