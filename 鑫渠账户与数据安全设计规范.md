# 鑫渠账户与数据安全设计规范

> **文档版本**：v1.0  
> **更新日期**：2026-07-29  
> **对应原型**：《鑫渠高保真原型.html》v1.8.0  
> **关联文档**：《鑫渠系统技术实现路线规划方案.html》《鑫渠技术架构与领域驱动设计规范.md》  
> **适用范围**：鑫渠 CRM 账户管理、数据生命周期管理、数据安全与合规  
> **文档性质**：后续系统后台设计与安全开发指导规范

---

## 版本记录

| 版本 | 更新日期 | 更新内容 | 更新人 |
|---|---|---|---|
| v1.0 | 2026-07-29 | 初始发布：账户管理、账户移交、数据备份恢复、数据初始化、分批导入、数据安全防泄漏、多租户扩展 | - |

> 约定：每次更新本文档时，须在「文档基础信息」中提升版本号，并在「版本记录」中补充一行。

---

## 一、账户管理

### 1.1 账户生命周期

账户从创建到注销经历完整生命周期，每个状态对应不同的操作权限和数据可见范围。

```
创建 → 待激活 → 启用 → 冻结/锁定 → 禁用 → 注销
            ↓        ↓
           休假     离职/调岗
```

| 状态 | 说明 | 权限 |
|---|---|---|
| 待激活 | 已创建但未设置密码 | 无法登录 |
| 启用 | 正常使用 | 按角色权限正常访问 |
| 冻结 | 异常登录或管理员临时冻结 | 无法登录，数据保留 |
| 锁定 | 登录失败次数超限 | 一段时间后自动解锁或管理员解锁 |
| 禁用 | 长期不使用或离职前过渡 | 无法登录，数据保留 |
| 注销 | 彻底删除或归档 | 无法登录，数据按策略移交或归档 |

### 1.2 账户类型

| 类型 | 权限范围 | 典型角色 |
|---|---|---|
| 平台超级管理员 | 整个 SaaS 平台 | 系统运维 |
| 租户管理员 | 单个租户全局 | 企业 IT 管理员 |
| 应用管理员 | 单个应用配置 | 业务管理员 |
| 部门管理员 | 本部门数据与用户 | 区域经理 |
| 普通用户 | 按角色和数据范围 | 销售代表、售后工程师 |
| 外部用户 | 受限数据和操作 | 经销商账号、客户门户账号 |

### 1.3 账户安全策略

- **密码策略**：最小长度 8 位，必须包含大小写字母、数字、特殊字符；90 天强制更换；不能与最近 5 次密码重复。
- **登录失败锁定**：连续 5 次失败锁定 30 分钟，触发通知管理员。
- **会话管理**：无操作 30 分钟自动退出；同一账号最多 3 个并发会话；支持管理员强制下线。
- **多因素认证（MFA）**：管理员账号强制开启，普通用户可选，支持短信/邮箱/OTP。
- **登录审计**：记录登录 IP、设备、时间、地理位置，异常登录触发告警。

### 1.4 账户与角色解耦

采用 RBAC 模型，一个账户可绑定多个角色，角色可继承。权限按以下维度控制：

- 功能权限：能否访问某个页面/按钮
- 数据权限：能看哪些范围的数据（本人/本部门/本大区/全部）
- 字段权限：能否查看/编辑某个字段
- 操作权限：能否执行分配、审批、导出等操作

---

## 二、账户移交

### 2.1 移交场景

| 场景 | 触发方式 | 处理要求 |
|---|---|---|
| 离职 | 管理员发起 | 必须完成数据移交后才能注销 |
| 调岗 | 管理员发起或本人申请 | 按新岗位重新分配数据范围 |
| 休假/长期外出 | 管理员临时指定 | 临时接管，保留原负责人关系 |
| 组织调整 | 批量调整 | 支持批量移交和权限变更 |

### 2.2 移交内容

移交时可以选择性转移以下对象：

- 客户：负责人、协同人
- 线索：归属人
- 意向：报单人、跟进人
- 任务：负责人
- 日程：参与人/负责人
- 设备：服务责任人
- 工单：处理人
- 待办：审批待办、任务待办

### 2.3 移交规则

1. **历史记录保留**：移交不改变历史操作日志，原负责人仍可在日志中追溯。
2. **时间线记录**：移交动作本身作为一条记录写入相关对象时间线。
3. **数据权限自动更新**：接交人自动获得移交对象的数据权限。
4. **重要移交需审批**：涉及高管、大客户的移交需要上级审批。
5. **冲突处理**：若接交人已有冲突对象（如同客户同项目生效意向），系统提示冲突并给出处理建议。
6. **批量移交支持**：支持按部门、按区域、按产品线批量选择对象移交。

### 2.4 移交流程

```
发起移交 → 选择移交人/接交人 → 选择移交对象类型和范围
    → 校验冲突 → 提交审批（必要时） → 执行移交
    → 更新权限 → 通知相关方 → 记录日志
```

---

## 三、数据备份与恢复

### 3.1 备份策略

| 备份类型 | 频率 | 保留周期 | 存储位置 |
|---|---|---|---|
| 全量备份 | 每日 02:00 | 30 天 | 本地 + 异地 |
| 增量备份 | 每小时 | 7 天 | 本地 + 异地 |
| 事务日志备份 | 实时 | 24 小时 | 本地 + 异地 |
| 配置文件备份 | 变更时触发 | 90 天 | 版本控制 + 异地 |
| 对象存储备份 | 每日同步 | 180 天 | 异地 |

### 3.2 恢复策略

| 恢复粒度 | 适用场景 | 实现方式 |
|---|---|---|
| 按时间点恢复（PITR） | 误操作、数据损坏 | 全量 + 增量 + 事务日志 |
| 按租户恢复 | 单个租户数据异常 | 租户隔离备份 |
| 按应用恢复 | 单个应用数据异常 | 应用级备份 |
| 按对象恢复 | 单条/批量数据恢复 | 回收站 + 历史版本 |
| 按文件恢复 | 附件误删 | 对象存储版本控制 |

### 3.3 备份验证

- 每月至少一次恢复演练。
- 定期校验备份文件完整性（校验和）。
- 备份恢复时间目标（RTO）：核心数据 < 4 小时。
- 备份恢复点目标（RPO）：核心数据 < 15 分钟。

### 3.4 数据归档

- 超过 2 年的历史操作日志自动归档到低成本存储。
- 已结束超过 3 年的业务数据可归档，归档后仍可查询但不可修改。
- 归档数据加密存储，保留审计能力。

---

## 四、数据初始化

### 4.1 租户初始化

新租户注册时自动初始化：

- 默认组织架构（总部、大区、省区）
- 默认角色（租户管理员、销售总监、区域经理、销售代表）
- 默认权限矩阵
- 通用数据字典（地区、医院等级、科室、产品线）
- 默认工作流模板（意向审批、设备投放审批）
- 默认标签和色板
- 示例数据（可选）

### 4.2 应用初始化

每个应用首次启用时初始化：

- 默认字段和字段权限
- 默认列表/卡片视图
- 默认筛选条件
- 默认 KPI 卡片
- 默认标签
- 默认流程模板

### 4.3 数据模板

提供标准化导入模板：

- 客户导入模板
- 线索导入模板
- 设备导入模板
- 试剂导入模板
- 经销商导入模板
- 联系人导入模板

模板包含字段说明、必填标识、示例数据、枚举值约束。

---

## 五、数据分批导入

### 5.1 导入方式

| 方式 | 适用场景 | 限制 |
|---|---|---|
| Excel/CSV 模板导入 | 业务人员批量导入 | 单次不超过 1 万条 |
| API 批量导入 | 系统集成、历史迁移 | 按 API 限流 |
| 数据库同步 | 大规模历史数据迁移 | 需要 DBA 参与 |

### 5.2 导入流程

```
上传文件 → 格式校验 → 数据预览 → 重复校验 → 权限校验
    → 分批处理 → 写入数据库 → 生成报告 → 发送通知
```

### 5.3 分批处理机制

- 前端分片上传，每片 5MB。
- 后端将导入任务拆分为小批次（每批 500-1000 条）。
- 使用消息队列异步处理，避免阻塞用户操作。
- 每批独立事务，失败仅回滚该批，不影响其他批次。
- 支持断点续传和任务重试。

### 5.4 校验规则

- **格式校验**：必填字段、数据类型、长度、日期格式、枚举值。
- **重复校验**：根据业务键判断是否重复（如客户名称+地区）。
- **权限校验**：只能导入自己有权限的数据范围。
- **关联校验**：导入设备必须关联存在的客户；导入试剂必须关联存在的设备型号。
- **冲突校验**：导入客户若已存在生效意向，需要特别标记。

### 5.5 导入结果

- 实时显示导入进度条。
- 导入完成后生成报告：成功数、失败数、重复数、跳过数。
- 失败数据可下载错误文件，标注错误原因。
- 成功数据自动触发后续流程（如线索自动分配）。

---

## 六、数据安全与防泄漏

### 6.1 数据分类分级

| 级别 | 数据类型 | 保护要求 |
|---|---|---|
| 公开 | 品牌信息、产品公开资料 | 常规访问控制 |
| 内部 | 销售数据、客户基础信息 | RBAC + 数据范围 |
| 敏感 | 客户决策链、价格、返利 | 字段权限 + 脱敏 + 审计 |
| 高度敏感 | 身份证号、银行账户、合同扫描件 | 加密 + 严格审计 + 导出审批 |

### 6.2 加密策略

| 场景 | 加密方式 | 说明 |
|---|---|---|
| 传输加密 | HTTPS/TLS 1.3 | 全站强制 HTTPS |
| 存储加密 | AES-256 | 敏感字段加密存储 |
| 数据库加密 | TDE | 数据库透明加密 |
| 备份加密 | AES-256 | 备份文件加密 |
| 密钥管理 | KMS | 密钥轮换，分离存储 |

### 6.3 数据脱敏

| 场景 | 脱敏方式 |
|---|---|
| 前端展示 | 手机号 138****1234、身份证 110**********1234 |
| 列表展示 | 价格显示为 ***，点击查看需权限 |
| 导出 | 无导出权限字段自动脱敏或禁止导出 |
| 测试环境 | 生产数据脱敏后才能导入 |
| 外部用户 | 经销商账号默认脱敏敏感字段 |

### 6.4 权限与访问控制

- **最小权限原则**：只授予完成工作所需的最小权限。
- **职责分离**：审批人与执行人分离，配置人与使用人分离。
- **动态权限**：根据对象状态、组织架构、数据范围动态计算权限。
- **权限审计**：定期审查账号权限，发现越权及时回收。

### 6.5 审计日志

| 日志类型 | 记录内容 | 保留周期 |
|---|---|---|
| 操作日志 | 谁在什么时间做了什么操作 | 5 年 |
| 访问日志 | 谁查看了什么数据 | 3 年 |
| 导出日志 | 谁导出了什么数据、多少条 | 5 年 |
| 登录日志 | 登录时间、IP、设备、结果 | 3 年 |
| 权限变更日志 | 角色/权限/数据范围变更 | 永久 |
| 数据变更日志 | 字段级变更前后值 | 5 年 |

### 6.6 防泄漏措施

- **屏幕水印**：页面和导出文件显示当前用户名、时间，震慑泄露行为。
- **导出审批**：敏感数据导出需要审批，记录导出用途。
- **API 限流**：防止批量爬取数据，异常访问自动限流和告警。
- **DLP 集成**：与数据丢失防护系统集成，监控异常外传。
- **敏感数据识别**：自动识别身份证号、手机号、银行卡号等敏感信息。
- **离职风控**：离职账号自动回收权限，禁止导出和复制。
- **沙箱环境**：测试、开发环境使用脱敏数据，禁止接触生产数据。

### 6.7 数据销毁

- 租户注销后，数据按合同约定保留 30-180 天后彻底删除。
- 删除前进行匿名化处理，保留统计分析价值。
- 物理删除后进行数据覆写，确保不可恢复。
- 销毁操作记录审计日志，永久保留。

---

## 七、多租户扩展准备

### 7.1 租户隔离策略

鑫渠当前为单租户使用，但中台和领域设计已为多租户预留扩展能力。未来可采用以下隔离策略：

| 隔离级别 | 实现方式 | 适用场景 |
|---|---|---|
| 共享数据库 + 租户字段 | 所有表增加 `tenant_id` | 中小租户，成本低 |
| 独立 Schema | 每个租户独立 Schema | 中大型租户，平衡成本与隔离 |
| 独立数据库 | 每个租户独立数据库实例 | 大客户、强合规要求 |
| 独立部署 | 每个租户独立应用集群 | 超大规模客户 |

推荐演进路径：第一阶段共享数据库 + 租户字段，第二阶段对头部客户提供独立 Schema/数据库选项。

### 7.2 多租户下的配置分层

```
平台级默认配置（平台运维）
    ↓ 继承
租户级配置（租户管理员）
    ↓ 继承
应用级配置（应用管理员）
    ↓ 继承
用户级偏好（个人）
```

- **平台级**：SaaS 平台基础参数、默认套餐限制。
- **租户级**：品牌色、组织架构、角色权限、数据字典、系统参数。
- **应用级**：字段、视图、流程、标签、业务规则。
- **用户级**：主题模式、字号、界面密度、视图偏好。

### 7.3 多租户下的数据权限

- 所有业务查询必须带 `tenant_id` 过滤。
- 共享内核（工作流、权限、通知）按租户隔离。
- 超级管理员可跨租户查看，但操作需审计。
- 租户间数据完全隔离，不可互相访问。

### 7.4 多租户下的资源限制

- 按套餐限制用户数、存储空间、API 调用次数、AI 调用次数。
- 支持租户级资源监控和告警。
- 超限时触发告警并可临时扩容。

---

## 八、相关文档索引

- 《鑫渠系统技术实现路线规划方案.html》
- 《鑫渠技术架构与领域驱动设计规范.md》
- 《鑫渠设计与开发规范.md》
- 《鑫渠全局设计系统规范.md》

---

*本文档为鑫渠 CRM 账户与数据安全的指导性文件，后续开发中应逐条落地并纳入验收检查。*
